# Codex feedback — 2026-07-12_233107

Auto-generated by root `scripts/codex_review.sh`. The Codex banner below (version, model,
session id) is emitted by Codex itself — that is the verification it actually ran.

- generated_at: 2026-07-12T23:31:07-04:00
- codex_cli: codex-cli 0.141.0
- mode: plan
- reasoning_effort: xhigh
- cwd: /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
- git_head: b319daa

-----------------------------------------------------------------------

Reading additional input from stdin...
OpenAI Codex v0.141.0
--------
workdir: /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019f5987-53f4-7e30-a101-c35e4aa95ae4
--------
user
# Adversarial re-review (ROUND 2) — earnings_agent Consensus Metrics Preview

Round 1 found 3 Highs, all now FIXED in the code below: (1) event_hour_yf fallback now coalesced in _row_to_reporter + added to _SELECT_COLS; (2) reaction_date now treats bmo AND dmh as same-day (only amc/blank defer); (3) build_preview_blocks now returns (blocks, n_included) and stops at the block cap without truncating mid-row, and run_consensus_preview marks ONLY posted_rows=rows[:n_included] + warns on overflow. ALSO changed this pass: FMP revenue-beat rewired to the working /stable/earnings endpoint (v3 was dead/403); fetch_fmp_estimate_range short-circuits to n/a (Starter-gated 402); the FMP gate is now bool(FMP_API_KEY) only.

VERIFY the 3 fixes are correct and complete, and hunt for any NEW Critical/High correctness/safety bug introduced by these changes (esp. the tuple-return contract at all call sites, the rows[:n_included] marking, the /stable/earnings parsing/sort/cap, and any place still assuming the old single-return build_preview_blocks or the removed row.eps_range assignment). Concrete file:line + one-line repro. IGNORE style/whitespace/line-endings. If clean, say exactly: 'No Critical/High issues.'

## Diff: main.py + config.py
```diff
diff --git a/config.py b/config.py
index 66796a2..71d8cf6 100644
--- a/config.py
+++ b/config.py
@@ -55,6 +55,10 @@ SLACK_WEBHOOK_EARNINGS = os.getenv("SLACK_WEBHOOK_EARNINGS")
 # channel when unset.
 SLACK_WEBHOOK_STATUS = os.getenv("SLACK_WEBHOOK_STATUS")
 SLACK_STATUS_CHANNEL_ID = os.getenv("SLACK_STATUS_CHANNEL_ID")
+# StreetAccount channel: consensus-metrics pre-earnings previews (§9A).
+# Secret name matches sa-monitor's. Unset -> --consensus-preview posts raise
+# (fail-loud) rather than silently dropping the brief.
+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
 # Bot-token Slack client (powers per-question threads + reply polling).
 # Webhook remains the path for heartbeat / digest posts that don't need
 # replies. When SLACK_BOT_TOKEN + SLACK_CHANNEL_ID are unset, threaded
diff --git a/main.py b/main.py
index 923fa84..59f63f6 100644
--- a/main.py
+++ b/main.py
@@ -21,6 +21,7 @@ from config import (
     FINNHUB_API_KEY,
     SLACK_WEBHOOK_EARNINGS,
     SLACK_WEBHOOK_STATUS,
+    SLACK_WEBHOOK_STREET_ACCOUNT,
     SLACK_BOT_TOKEN,
     SLACK_CHANNEL_ID,
     SLACK_STATUS_CHANNEL_ID,
@@ -83,6 +84,13 @@ from ticktick import (
     TickTickTokenExpired,
 )
 from digest import build_weekly_digest
+from consensus_preview import (
+    select_upcoming_reporters,
+    assemble_preview_rows,
+    build_preview_blocks,
+    build_preview_fallback,
+    write_preview_export,
+)
 from notifications import (
     build_slack_blocks,
     build_slack_fallback_text,
@@ -1598,6 +1606,134 @@ def run_weekly_digest(dry_run: bool = False):
                 f"using content from {DIGEST_HTML_PATH}")
 
 
+# ---------------------------------------------------------------------------
+# Consensus Metrics Preview (StreetAccount §9A) — additive, run() untouched
+# ---------------------------------------------------------------------------
+
+
+def run_consensus_preview(
+    dry_run: bool = False,
+    days_ahead: int = 3,
+    ticker: str | None = None,
+):
+    """Build + post the pre-earnings consensus-metrics preview.
+
+    Core fields are free/cached (yfinance + SQLite + Coverage Manager). The
+    revenue beat rate additionally uses FMP's /stable/earnings when FMP_API_KEY
+    is set (verified Starter-available 2026-07-12); the estimate count/range
+    stays n/a (quarterly analyst-estimates is Starter-gated).
+
+    Dedup: kv_store `consensus_preview_posted:TICKER:EVENT_DATE`. Post-then-mark
+    (mark only after a successful Slack post) so a failed post retries; only
+    rows actually included in the posted payload are marked.
+    --dry-run writes the JSON export but posts nothing and marks nothing.
+    """
+    import json
+    from pathlib import Path
+
+    coverage = load_coverage()
+    if not coverage:
+        logger.error("No tickers loaded. Cannot build consensus preview.")
+        sys.exit(1)
+    coverage_map = {t.ticker: t for t in coverage}
+
+    conn = init_db()
+
+    reporters = select_upcoming_reporters(
+        conn, coverage, days_ahead=days_ahead, max_tier=1, ticker=ticker
+    )
+
+    # Dedup (live runs only — dry-run must never consult/mutate the ledger,
+    # so a preview is re-previewable on demand).
+    if not dry_run and reporters:
+        pending = []
+        for r in reporters:
+            key = f"consensus_preview_posted:{r.ticker}:{r.event_date}"
+            if kv_get(conn, key):
+                logger.info(
+                    f"Preview already posted for {r.ticker} {r.event_date}; skipping"
+                )
+                continue
+            pending.append(r)
+        reporters = pending
+
+    # Revenue beat rate comes from FMP /stable/earnings (verified available on
+    # the Starter plan 2026-07-12); estimate count/range stays n/a (Starter-
+    # gated). Gate purely on the key — the endpoint is confirmed, not probed.
+    fmp_ok = bool(FMP_API_KEY)
+    logger.info(
+        "FMP revenue-beat %s", "ON" if fmp_ok else "OFF (no FMP_API_KEY)"
+    )
+
+    as_of = date.today()
+    rows = assemble_preview_rows(
+        conn, reporters, coverage_map,
+        fmp_key=(FMP_API_KEY if fmp_ok else None),
+        fmp_ok=fmp_ok,
+    )
+
+    # Window for the export mirrors the selection window (or the single event).
+    if ticker and rows:
+        window = {"start": as_of.isoformat(), "end": rows[0].event_date}
+    else:
+        window = {
+            "start": as_of.isoformat(),
+            "end": (as_of + timedelta(days=days_ahead)).isoformat(),
+        }
+    out_path = Path(__file__).parent / "exports" / "consensus_preview.json"
+    n = write_preview_export(rows, out_path, window)
+    logger.info(f"Wrote {n} consensus preview(s) to {out_path}")
+
+    blocks, n_included = build_preview_blocks(rows, as_of)
+    fallback = build_preview_fallback(rows, as_of)
+
+    if dry_run:
+        logger.info("=" * 50)
+        logger.info("DRY RUN — consensus preview fallback text:")
+        logger.info("\n" + fallback)
+        logger.info("-" * 50)
+        logger.info("DRY RUN — Block Kit JSON:")
+        logger.info(json.dumps(blocks, indent=2))
+        logger.info("=" * 50)
+        logger.info("(Dry run — no Slack post, no kv_store marks)")
+        conn.close()
+        return
+
+    if not rows:
+        logger.info("No upcoming reporters to preview — nothing to post.")
+        conn.close()
+        return
+
+    if not SLACK_WEBHOOK_STREET_ACCOUNT:
+        conn.close()
+        raise NotificationError(
+            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
+        )
+
+    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
+    # Post-then-mark, and mark ONLY rows actually in the posted payload
+    # (build_preview_blocks stops at the Slack block cap and returns how many
+    # it included, in order). Overflow rows stay unmarked so the next run
+    # re-posts them — they are never silently dropped-yet-marked.
+    posted_rows = rows[:n_included]
+    if n_included < len(rows):
+        overflow = rows[n_included:]
+        logger.warning(
+            "Consensus preview overflow: posted %d of %d rows (Slack block cap); "
+            "%d left unmarked for the next run: %s",
+            n_included, len(rows), len(overflow),
+            ", ".join(r.ticker for r in overflow),
+        )
+    for r in posted_rows:
+        kv_set(
+            conn,
+            f"consensus_preview_posted:{r.ticker}:{r.event_date}",
+            f"posted:{as_of.isoformat()}",
+        )
+    logger.info(f"Posted consensus preview for {len(posted_rows)} name(s)")
+    conn.close()
+
+
 # ---------------------------------------------------------------------------
 # Shared results notification (Slack + TickTick)
 # ---------------------------------------------------------------------------
@@ -4113,6 +4249,25 @@ def main():
         action="store_true",
         help="Poll Slack threads for replies on open questions; apply commands (lock/wait/snooze/ignore/etc) to DB state",
     )
+    parser.add_argument(
+        "--consensus-preview",
+        action="store_true",
+        help="Build the pre-earnings consensus-metrics preview (StreetAccount §9A) "
+             "for upcoming Tier-1 reporters and post to #street-account",
+    )
+    parser.add_argument(
+        "--preview-ticker",
+        type=str,
+        default=None,
+        metavar="TICKER",
+        help="Restrict --consensus-preview to a single ticker's next upcoming event (any tier)",
+    )
+    parser.add_argument(
+        "--preview-days",
+        type=int,
+        default=3,
+        help="Look-ahead window (days) for --consensus-preview reporter selection (default: 3)",
+    )
     args = parser.parse_args()
 
     # --populate-db-only is a self-documenting alias for --dry-run (DB writes
@@ -4167,6 +4322,12 @@ def main():
             "Slack reply processing",
             lambda: run_check_replies(dry_run=args.dry_run),
         )
+    elif args.consensus_preview:
+        run_consensus_preview(
+            dry_run=args.dry_run,
+            days_ahead=args.preview_days,
+            ticker=args.preview_ticker,
+        )
     elif args.cleanup:
         conn = init_db()
         cleanup_duplicates(conn, dry_run=args.dry_run)
```

## Full current consensus_preview.py
```python
"""
Consensus Metrics Preview (StreetAccount Wave 1 §9A).

Builds a pre-earnings brief for each upcoming covered reporter, composed
entirely from FREE / cached data (yfinance + the SQLite events table +
Coverage Manager exports). One brief per upcoming Tier-1 name carries:

  - header: company, event date, BMO/AMC badge, last price ± day change
  - mean consensus Rev + EPS (from the DB events table — zero API calls)
  - QTD price move since the prior quarter-end for TICKER / SPY / sector-ETF
  - options-implied move (ATM straddle mid ÷ spot on the first expiry that
    contains the post-earnings reaction — see compute_implied_move)
  - last-4-quarter post-earnings stock reactions
  - EPS beat rate (up to 20 quarters, yfinance Surprise%)
  - conference-call time (from events.call_datetime_utc)

Two further fields are FMP-Starter-gated (estimate count/low-high range and
the revenue beat rate). They are only fetched when the caller passes an FMP
key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
then they render as an explicit "n/a (FMP endpoint unavailable)" — never a
silent blank and never a fabricated number.

Design invariant: NO field is ever silently dropped. Every degraded field
appends a human note to PreviewRow.na_notes and a machine-readable
{field, reason} entry to PreviewRow.na_fields, and renders as an explicit
"n/a (reason)" string. A per-ticker failure never drops the row.

This module is additive — it does not touch the daily run() flow.
"""

from __future__ import annotations

import io
import logging
import sqlite3
from contextlib import contextmanager, redirect_stderr, redirect_stdout
from dataclasses import dataclass, field
from datetime import date, datetime, timedelta, timezone
from pathlib import Path
from typing import Optional

try:
    from zoneinfo import ZoneInfo
except ImportError:  # pragma: no cover - py<3.9 fallback
    ZoneInfo = None

import yfinance as yf

from coverage import TickerInfo
from market_data import (
    PostEarningsMove,
    fetch_post_earnings_move,
    infer_hour_from_datetime,
)

logger = logging.getLogger("earnings_agent")

SCHEMA_VERSION = 1

# Coverage-Manager sector (or subsector) -> sector-proxy ETF for the QTD row.
# Keys are the canonical CM sector labels. Unknown sector -> no ETF row + note.
SECTOR_ETF: dict[str, str] = {
    "Healthcare Services": "XLV",
    "MedTech": "IHI",
    "Large Pharma": "XLV",
    "Biotech": "XBI",
    "Fintech": "XLF",
}

# Implied-move sanity gates (percent of spot). A straddle mid outside this
# band is almost always a data artifact (stale/one-sided quotes, wrong
# strike), not a real expectation — surface it as n/a rather than a bad number.
_IMPLIED_MOVE_MIN_PCT = 0.5
_IMPLIED_MOVE_MAX_PCT = 40.0
# Nearest common strike must be within this fraction of spot to be "ATM".
_ATM_MAX_STRIKE_DISTANCE = 0.05
# A leg with (ask - bid)/mid above this is flagged "wide market".
_WIDE_MARKET_RATIO = 0.6
# Expiry more than this many days past the reaction date carries extra
# time value; we annotate so the reader knows the move is inflated.
_EXTRA_TIME_VALUE_DAYS = 7


# ---------------------------------------------------------------------------
# Dataclasses
# ---------------------------------------------------------------------------


@dataclass
class ImpliedMove:
    """Options-implied one-day move from an ATM straddle.

    pct is None when the move could not be computed for any reason; in that
    case notes[0] carries the "n/a (reason)" string. When pct is present,
    notes holds any annotations ("stale quote", "wide market",
    "(includes N extra days of time value)").
    """
    pct: Optional[float]
    expiry: Optional[str]
    method: str
    notes: list[str] = field(default_factory=list)

    @property
    def ok(self) -> bool:
        return self.pct is not None


@dataclass
class EstimateRange:
    """FMP analyst-estimate spread (count + low/high). All None => n/a."""
    count: Optional[int]
    low: Optional[float]
    high: Optional[float]

    @property
    def ok(self) -> bool:
        return self.count is not None


@dataclass
class BeatRate:
    """Historical beat rate (beats out of total). beats/total None => n/a."""
    beats: Optional[int]
    total: Optional[int]
    source: str

    @property
    def ok(self) -> bool:
        return self.total is not None and self.total > 0


@dataclass
class QuarterMove:
    """One historical post-earnings stock reaction."""
    event_date: str
    move_pct: float
    window_label: str


@dataclass
class Reporter:
    """A single upcoming reporter selected for a preview (pre-assembly)."""
    ticker: str
    company_name: str
    tier: int
    event_date: str
    event_hour: Optional[str]
    call_datetime_utc: Optional[str]
    eps_estimate: Optional[float]
    rev_estimate: Optional[float]
    sector: str
    subsector: str


@dataclass
class PreviewRow:
    """The fully-assembled preview for one reporter."""
    ticker: str
    company_name: str
    tier: int
    event_date: str
    event_hour: Optional[str]
    call_datetime_utc: Optional[str]

    last_price: Optional[float] = None
    day_change_pct: Optional[float] = None

    eps_mean: Optional[float] = None
    rev_mean: Optional[float] = None
    eps_range: EstimateRange = field(
        default_factory=lambda: EstimateRange(None, None, None)
    )

    qtd_since: Optional[str] = None
    qtd_ticker_pct: Optional[float] = None
    qtd_spx_pct: Optional[float] = None
    sector_etf: Optional[str] = None          # ETF ticker, e.g. "XLV"
    sector_etf_label: Optional[str] = None    # human label, e.g. "Healthcare Services"
    sector_etf_pct: Optional[float] = None

    implied_move: ImpliedMove = field(
        default_factory=lambda: ImpliedMove(None, None, "ATM straddle", ["n/a (not computed)"])
    )

    post_moves: list[QuarterMove] = field(default_factory=list)

    eps_beat: BeatRate = field(default_factory=lambda: BeatRate(None, None, "yfinance"))
    rev_beat: BeatRate = field(default_factory=lambda: BeatRate(None, None, "fmp"))

    na_notes: list[str] = field(default_factory=list)
    na_fields: list[dict] = field(default_factory=list)

    def mark_na(self, field_name: str, reason: str) -> None:
        """Record a degraded/unavailable field once, both human + machine."""
        self.na_notes.append(f"{field_name}: {reason}")
        self.na_fields.append({"field": field_name, "reason": reason})


# ---------------------------------------------------------------------------
# yfinance noise suppression
# ---------------------------------------------------------------------------


@contextmanager
def _silence():
    """Silence yfinance's chatty stdout/stderr (matches market_data idiom)."""
    buf_out, buf_err = io.StringIO(), io.StringIO()
    with redirect_stdout(buf_out), redirect_stderr(buf_err):
        yield


# ---------------------------------------------------------------------------
# Small helpers
# ---------------------------------------------------------------------------


def _parse_iso_date(s: str) -> Optional[date]:
    try:
        return date.fromisoformat(s[:10])
    except (ValueError, TypeError):
        return None


def _prior_quarter_end(today: date) -> date:
    """Return the calendar-quarter-end immediately before `today`'s quarter.

    For any day in Q3 (Jul-Sep) this is Jun 30; the base for the QTD move.
    """
    q_start_month = ((today.month - 1) // 3) * 3 + 1
    q_start = date(today.year, q_start_month, 1)
    return q_start - timedelta(days=1)


def sector_etf_for(sector: str, subsector: str) -> Optional[tuple[str, str]]:
    """Map a CM sector (then subsector) to (label, etf_ticker) or None."""
    for key in (sector, subsector):
        if key and key in SECTOR_ETF:
            return key, SECTOR_ETF[key]
    return None


def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
    """The trading day whose close reflects the market's reaction.

    bmo / dmh           -> the event date itself. A pre-open (bmo) OR during-
                           market-hours (dmh) release reacts the SAME session.
    amc / blank         -> the next weekday after the event date (post-close or
                           unknown-timing release reacts the following session;
                           unknown is treated as amc so the chosen expiry can
                           never fall BEFORE an actual post-close reaction).
                           Weekend-skip only (no holiday calendar).
    """
    ed = _parse_iso_date(event_date)
    if ed is None:
        return None
    # Mirror market_data.fetch_post_earnings_move: only amc defers to the next
    # session; bmo and dmh both react same-day.
    if (event_hour or "").lower() in ("bmo", "dmh"):
        return ed
    d = ed + timedelta(days=1)
    while d.weekday() >= 5:  # Sat=5, Sun=6
        d += timedelta(days=1)
    return d


def _last_close(ticker: str) -> Optional[float]:
    """Most recent daily close from a 5d history (NOT intraday fast_info)."""
    try:
        with _silence():
            hist = yf.Ticker(ticker).history(period="5d", auto_adjust=True)
    except Exception as exc:  # noqa: BLE001
        logger.debug(f"5d history failed for {ticker}: {exc}")
        return None
    if hist is None or hist.empty:
        return None
    closes = hist["Close"].dropna()
    if closes.empty:
        return None
    return float(closes.iloc[-1])


def _leg_mid(row) -> tuple[Optional[float], bool, Optional[float]]:
    """Return (mid, is_stale, spread) for one option leg.

    Rule (spec §2):
      bid > 0 and ask >= bid  -> (bid + ask) / 2, not stale, spread = ask - bid
      elif lastPrice > 0      -> lastPrice, STALE (no live two-sided quote),
                                 spread unknown (None)
      else                    -> (None, False, None)  -> leg unusable
    """
    def _f(key: str) -> float:
        try:
            v = row.get(key)
        except AttributeError:  # pandas Series
            v = row[key] if key in row else None
        try:
            return float(v) if v is not None else 0.0
        except (TypeError, ValueError):
            return 0.0

    bid, ask, last = _f("bid"), _f("ask"), _f("lastPrice")
    if bid > 0 and ask >= bid:
        return (bid + ask) / 2.0, False, (ask - bid)
    if last > 0:
        return last, True, None
    return None, False, None


# ---------------------------------------------------------------------------
# Reporter selection
# ---------------------------------------------------------------------------


def _row_to_reporter(raw: tuple, coverage_map: dict[str, TickerInfo]) -> Reporter:
    (ticker, event_date, event_hour, event_hour_yf, db_tier, company_name,
     eps_est, rev_est, call_dt) = raw
    info = coverage_map.get(ticker)
    # Finnhub-canonical hour first; fall back to the yfinance-inferred hour
    # (v11 event_hour_yf column) when Finnhub left timing blank — the same
    # fallback the rest of the pipeline uses. Without it a bmo/amc name whose
    # timing only lives in event_hour_yf renders TBD and gets a wrong (next-
    # session) reaction date for the implied-move expiry.
    effective_hour = event_hour or event_hour_yf
    return Reporter(
        ticker=ticker,
        company_name=company_name or (info.company_name if info else ""),
        tier=(info.tier if info else (int(db_tier) if db_tier is not None else 99)),
        event_date=event_date,
        event_hour=effective_hour,
        call_datetime_utc=call_dt,
        eps_estimate=eps_est,
        rev_estimate=rev_est,
        sector=(info.sector if info else ""),
        subsector=(info.subsector if info else ""),
    )


_SELECT_COLS = (
    "ticker, event_date, event_hour, event_hour_yf, tier, company_name, "
    "eps_estimate, rev_estimate, call_datetime_utc"
)


def select_upcoming_reporters(
    conn: sqlite3.Connection,
    coverage: list[TickerInfo],
    *,
    days_ahead: int = 3,
    max_tier: int = 1,
    ticker: Optional[str] = None,
) -> list[Reporter]:
    """Select upcoming, not-yet-reported reporters for previewing.

    When `ticker` is given, returns that ticker's single nearest upcoming
    unreported event regardless of tier or window (on-demand preview).
    Otherwise returns every unreported event in [today, today+days_ahead]
    whose Coverage-Manager tier is <= max_tier (Tier-1 only by default).
    """
    coverage_map = {t.ticker: t for t in coverage}
    today = date.today()

    if ticker:
        tk = ticker.strip().upper()
        row = conn.execute(
            f"SELECT {_SELECT_COLS} FROM events "
            "WHERE ticker = ? AND COALESCE(reported, 0) = 0 AND event_date >= ? "
            "ORDER BY event_date LIMIT 1",
            (tk, today.isoformat()),
        ).fetchone()
        if not row:
            logger.warning(f"No upcoming unreported event found for {tk}")
            return []
        return [_row_to_reporter(row, coverage_map)]

    end = (today + timedelta(days=days_ahead)).isoformat()
    rows = conn.execute(
        f"SELECT {_SELECT_COLS} FROM events "
        "WHERE event_date BETWEEN ? AND ? AND COALESCE(reported, 0) = 0 "
        "ORDER BY event_date, ticker",
        (today.isoformat(), end),
    ).fetchall()

    reporters: list[Reporter] = []
    for raw in rows:
        rep = _row_to_reporter(raw, coverage_map)
        if rep.tier <= max_tier:
            reporters.append(rep)
    logger.info(
        f"Selected {len(reporters)} upcoming Tier<= {max_tier} reporter(s) "
        f"in next {days_ahead}d ({len(rows)} events scanned)"
    )
    return reporters


# ---------------------------------------------------------------------------
# Free-data field fetchers
# ---------------------------------------------------------------------------


def fetch_price_snapshot(ticker: str) -> tuple[Optional[float], Optional[float]]:
    """Return (last_close, 1-day_pct_change) from a single 5d history call."""
    try:
        with _silence():
            hist = yf.Ticker(ticker).history(period="5d", auto_adjust=True)
    except Exception as exc:  # noqa: BLE001
        logger.debug(f"price snapshot failed for {ticker}: {exc}")
        return None, None
    if hist is None or hist.empty:
        return None, None
    closes = hist["Close"].dropna()
    if closes.empty:
        return None, None
    last = float(closes.iloc[-1])
    if len(closes) >= 2:
        prev = float(closes.iloc[-2])
        day_change = (last - prev) / prev * 100 if prev else None
    else:
        day_change = None
    return last, day_change


def fetch_quarter_performance(
    tickers: list[str], since: date
) -> dict[str, Optional[float]]:
    """QTD % change (close on/after `since` -> latest close) for each ticker.

    One batched yfinance download (the caller pre-adds SPY + sector ETFs so
    the whole panel comes over the wire once). A ticker yfinance can't
    resolve maps to None (caller renders n/a), never raising.
    """
    unique = sorted({t.upper() for t in tickers})
    result: dict[str, Optional[float]] = {t: None for t in unique}
    if not unique:
        return result

    try:
        with _silence():
            data = yf.download(
                tickers=unique,
                start=since.isoformat(),
                progress=False,
                auto_adjust=True,
                threads=True,
                group_by="ticker",
            )
    except Exception as exc:  # noqa: BLE001
        logger.warning(f"QTD batch download failed: {exc}")
        return result

    if data is None or data.empty:
        logger.warning("QTD download returned empty dataset")
        return result

    for t in unique:
        try:
            if len(unique) == 1:
                closes = data["Close"].dropna()
            else:
                closes = data[t]["Close"].dropna()
            if closes.empty:
                continue
            first = float(closes.iloc[0])
            last = float(closes.iloc[-1])
            if first == 0:
                continue
            result[t] = (last - first) / first * 100
        except (KeyError, IndexError, ValueError) as exc:
            logger.debug(f"QTD unavailable for {t}: {exc}")
    return result


def compute_implied_move(
    ticker: str, event_date: str, event_hour: Optional[str]
) -> ImpliedMove:
    """Options-implied move = ATM straddle mid ÷ spot on the first expiry that
    contains the post-earnings reaction date.

    Never raises and never returns a bad number: every failure mode resolves
    to ImpliedMove(pct=None, ...) whose notes[0] is an explicit "n/a (reason)".
    """
    method = "ATM straddle"

    rxn = reaction_date(event_date, event_hour)
    if rxn is None:
        return ImpliedMove(None, None, method, ["n/a (unparseable event date)"])

    try:
        tkr = yf.Ticker(ticker)
        with _silence():
            expiries = list(tkr.options or [])
    except Exception as exc:  # noqa: BLE001
        return ImpliedMove(
            None, None, method,
            [f"n/a (options unavailable: {exc.__class__.__name__})"],
        )
    if not expiries:
        return ImpliedMove(None, None, method, ["n/a (no listed options)"])

    future = sorted(
        e for e in expiries
        if (_parse_iso_date(e) is not None and _parse_iso_date(e) >= rxn)
    )
    if not future:
        return ImpliedMove(
            None, None, method,
            ["n/a (no expiry on or after the reaction date)"],
        )
    expiry = future[0]
    exp_date = _parse_iso_date(expiry)

    spot = _last_close(ticker)
    if spot is None or spot <= 0:
        return ImpliedMove(None, expiry, method, ["n/a (spot price unavailable)"])

    try:
        with _silence():
            chain = tkr.option_chain(expiry)
        calls, puts = chain.calls, chain.puts
    except Exception as exc:  # noqa: BLE001
        return ImpliedMove(
            None, expiry, method,
            [f"n/a (option chain fetch failed: {exc.__class__.__name__})"],
        )

    if calls is None or puts is None or calls.empty or puts.empty:
        return ImpliedMove(None, expiry, method, ["n/a (empty option chain)"])

    call_strikes = set(calls["strike"].tolist())
    put_strikes = set(puts["strike"].tolist())
    common = call_strikes & put_strikes
    if not common:
        return ImpliedMove(
            None, expiry, method,
            ["n/a (no strike present in both calls and puts)"],
        )

    K = min(common, key=lambda k: abs(k - spot))
    if abs(K - spot) / spot > _ATM_MAX_STRIKE_DISTANCE:
        return ImpliedMove(
            None, expiry, method,
            [f"n/a (nearest common strike {K:g} is >5% from spot {spot:.2f})"],
        )

    call_row = calls.loc[calls["strike"] == K].iloc[0]
    put_row = puts.loc[puts["strike"] == K].iloc[0]
    call_mid, call_stale, call_spread = _leg_mid(call_row)
    put_mid, put_stale, put_spread = _leg_mid(put_row)
    if call_mid is None or put_mid is None:
        return ImpliedMove(
            None, expiry, method,
            ["n/a (no usable bid/ask/last on a straddle leg)"],
        )

    pct = (call_mid + put_mid) / spot * 100
    if not (_IMPLIED_MOVE_MIN_PCT <= pct <= _IMPLIED_MOVE_MAX_PCT):
        return ImpliedMove(
            None, expiry, method,
            [f"n/a (implausible straddle: {pct:.1f}%)"],
        )

    notes: list[str] = []
    if call_stale or put_stale:
        notes.append("stale quote")
    for spread, mid in ((call_spread, call_mid), (put_spread, put_mid)):
        if spread is not None and mid > 0 and spread / mid > _WIDE_MARKET_RATIO:
            notes.append("wide market")
            break
    if exp_date is not None:
        extra_days = (exp_date - rxn).days
        if extra_days > _EXTRA_TIME_VALUE_DAYS:
            notes.append(f"(includes {extra_days} extra days of time value)")

    return ImpliedMove(round(pct, 1), expiry, method, notes)


def fetch_recent_post_moves(
    conn: sqlite3.Connection, ticker: str, n: int = 4
) -> list[QuarterMove]:
    """Last `n` post-earnings stock reactions, newest first.

    Prefers the DB's reported events (already timed BMO/AMC), then backfills
    from yfinance's historical earnings dates for names with a thin DB
    history, inferring timing from the timestamp's time-of-day.
    """
    moves: list[QuarterMove] = []
    seen: set[str] = set()

    rows = conn.execute(
        "SELECT event_date, event_hour FROM events "
        "WHERE ticker = ? AND COALESCE(reported, 0) = 1 AND event_date <= date('now') "
        "ORDER BY event_date DESC LIMIT ?",
        (ticker, n * 3),
    ).fetchall()
    for ev_date, ev_hour in rows:
        if len(moves) >= n:
            break
        if ev_date in seen:
            continue
        mv = fetch_post_earnings_move(ticker, ev_date, ev_hour)
        if mv is not None:
            moves.append(QuarterMove(ev_date, mv.move_pct, mv.window_label))
            seen.add(ev_date)

    if len(moves) < n:
        try:
            with _silence():
                ed = yf.Ticker(ticker).get_earnings_dates(limit=max(n * 3, 12))
        except Exception as exc:  # noqa: BLE001
            logger.debug(f"earnings-date backfill failed for {ticker}: {exc}")
            ed = None
        if ed is not None and not ed.empty:
            today = date.today()
            for idx in ed.index:
                if len(moves) >= n:
                    break
                try:
                    d = idx.date()
                except AttributeError:
                    continue
                if d >= today:  # only settled, past reactions
                    continue
                iso = d.isoformat()
                if iso in seen:
                    continue
                try:
                    hour = infer_hour_from_datetime(idx.to_pydatetime())
                except Exception:  # noqa: BLE001
                    hour = None
                mv = fetch_post_earnings_move(ticker, iso, hour)
                if mv is not None:
                    moves.append(QuarterMove(iso, mv.move_pct, mv.window_label))
                    seen.add(iso)

    moves.sort(key=lambda m: m.event_date, reverse=True)
    return moves[:n]


def fetch_eps_beat_history(ticker: str, max_q: int = 20) -> BeatRate:
    """EPS beat rate over up to `max_q` reported quarters (yfinance Surprise%).

    A positive Surprise(%) is a beat. Rows with NaN surprise (the upcoming,
    not-yet-reported quarter) are excluded. Returns BeatRate with beats/total
    None when yfinance has no usable surprise history (caller renders n/a).
    """
    try:
        with _silence():
            ed = yf.Ticker(ticker).get_earnings_dates(limit=max_q)
    except Exception as exc:  # noqa: BLE001
        logger.debug(f"EPS beat history failed for {ticker}: {exc}")
        return BeatRate(None, None, "yfinance")
    if ed is None or ed.empty or "Surprise(%)" not in ed.columns:
        return BeatRate(None, None, "yfinance")
    # yfinance does not strictly honor `limit` (it pads future/extra rows), so
    # cap to the most recent max_q reported quarters after dropping the
    # not-yet-reported (NaN-surprise) rows. Index is newest-first.
    surprises = ed["Surprise(%)"].dropna().head(max_q)
    total = int(len(surprises))
    if total == 0:
        return BeatRate(None, None, "yfinance")
    beats = int((surprises > 0).sum())
    return BeatRate(beats, total, "yfinance")


# ---------------------------------------------------------------------------
# FMP-gated fetchers (only called when the probe flag is set — see run_*).
# These deliberately never raise; any failure yields an n/a result.
# ---------------------------------------------------------------------------


def _fmp_get_json(url: str, timeout: int = 20):
    import json
    import urllib.request

    with urllib.request.urlopen(url, timeout=timeout) as resp:  # noqa: S310
        return json.load(resp)


def fetch_fmp_estimate_range(
    ticker: str, event_date: str, api_key: str
) -> EstimateRange:
    """Analyst estimate count + low/high EPS range for the reporting quarter.

    UNAVAILABLE on the current FMP Starter plan and therefore always n/a:
    - /stable/analyst-estimates with period=quarter -> HTTP 402 ("this value
      set for 'period' is not available under your current subscription")
    - the v3 /api/v3/analyst-estimates endpoint is retired -> HTTP 403
      ("Legacy Endpoint")
    (Both verified 2026-07-12.) We short-circuit rather than fire a request
    that can only burn a gated 402/403; the caller renders the honest
    "not available on FMP Starter plan" reason. Kept as a documented seam so a
    future plan upgrade can restore quarterly estimate ranges here.
    """
    return EstimateRange(None, None, None)


def fetch_fmp_rev_beat_history(
    ticker: str, api_key: str, max_q: int = 20
) -> BeatRate:
    """Revenue beat rate over up to `max_q` most-recent COMPLETED quarters.

    Endpoint: FMP /stable/earnings (fields revenueActual / revenueEstimated).
    This is the Starter-plan-available successor to the retired v3
    /historical/earning_calendar endpoint (v3 -> HTTP 403 "Legacy Endpoint";
    /stable/earnings -> HTTP 200, verified 2026-07-12). Returns n/a on any
    error or unexpected shape — never a partial/fabricated number.
    """
    if not api_key:
        return BeatRate(None, None, "fmp")
    # /stable/earnings returns the upcoming (actual=None) quarter plus history;
    # over-request so `max_q` COMPLETED quarters survive the null-actual skip.
    limit = max(max_q * 2, 40)
    url = (
        f"https://financialmodelingprep.com/stable/earnings"
        f"?symbol={ticker}&limit={limit}&apikey={api_key}"
    )
    try:
        data = _fmp_get_json(url)
    except Exception as exc:  # noqa: BLE001
        logger.debug(f"FMP revenue-beat fetch failed for {ticker}: {exc}")
        return BeatRate(None, None, "fmp")
    if not isinstance(data, list) or not data:
        return BeatRate(None, None, "fmp")

    # Newest-first so the `max_q` cap keeps the MOST RECENT completed quarters,
    # regardless of the API's return order.
    data = sorted(data, key=lambda r: str(r.get("date", "")), reverse=True)

    beats = 0
    total = 0
    for row in data:
        actual = row.get("revenueActual")
        est = row.get("revenueEstimated")
        if actual is None or est is None:
            continue
        try:
            actual_f, est_f = float(actual), float(est)
        except (TypeError, ValueError):
            continue
        if est_f == 0:
            continue
        total += 1
        if actual_f > est_f:
            beats += 1
        if total >= max_q:
            break
    if total == 0:
        return BeatRate(None, None, "fmp")
    return BeatRate(beats, total, "fmp")


# ---------------------------------------------------------------------------
# Row assembly
# ---------------------------------------------------------------------------


def assemble_preview_rows(
    conn: sqlite3.Connection,
    reporters: list[Reporter],
    coverage_map: dict[str, TickerInfo],
    *,
    fmp_key: Optional[str] = None,
    fmp_ok: bool = False,
) -> list[PreviewRow]:
    """Assemble one PreviewRow per reporter.

    QTD is batched across every reporter + SPY + sector ETFs in a single
    yfinance download. Each per-field fetch is wrapped so a single failure
    degrades that field to n/a rather than losing the row; a catastrophic
    per-ticker error still yields a skeleton row flagged in na_fields.
    """
    today = date.today()
    since = _prior_quarter_end(today)

    # Batch the QTD panel: reporters + SPY + every mapped sector ETF.
    qtd_universe: set[str] = {"SPY"}
    for rep in reporters:
        qtd_universe.add(rep.ticker.upper())
        etf = sector_etf_for(rep.sector, rep.subsector)
        if etf:
            qtd_universe.add(etf[1].upper())
    qtd_map = fetch_quarter_performance(sorted(qtd_universe), since)

    rows: list[PreviewRow] = []
    for rep in reporters:
        try:
            rows.append(
                _build_row(conn, rep, since, qtd_map, fmp_key=fmp_key, fmp_ok=fmp_ok)
            )
        except Exception as exc:  # noqa: BLE001 - never drop a row
            logger.warning(f"Preview assembly failed for {rep.ticker}: {exc}")
            row = PreviewRow(
                ticker=rep.ticker,
                company_name=rep.company_name,
                tier=rep.tier,
                event_date=rep.event_date,
                event_hour=rep.event_hour,
                call_datetime_utc=rep.call_datetime_utc,
                eps_mean=rep.eps_estimate,
                rev_mean=rep.rev_estimate,
            )
            row.mark_na("row", f"assembly failed ({exc.__class__.__name__})")
            rows.append(row)
    return rows


def _build_row(
    conn: sqlite3.Connection,
    rep: Reporter,
    since: date,
    qtd_map: dict[str, Optional[float]],
    *,
    fmp_key: Optional[str],
    fmp_ok: bool,
) -> PreviewRow:
    row = PreviewRow(
        ticker=rep.ticker,
        company_name=rep.company_name,
        tier=rep.tier,
        event_date=rep.event_date,
        event_hour=rep.event_hour,
        call_datetime_utc=rep.call_datetime_utc,
        eps_mean=rep.eps_estimate,
        rev_mean=rep.rev_estimate,
        qtd_since=since.isoformat(),
    )

    if rep.eps_estimate is None:
        row.mark_na("consensus.eps.mean", "no EPS estimate in DB")
    if rep.rev_estimate is None:
        row.mark_na("consensus.revenue.mean", "no revenue estimate in DB")

    # --- price snapshot ---
    try:
        last, day_change = fetch_price_snapshot(rep.ticker)
    except Exception as exc:  # noqa: BLE001
        last, day_change = None, None
        logger.debug(f"price snapshot error {rep.ticker}: {exc}")
    row.last_price = last
    row.day_change_pct = day_change
    if last is None:
        row.mark_na("last_price", "yfinance price unavailable")

    # --- QTD ---
    row.qtd_ticker_pct = qtd_map.get(rep.ticker.upper())
    row.qtd_spx_pct = qtd_map.get("SPY")
    if row.qtd_ticker_pct is None:
        row.mark_na("qtd.ticker_pct", "yfinance QTD unavailable")
    if row.qtd_spx_pct is None:
        row.mark_na("qtd.spx_pct", "yfinance QTD unavailable for SPY")
    etf = sector_etf_for(rep.sector, rep.subsector)
    if etf is None:
        row.mark_na(
            "qtd.sector_etf",
            f"no mapped ETF for sector '{rep.sector or 'unknown'}'",
        )
    else:
        label, etf_ticker = etf
        row.sector_etf = etf_ticker
        row.sector_etf_label = label
        row.sector_etf_pct = qtd_map.get(etf_ticker.upper())
        if row.sector_etf_pct is None:
            row.mark_na("qtd.sector_etf_pct", f"yfinance QTD unavailable for {etf_ticker}")

    # --- implied move ---
    try:
        row.implied_move = compute_implied_move(
            rep.ticker, rep.event_date, rep.event_hour
        )
    except Exception as exc:  # noqa: BLE001 - defensive; compute_* shouldn't raise
        row.implied_move = ImpliedMove(
            None, None, "ATM straddle",
            [f"n/a (unexpected error: {exc.__class__.__name__})"],
        )
    if not row.implied_move.ok:
        reason = row.implied_move.notes[0] if row.implied_move.notes else "n/a"
        row.mark_na("implied_move", reason)

    # --- post-earnings moves ---
    try:
        row.post_moves = fetch_recent_post_moves(conn, rep.ticker, n=4)
    except Exception as exc:  # noqa: BLE001
        row.post_moves = []
        logger.debug(f"post-move error {rep.ticker}: {exc}")
    if not row.post_moves:
        row.mark_na("post_earnings_moves", "no computable prior reactions")

    # --- EPS beat rate ---
    try:
        row.eps_beat = fetch_eps_beat_history(rep.ticker, max_q=20)
    except Exception as exc:  # noqa: BLE001
        row.eps_beat = BeatRate(None, None, "yfinance")
        logger.debug(f"eps beat error {rep.ticker}: {exc}")
    if not row.eps_beat.ok:
        row.mark_na("beat_rates.eps", "no yfinance surprise history")

    # --- FMP B-fields (independently gated) ---
    # Estimate count/range: permanently unavailable on the FMP Starter plan
    # (quarterly analyst-estimates -> 402; verified 2026-07-12) -> always n/a.
    row.mark_na(
        "consensus.eps.range",
        "estimate range not available on FMP Starter plan",
    )
    # Revenue beat rate: reachable via /stable/earnings whenever a key is set.
    if fmp_ok and fmp_key:
        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
        if not row.rev_beat.ok:
            row.mark_na("beat_rates.revenue", "FMP revenue-beat history unavailable")
    else:
        row.mark_na("beat_rates.revenue", "no FMP_API_KEY — revenue beat n/a")

    return row


# ---------------------------------------------------------------------------
# Rendering helpers
# ---------------------------------------------------------------------------


def _fmt_short_date(iso: Optional[str]) -> str:
    d = _parse_iso_date(iso or "")
    if d is None:
        return "n/a"
    return f"{d.strftime('%a %b')} {d.day}"


def _fmt_expiry(iso: Optional[str]) -> str:
    d = _parse_iso_date(iso or "")
    if d is None:
        return "n/a"
    return f"{d.strftime('%b')} {d.day}"


def _timing_badge(hour: Optional[str]) -> str:
    if not hour:
        return "TBD"
    h = hour.lower()
    return {"bmo": "BMO", "amc": "AMC", "dmh": "DMH"}.get(h, hour.upper())


def _fmt_pct(v: Optional[float], *, signed: bool = True, na: str = "n/a") -> str:
    if v is None:
        return na
    if signed:
        return f"{v:+.1f}%"
    return f"{v:.1f}%"


def _fmt_price(v: Optional[float]) -> str:
    if v is None:
        return "n/a"
    return f"${v:,.2f}"


def _fmt_eps(v: Optional[float]) -> str:
    if v is None:
        return "n/a"
    return f"${v:.2f}"


def _fmt_rev(v: Optional[float]) -> str:
    if v is None:
        return "n/a"
    if v >= 1e9:
        return f"${v / 1e9:.2f}B"
    if v >= 1e6:
        return f"${v / 1e6:.1f}M"
    return f"${v:,.0f}"


def _fmt_call_et(call_iso: Optional[str], event_date: Optional[str]) -> str:
    """Conference-call time in ET, noting same-day vs next-day."""
    if not call_iso:
        return "n/a"
    try:
        raw = call_iso.replace("Z", "+00:00")
        dt = datetime.fromisoformat(raw)
        if dt.tzinfo is None:
            dt = dt.replace(tzinfo=timezone.utc)
    except (ValueError, TypeError):
        return "n/a"
    if ZoneInfo is None:
        return dt.strftime("%b %d %H:%M UTC")
    try:
        et = dt.astimezone(ZoneInfo("America/New_York"))
    except Exception:  # noqa: BLE001
        return dt.strftime("%b %d %H:%M UTC")
    hour12 = et.strftime("%I:%M %p").lstrip("0")
    ev = _parse_iso_date(event_date or "")
    if ev is not None and et.date() == ev:
        return f"{hour12} ET (same day)"
    return f"{et.strftime('%a %b')} {et.day} {hour12} ET"


def _render_implied_move(im: ImpliedMove) -> str:
    if not im.ok:
        reason = im.notes[0] if im.notes else "n/a"
        return f"Options-implied move: {reason}"
    line = f"Options imply ~{im.pct:.1f}% move ({_fmt_expiry(im.expiry)} exp straddle)"
    annotations = [n for n in im.notes if n]
    if annotations:
        line += " — " + ", ".join(annotations)
    return line


def _render_post_moves(moves: list[QuarterMove]) -> str:
    if not moves:
        return "Last post-earnings moves: n/a (no computable prior reactions)"
    parts = [f"{_fmt_expiry(m.event_date)} {m.move_pct:+.1f}%" for m in moves]
    return f"Last {len(moves)} post-earnings moves: " + ", ".join(parts)


def _render_beat_rate(br: BeatRate, label: str) -> str:
    if not br.ok:
        return f"{label}: n/a (FMP endpoint unavailable)" if br.source == "fmp" \
            else f"{label}: n/a"
    return f"{label}: {br.beats}/{br.total} ({br.source})"


def _render_estimate_range(er: EstimateRange) -> str:
    if not er.ok:
        return "est. range n/a (FMP endpoint unavailable)"
    lo = _fmt_eps(er.low)
    hi = _fmt_eps(er.high)
    return f"est. from {er.count} analysts ({lo}-{hi})"


def _render_row_lines(row: PreviewRow) -> list[str]:
    """The full set of spec §-scope lines for one reporter (fallback text)."""
    badge = _timing_badge(row.event_hour)
    name = row.company_name or row.ticker
    day = f" ({_fmt_pct(row.day_change_pct)} day)" if row.day_change_pct is not None else ""

    header = (
        f"`{row.ticker}` {name} — {_fmt_short_date(row.event_date)} · {badge} · "
        f"{_fmt_price(row.last_price)}{day}"
    )

    consensus = (
        f"Consensus: EPS {_fmt_eps(row.eps_mean)} · Rev {_fmt_rev(row.rev_mean)} · "
        f"{_render_estimate_range(row.eps_range)}"
    )

    qtd_bits = [f"{row.ticker} {_fmt_pct(row.qtd_ticker_pct)}",
                f"S&P 500 (SPY) {_fmt_pct(row.qtd_spx_pct)}"]
    if row.sector_etf:
        qtd_bits.append(
            f"{row.sector_etf_label} ({row.sector_etf}) {_fmt_pct(row.sector_etf_pct)}"
        )
    else:
        qtd_bits.append("sector ETF n/a (no mapped ETF)")
    qtd = f"QTD since {_fmt_short_date(row.qtd_since)}: " + " · ".join(qtd_bits)

    implied = _render_implied_move(row.implied_move)
    post = _render_post_moves(row.post_moves)
    beats = (
        f"{_render_beat_rate(row.eps_beat, 'EPS beat rate')} · "
        f"{_render_beat_rate(row.rev_beat, 'Rev beat rate')}"
    )
    call = f"Conference call: {_fmt_call_et(row.call_datetime_utc, row.event_date)}"

    return [header, consensus, qtd, implied, post, beats, call]


# ---------------------------------------------------------------------------
# Slack builders
# ---------------------------------------------------------------------------


SLACK_MAX_BLOCKS = 48


def build_preview_fallback(rows: list[PreviewRow], as_of: date) -> str:
    lines = [
        f"Consensus previews — {len(rows)} name(s) (as of {_fmt_short_date(as_of.isoformat())})",
        "",
    ]
    if not rows:
        lines.append("(no upcoming reporters in window)")
        return "\n".join(lines)
    for row in rows:
        lines.extend(_render_row_lines(row))
        lines.append("")
    return "\n".join(lines).rstrip()


def build_preview_blocks(
    rows: list[PreviewRow], as_of: date
) -> tuple[list[dict], int]:
    """Build the Block Kit payload and return (blocks, n_included).

    Rows are added WHOLE (section + optional degraded-context) until the next
    row would push the payload past SLACK_MAX_BLOCKS; the loop then stops. It
    never truncates mid-row. `n_included` (rows actually in the payload, in
    order) lets the caller mark ONLY posted rows for dedup and warn about the
    overflow — instead of the old truncate-then-mark-all, which silently
    dropped overflow rows yet recorded them as posted.
    """
    body: list[dict] = []
    n_included = 0
    # Reserve the 3 lead blocks (header + context + divider).
    body_budget = SLACK_MAX_BLOCKS - 3
    for row in rows:
        header, *rest = _render_row_lines(row)
        row_blocks: list[dict] = [{
            "type": "section",
            "text": {"type": "mrkdwn", "text": f"{header}\n{chr(10).join(rest)}"[:2900]},
        }]
        if row.na_notes:
            row_blocks.append({
                "type": "context",
                "elements": [
                    {
                        "type": "mrkdwn",
                        "text": "Degraded: " + "; ".join(row.na_notes)[:1900],
                    }
                ],
            })
        if len(body) + len(row_blocks) > body_budget:
            break
        body.extend(row_blocks)
        n_included += 1

    count_label = (
        f"{len(rows)} name(s)" if n_included == len(rows)
        else f"{n_included} of {len(rows)} name(s)"
    )
    lead: list[dict] = [
        {
            "type": "header",
            "text": {
                "type": "plain_text",
                "text": f"Consensus previews — {count_label}"[:150],
            },
        },
        {
            "type": "context",
            "elements": [
                {
                    "type": "mrkdwn",
                    "text": (
                        f"As of {_fmt_short_date(as_of.isoformat())} · "
                        "consensus + options-implied moves · free data "
                        "(yfinance + SQLite)"
                    ),
                }
            ],
        },
        {"type": "divider"},
    ]
    return lead + body, n_included


# ---------------------------------------------------------------------------
# JSON export (mirrors scripts/export_upcoming_events.py contract)
# ---------------------------------------------------------------------------


def _beat_rate_json(br: BeatRate) -> dict:
    return {"beats": br.beats, "total": br.total, "source": br.source}


def _row_to_json(row: PreviewRow) -> dict:
    return {
        "ticker": row.ticker,
        "company_name": row.company_name,
        "tier": row.tier,
        "event_date": row.event_date,
        "event_hour": (row.event_hour or "").lower() or None,
        "call_datetime_utc": row.call_datetime_utc,
        "last_price": row.last_price,
        "day_change_pct": row.day_change_pct,
        "consensus": {
            "eps": {
                "mean": row.eps_mean,
                "count": row.eps_range.count,
                "low": row.eps_range.low,
                "high": row.eps_range.high,
            },
            "revenue": {
                "mean": row.rev_mean,
                "count": None,
                "low": None,
                "high": None,
            },
        },
        "qtd": {
            "since": row.qtd_since,
            "ticker_pct": row.qtd_ticker_pct,
            "spx_pct": row.qtd_spx_pct,
            "sector_etf": row.sector_etf,
            "sector_etf_pct": row.sector_etf_pct,
        },
        "implied_move": {
            "pct": row.implied_move.pct,
            "expiry": row.implied_move.expiry,
            "method": row.implied_move.method,
            "notes": row.implied_move.notes,
        },
        "post_earnings_moves": [
            {
                "event_date": m.event_date,
                "move_pct": m.move_pct,
                "window_label": m.window_label,
            }
            for m in row.post_moves
        ],
        "beat_rates": {
            "eps": _beat_rate_json(row.eps_beat),
            "revenue": _beat_rate_json(row.rev_beat),
        },
        "na_fields": row.na_fields,
    }


def write_preview_export(
    rows: list[PreviewRow], out_path: Path, window: dict
) -> int:
    """Write the versioned preview JSON. Returns the count written.

    Mirrors the upcoming_events.json contract: schema_version, source,
    generated_at (ISO-Z), window{start,end}, counts{previews}, previews[].
    """
    import json

    payload = {
        "schema_version": SCHEMA_VERSION,
        "source": "earnings-agent",
        "generated_at": datetime.now(timezone.utc)
        .isoformat(timespec="seconds")
        .replace("+00:00", "Z"),
        "window": window,
        "counts": {"previews": len(rows)},
        "previews": [_row_to_json(r) for r in rows],
    }
    out_path.parent.mkdir(parents=True, exist_ok=True)
    out_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
    return len(rows)
```
codex
I’ll verify this against the actual workspace files rather than just the pasted diff, then I’ll focus only on Critical/High correctness and safety risks with concrete line references.
exec
/bin/bash -lc "nl -ba main.py | sed -n '4240,4340p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
  4240	        help="Scan configured IR RSS feeds (ir_feeds.json) for earnings-date announcements; upgrade estimated Tier 1 events to confirmed when found",
  4241	    )
  4242	    parser.add_argument(
  4243	        "--check-ir-emails",
  4244	        action="store_true",
  4245	        help="Scan Gmail (via gmail_token.json) for IR-alert emails on Tier 1/2 estimated events; upgrade to confirmed and store the Gmail thread URL",
  4246	    )
  4247	    parser.add_argument(
  4248	        "--check-replies",
  4249	        action="store_true",
  4250	        help="Poll Slack threads for replies on open questions; apply commands (lock/wait/snooze/ignore/etc) to DB state",
  4251	    )
  4252	    parser.add_argument(
  4253	        "--consensus-preview",
  4254	        action="store_true",
  4255	        help="Build the pre-earnings consensus-metrics preview (StreetAccount §9A) "
  4256	             "for upcoming Tier-1 reporters and post to #street-account",
  4257	    )
  4258	    parser.add_argument(
  4259	        "--preview-ticker",
  4260	        type=str,
  4261	        default=None,
  4262	        metavar="TICKER",
  4263	        help="Restrict --consensus-preview to a single ticker's next upcoming event (any tier)",
  4264	    )
  4265	    parser.add_argument(
  4266	        "--preview-days",
  4267	        type=int,
  4268	        default=3,
  4269	        help="Look-ahead window (days) for --consensus-preview reporter selection (default: 3)",
  4270	    )
  4271	    args = parser.parse_args()
  4272	
  4273	    # --populate-db-only is a self-documenting alias for --dry-run (DB writes
  4274	    # only, no external side effects). Fold it in so all downstream checks of
  4275	    # args.dry_run see it.
  4276	    if getattr(args, "populate_db_only", False):
  4277	        args.dry_run = True
  4278	
  4279	    if args.list_locks:
  4280	        run_list_locks()
  4281	    elif args.lock:
  4282	        run_set_lock(args.lock, locked=True)
  4283	    elif args.unlock:
  4284	        run_set_lock(args.unlock, locked=False)
  4285	    elif args.ticktick_status:
  4286	        config = get_ticktick_config()
  4287	        if not config:
  4288	            logger.error("TickTick not configured. Set TICKTICK_ACCESS_TOKEN in .env")
  4289	            sys.exit(1)
  4290	        show_ticktick_status(config["token"])
  4291	    elif args.weekly_digest:
  4292	        run_weekly_digest(dry_run=args.dry_run)
  4293	    elif args.check_results:
  4294	        run_check_results(
  4295	            target_date=args.date,
  4296	            dry_run=args.dry_run,
  4297	            skip_heartbeat=args.no_heartbeat,
  4298	        )
  4299	    elif args.check_missed_results:
  4300	        _run_safeguard(
  4301	            "EDGAR missed-results backstop",
  4302	            lambda: run_edgar_results_fallback(
  4303	                dry_run=args.dry_run,
  4304	                skip_heartbeat=args.no_heartbeat,
  4305	            ),
  4306	        )
  4307	    elif args.reconcile_calendar:
  4308	        run_reconcile_calendar(dry_run=args.dry_run)
  4309	    elif args.cross_check:
  4310	        _run_safeguard(
  4311	            "cross-check (Finnhub vs yfinance + EDGAR)",
  4312	            lambda: run_cross_check(dry_run=args.dry_run),
  4313	        )
  4314	    elif args.refresh_descriptions:
  4315	        run_refresh_descriptions(dry_run=args.dry_run)
  4316	    elif args.check_announcements:
  4317	        run_check_announcements(dry_run=args.dry_run)
  4318	    elif args.check_ir_emails:
  4319	        run_check_ir_emails(dry_run=args.dry_run)
  4320	    elif args.check_replies:
  4321	        _run_safeguard(
  4322	            "Slack reply processing",
  4323	            lambda: run_check_replies(dry_run=args.dry_run),
  4324	        )
  4325	    elif args.consensus_preview:
  4326	        run_consensus_preview(
  4327	            dry_run=args.dry_run,
  4328	            days_ahead=args.preview_days,
  4329	            ticker=args.preview_ticker,
  4330	        )
  4331	    elif args.cleanup:
  4332	        conn = init_db()
  4333	        cleanup_duplicates(conn, dry_run=args.dry_run)
  4334	        conn.close()
  4335	    else:
  4336	        run(
  4337	            dry_run=args.dry_run,
  4338	            backfill=args.backfill,
  4339	            skip_ticktick=args.no_ticktick,
  4340	            skip_heartbeat=args.no_heartbeat,

exec
/bin/bash -lc "nl -ba main.py | sed -n '1580,1745p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
  1580	        logger.info("=" * 50)
  1581	        logger.info("DRY RUN — Slack payload preview:")
  1582	        logger.info(fallback)
  1583	        for block in blocks:
  1584	            if block.get("type") == "section":
  1585	                logger.info(f"  [section] {block['text']['text'][:200]}")
  1586	            elif block.get("type") == "header":
  1587	                logger.info(f"  [header] {block['text']['text']}")
  1588	            elif block.get("type") == "context":
  1589	                for el in block.get("elements", []):
  1590	                    logger.info(f"  [context] {el.get('text', '')[:200]}")
  1591	            elif block.get("type") == "divider":
  1592	                logger.info("  [divider]")
  1593	        logger.info("=" * 50)
  1594	        logger.info("(Dry run — no Slack post, no email draft created)")
  1595	        return
  1596	
  1597	    if not SLACK_WEBHOOK_EARNINGS:
  1598	        logger.warning("SLACK_WEBHOOK_EARNINGS not set — skipping Slack post.")
  1599	    else:
  1600	        try:
  1601	            post_slack(SLACK_WEBHOOK_EARNINGS, blocks, fallback)
  1602	        except NotificationError as exc:
  1603	            logger.error(f"Slack digest post failed: {exc}")
  1604	
  1605	    logger.info("Weekly digest complete. Run Gmail MCP draft creation "
  1606	                f"using content from {DIGEST_HTML_PATH}")
  1607	
  1608	
  1609	# ---------------------------------------------------------------------------
  1610	# Consensus Metrics Preview (StreetAccount §9A) — additive, run() untouched
  1611	# ---------------------------------------------------------------------------
  1612	
  1613	
  1614	def run_consensus_preview(
  1615	    dry_run: bool = False,
  1616	    days_ahead: int = 3,
  1617	    ticker: str | None = None,
  1618	):
  1619	    """Build + post the pre-earnings consensus-metrics preview.
  1620	
  1621	    Core fields are free/cached (yfinance + SQLite + Coverage Manager). The
  1622	    revenue beat rate additionally uses FMP's /stable/earnings when FMP_API_KEY
  1623	    is set (verified Starter-available 2026-07-12); the estimate count/range
  1624	    stays n/a (quarterly analyst-estimates is Starter-gated).
  1625	
  1626	    Dedup: kv_store `consensus_preview_posted:TICKER:EVENT_DATE`. Post-then-mark
  1627	    (mark only after a successful Slack post) so a failed post retries; only
  1628	    rows actually included in the posted payload are marked.
  1629	    --dry-run writes the JSON export but posts nothing and marks nothing.
  1630	    """
  1631	    import json
  1632	    from pathlib import Path
  1633	
  1634	    coverage = load_coverage()
  1635	    if not coverage:
  1636	        logger.error("No tickers loaded. Cannot build consensus preview.")
  1637	        sys.exit(1)
  1638	    coverage_map = {t.ticker: t for t in coverage}
  1639	
  1640	    conn = init_db()
  1641	
  1642	    reporters = select_upcoming_reporters(
  1643	        conn, coverage, days_ahead=days_ahead, max_tier=1, ticker=ticker
  1644	    )
  1645	
  1646	    # Dedup (live runs only — dry-run must never consult/mutate the ledger,
  1647	    # so a preview is re-previewable on demand).
  1648	    if not dry_run and reporters:
  1649	        pending = []
  1650	        for r in reporters:
  1651	            key = f"consensus_preview_posted:{r.ticker}:{r.event_date}"
  1652	            if kv_get(conn, key):
  1653	                logger.info(
  1654	                    f"Preview already posted for {r.ticker} {r.event_date}; skipping"
  1655	                )
  1656	                continue
  1657	            pending.append(r)
  1658	        reporters = pending
  1659	
  1660	    # Revenue beat rate comes from FMP /stable/earnings (verified available on
  1661	    # the Starter plan 2026-07-12); estimate count/range stays n/a (Starter-
  1662	    # gated). Gate purely on the key — the endpoint is confirmed, not probed.
  1663	    fmp_ok = bool(FMP_API_KEY)
  1664	    logger.info(
  1665	        "FMP revenue-beat %s", "ON" if fmp_ok else "OFF (no FMP_API_KEY)"
  1666	    )
  1667	
  1668	    as_of = date.today()
  1669	    rows = assemble_preview_rows(
  1670	        conn, reporters, coverage_map,
  1671	        fmp_key=(FMP_API_KEY if fmp_ok else None),
  1672	        fmp_ok=fmp_ok,
  1673	    )
  1674	
  1675	    # Window for the export mirrors the selection window (or the single event).
  1676	    if ticker and rows:
  1677	        window = {"start": as_of.isoformat(), "end": rows[0].event_date}
  1678	    else:
  1679	        window = {
  1680	            "start": as_of.isoformat(),
  1681	            "end": (as_of + timedelta(days=days_ahead)).isoformat(),
  1682	        }
  1683	    out_path = Path(__file__).parent / "exports" / "consensus_preview.json"
  1684	    n = write_preview_export(rows, out_path, window)
  1685	    logger.info(f"Wrote {n} consensus preview(s) to {out_path}")
  1686	
  1687	    blocks, n_included = build_preview_blocks(rows, as_of)
  1688	    fallback = build_preview_fallback(rows, as_of)
  1689	
  1690	    if dry_run:
  1691	        logger.info("=" * 50)
  1692	        logger.info("DRY RUN — consensus preview fallback text:")
  1693	        logger.info("\n" + fallback)
  1694	        logger.info("-" * 50)
  1695	        logger.info("DRY RUN — Block Kit JSON:")
  1696	        logger.info(json.dumps(blocks, indent=2))
  1697	        logger.info("=" * 50)
  1698	        logger.info("(Dry run — no Slack post, no kv_store marks)")
  1699	        conn.close()
  1700	        return
  1701	
  1702	    if not rows:
  1703	        logger.info("No upcoming reporters to preview — nothing to post.")
  1704	        conn.close()
  1705	        return
  1706	
  1707	    if not SLACK_WEBHOOK_STREET_ACCOUNT:
  1708	        conn.close()
  1709	        raise NotificationError(
  1710	            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
  1711	        )
  1712	
  1713	    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
  1714	    # Post-then-mark, and mark ONLY rows actually in the posted payload
  1715	    # (build_preview_blocks stops at the Slack block cap and returns how many
  1716	    # it included, in order). Overflow rows stay unmarked so the next run
  1717	    # re-posts them — they are never silently dropped-yet-marked.
  1718	    posted_rows = rows[:n_included]
  1719	    if n_included < len(rows):
  1720	        overflow = rows[n_included:]
  1721	        logger.warning(
  1722	            "Consensus preview overflow: posted %d of %d rows (Slack block cap); "
  1723	            "%d left unmarked for the next run: %s",
  1724	            n_included, len(rows), len(overflow),
  1725	            ", ".join(r.ticker for r in overflow),
  1726	        )
  1727	    for r in posted_rows:
  1728	        kv_set(
  1729	            conn,
  1730	            f"consensus_preview_posted:{r.ticker}:{r.event_date}",
  1731	            f"posted:{as_of.isoformat()}",
  1732	        )
  1733	    logger.info(f"Posted consensus preview for {len(posted_rows)} name(s)")
  1734	    conn.close()
  1735	
  1736	
  1737	# ---------------------------------------------------------------------------
  1738	# Shared results notification (Slack + TickTick)
  1739	# ---------------------------------------------------------------------------
  1740	
  1741	
  1742	def notify_results(
  1743	    conn, results: list[ResultRow], as_of: date
  1744	) -> bool:
  1745	    """

exec
/bin/bash -lc "nl -ba consensus_preview.py | sed -n '1,260p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
     1	"""
     2	Consensus Metrics Preview (StreetAccount Wave 1 §9A).
     3	
     4	Builds a pre-earnings brief for each upcoming covered reporter, composed
     5	entirely from FREE / cached data (yfinance + the SQLite events table +
     6	Coverage Manager exports). One brief per upcoming Tier-1 name carries:
     7	
     8	  - header: company, event date, BMO/AMC badge, last price ± day change
     9	  - mean consensus Rev + EPS (from the DB events table — zero API calls)
    10	  - QTD price move since the prior quarter-end for TICKER / SPY / sector-ETF
    11	  - options-implied move (ATM straddle mid ÷ spot on the first expiry that
    12	    contains the post-earnings reaction — see compute_implied_move)
    13	  - last-4-quarter post-earnings stock reactions
    14	  - EPS beat rate (up to 20 quarters, yfinance Surprise%)
    15	  - conference-call time (from events.call_datetime_utc)
    16	
    17	Two further fields are FMP-Starter-gated (estimate count/low-high range and
    18	the revenue beat rate). They are only fetched when the caller passes an FMP
    19	key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
    20	then they render as an explicit "n/a (FMP endpoint unavailable)" — never a
    21	silent blank and never a fabricated number.
    22	
    23	Design invariant: NO field is ever silently dropped. Every degraded field
    24	appends a human note to PreviewRow.na_notes and a machine-readable
    25	{field, reason} entry to PreviewRow.na_fields, and renders as an explicit
    26	"n/a (reason)" string. A per-ticker failure never drops the row.
    27	
    28	This module is additive — it does not touch the daily run() flow.
    29	"""
    30	
    31	from __future__ import annotations
    32	
    33	import io
    34	import logging
    35	import sqlite3
    36	from contextlib import contextmanager, redirect_stderr, redirect_stdout
    37	from dataclasses import dataclass, field
    38	from datetime import date, datetime, timedelta, timezone
    39	from pathlib import Path
    40	from typing import Optional
    41	
    42	try:
    43	    from zoneinfo import ZoneInfo
    44	except ImportError:  # pragma: no cover - py<3.9 fallback
    45	    ZoneInfo = None
    46	
    47	import yfinance as yf
    48	
    49	from coverage import TickerInfo
    50	from market_data import (
    51	    PostEarningsMove,
    52	    fetch_post_earnings_move,
    53	    infer_hour_from_datetime,
    54	)
    55	
    56	logger = logging.getLogger("earnings_agent")
    57	
    58	SCHEMA_VERSION = 1
    59	
    60	# Coverage-Manager sector (or subsector) -> sector-proxy ETF for the QTD row.
    61	# Keys are the canonical CM sector labels. Unknown sector -> no ETF row + note.
    62	SECTOR_ETF: dict[str, str] = {
    63	    "Healthcare Services": "XLV",
    64	    "MedTech": "IHI",
    65	    "Large Pharma": "XLV",
    66	    "Biotech": "XBI",
    67	    "Fintech": "XLF",
    68	}
    69	
    70	# Implied-move sanity gates (percent of spot). A straddle mid outside this
    71	# band is almost always a data artifact (stale/one-sided quotes, wrong
    72	# strike), not a real expectation — surface it as n/a rather than a bad number.
    73	_IMPLIED_MOVE_MIN_PCT = 0.5
    74	_IMPLIED_MOVE_MAX_PCT = 40.0
    75	# Nearest common strike must be within this fraction of spot to be "ATM".
    76	_ATM_MAX_STRIKE_DISTANCE = 0.05
    77	# A leg with (ask - bid)/mid above this is flagged "wide market".
    78	_WIDE_MARKET_RATIO = 0.6
    79	# Expiry more than this many days past the reaction date carries extra
    80	# time value; we annotate so the reader knows the move is inflated.
    81	_EXTRA_TIME_VALUE_DAYS = 7
    82	
    83	
    84	# ---------------------------------------------------------------------------
    85	# Dataclasses
    86	# ---------------------------------------------------------------------------
    87	
    88	
    89	@dataclass
    90	class ImpliedMove:
    91	    """Options-implied one-day move from an ATM straddle.
    92	
    93	    pct is None when the move could not be computed for any reason; in that
    94	    case notes[0] carries the "n/a (reason)" string. When pct is present,
    95	    notes holds any annotations ("stale quote", "wide market",
    96	    "(includes N extra days of time value)").
    97	    """
    98	    pct: Optional[float]
    99	    expiry: Optional[str]
   100	    method: str
   101	    notes: list[str] = field(default_factory=list)
   102	
   103	    @property
   104	    def ok(self) -> bool:
   105	        return self.pct is not None
   106	
   107	
   108	@dataclass
   109	class EstimateRange:
   110	    """FMP analyst-estimate spread (count + low/high). All None => n/a."""
   111	    count: Optional[int]
   112	    low: Optional[float]
   113	    high: Optional[float]
   114	
   115	    @property
   116	    def ok(self) -> bool:
   117	        return self.count is not None
   118	
   119	
   120	@dataclass
   121	class BeatRate:
   122	    """Historical beat rate (beats out of total). beats/total None => n/a."""
   123	    beats: Optional[int]
   124	    total: Optional[int]
   125	    source: str
   126	
   127	    @property
   128	    def ok(self) -> bool:
   129	        return self.total is not None and self.total > 0
   130	
   131	
   132	@dataclass
   133	class QuarterMove:
   134	    """One historical post-earnings stock reaction."""
   135	    event_date: str
   136	    move_pct: float
   137	    window_label: str
   138	
   139	
   140	@dataclass
   141	class Reporter:
   142	    """A single upcoming reporter selected for a preview (pre-assembly)."""
   143	    ticker: str
   144	    company_name: str
   145	    tier: int
   146	    event_date: str
   147	    event_hour: Optional[str]
   148	    call_datetime_utc: Optional[str]
   149	    eps_estimate: Optional[float]
   150	    rev_estimate: Optional[float]
   151	    sector: str
   152	    subsector: str
   153	
   154	
   155	@dataclass
   156	class PreviewRow:
   157	    """The fully-assembled preview for one reporter."""
   158	    ticker: str
   159	    company_name: str
   160	    tier: int
   161	    event_date: str
   162	    event_hour: Optional[str]
   163	    call_datetime_utc: Optional[str]
   164	
   165	    last_price: Optional[float] = None
   166	    day_change_pct: Optional[float] = None
   167	
   168	    eps_mean: Optional[float] = None
   169	    rev_mean: Optional[float] = None
   170	    eps_range: EstimateRange = field(
   171	        default_factory=lambda: EstimateRange(None, None, None)
   172	    )
   173	
   174	    qtd_since: Optional[str] = None
   175	    qtd_ticker_pct: Optional[float] = None
   176	    qtd_spx_pct: Optional[float] = None
   177	    sector_etf: Optional[str] = None          # ETF ticker, e.g. "XLV"
   178	    sector_etf_label: Optional[str] = None    # human label, e.g. "Healthcare Services"
   179	    sector_etf_pct: Optional[float] = None
   180	
   181	    implied_move: ImpliedMove = field(
   182	        default_factory=lambda: ImpliedMove(None, None, "ATM straddle", ["n/a (not computed)"])
   183	    )
   184	
   185	    post_moves: list[QuarterMove] = field(default_factory=list)
   186	
   187	    eps_beat: BeatRate = field(default_factory=lambda: BeatRate(None, None, "yfinance"))
   188	    rev_beat: BeatRate = field(default_factory=lambda: BeatRate(None, None, "fmp"))
   189	
   190	    na_notes: list[str] = field(default_factory=list)
   191	    na_fields: list[dict] = field(default_factory=list)
   192	
   193	    def mark_na(self, field_name: str, reason: str) -> None:
   194	        """Record a degraded/unavailable field once, both human + machine."""
   195	        self.na_notes.append(f"{field_name}: {reason}")
   196	        self.na_fields.append({"field": field_name, "reason": reason})
   197	
   198	
   199	# ---------------------------------------------------------------------------
   200	# yfinance noise suppression
   201	# ---------------------------------------------------------------------------
   202	
   203	
   204	@contextmanager
   205	def _silence():
   206	    """Silence yfinance's chatty stdout/stderr (matches market_data idiom)."""
   207	    buf_out, buf_err = io.StringIO(), io.StringIO()
   208	    with redirect_stdout(buf_out), redirect_stderr(buf_err):
   209	        yield
   210	
   211	
   212	# ---------------------------------------------------------------------------
   213	# Small helpers
   214	# ---------------------------------------------------------------------------
   215	
   216	
   217	def _parse_iso_date(s: str) -> Optional[date]:
   218	    try:
   219	        return date.fromisoformat(s[:10])
   220	    except (ValueError, TypeError):
   221	        return None
   222	
   223	
   224	def _prior_quarter_end(today: date) -> date:
   225	    """Return the calendar-quarter-end immediately before `today`'s quarter.
   226	
   227	    For any day in Q3 (Jul-Sep) this is Jun 30; the base for the QTD move.
   228	    """
   229	    q_start_month = ((today.month - 1) // 3) * 3 + 1
   230	    q_start = date(today.year, q_start_month, 1)
   231	    return q_start - timedelta(days=1)
   232	
   233	
   234	def sector_etf_for(sector: str, subsector: str) -> Optional[tuple[str, str]]:
   235	    """Map a CM sector (then subsector) to (label, etf_ticker) or None."""
   236	    for key in (sector, subsector):
   237	        if key and key in SECTOR_ETF:
   238	            return key, SECTOR_ETF[key]
   239	    return None
   240	
   241	
   242	def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
   243	    """The trading day whose close reflects the market's reaction.
   244	
   245	    bmo / dmh           -> the event date itself. A pre-open (bmo) OR during-
   246	                           market-hours (dmh) release reacts the SAME session.
   247	    amc / blank         -> the next weekday after the event date (post-close or
   248	                           unknown-timing release reacts the following session;
   249	                           unknown is treated as amc so the chosen expiry can
   250	                           never fall BEFORE an actual post-close reaction).
   251	                           Weekend-skip only (no holiday calendar).
   252	    """
   253	    ed = _parse_iso_date(event_date)
   254	    if ed is None:
   255	        return None
   256	    # Mirror market_data.fetch_post_earnings_move: only amc defers to the next
   257	    # session; bmo and dmh both react same-day.
   258	    if (event_hour or "").lower() in ("bmo", "dmh"):
   259	        return ed
   260	    d = ed + timedelta(days=1)

exec
/bin/bash -lc "rg -n \"build_preview_blocks|eps_range|fetch_fmp_rev_beat_history|fetch_fmp_estimate_range|event_hour_yf|reaction_date|rows\\[:n_included\\]|n_included|SLACK_WEBHOOK_STREET_ACCOUNT|fmp_preview_endpoints_ok|/stable/earnings|revenueActual|revenueEstimated\" ." in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 133ms:
./codex_feedback/codex_feedback_2026-07-12_231953.md:47:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231953.md:59:+    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_231953.md:70:+    build_preview_blocks,
./codex_feedback/codex_feedback_2026-07-12_231953.md:96:+    `fmp_preview_endpoints_ok` == "true"; otherwise they render n/a.
./codex_feedback/codex_feedback_2026-07-12_231953.md:133:+    fmp_ok = bool(FMP_API_KEY) and (kv_get(conn, "fmp_preview_endpoints_ok") == "true")
./codex_feedback/codex_feedback_2026-07-12_231953.md:157:+    blocks = build_preview_blocks(rows, as_of)
./codex_feedback/codex_feedback_2026-07-12_231953.md:177:+    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_231953.md:180:+            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_231953.md:183:+    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_231953.md:259:key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_231953.md:410:    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_231953.md:482:def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./codex_feedback/codex_feedback_2026-07-12_231953.md:713:    rxn = reaction_date(event_date, event_hour)
./codex_feedback/codex_feedback_2026-07-12_231953.md:908:def fetch_fmp_estimate_range(
./codex_feedback/codex_feedback_2026-07-12_231953.md:954:def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_231953.md:980:        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_231953.md:1142:        row.eps_range = fetch_fmp_estimate_range(rep.ticker, rep.event_date, fmp_key)
./codex_feedback/codex_feedback_2026-07-12_231953.md:1143:        if not row.eps_range.ok:
./codex_feedback/codex_feedback_2026-07-12_231953.md:1145:        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./codex_feedback/codex_feedback_2026-07-12_231953.md:1281:        f"{_render_estimate_range(row.eps_range)}"
./codex_feedback/codex_feedback_2026-07-12_231953.md:1327:def build_preview_blocks(rows: list[PreviewRow], as_of: date) -> list[dict]:
./codex_feedback/codex_feedback_2026-07-12_231953.md:1398:                "count": row.eps_range.count,
./codex_feedback/codex_feedback_2026-07-12_231953.md:1399:                "low": row.eps_range.low,
./codex_feedback/codex_feedback_2026-07-12_231953.md:1400:                "high": row.eps_range.high,
./codex_feedback/codex_feedback_2026-07-12_231953.md:1725:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231953.md:1832:+key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_231953.md:1983:+    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_231953.md:2055:+def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./codex_feedback/codex_feedback_2026-07-12_231953.md:2286:+    rxn = reaction_date(event_date, event_hour)
./codex_feedback/codex_feedback_2026-07-12_231953.md:2481:+def fetch_fmp_estimate_range(
./codex_feedback/codex_feedback_2026-07-12_231953.md:2527:+def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_231953.md:2553:+        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_231953.md:2715:+        row.eps_range = fetch_fmp_estimate_range(rep.ticker, rep.event_date, fmp_key)
./codex_feedback/codex_feedback_2026-07-12_231953.md:2716:+        if not row.eps_range.ok:
./codex_feedback/codex_feedback_2026-07-12_231953.md:2718:+        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./codex_feedback/codex_feedback_2026-07-12_231953.md:2854:+        f"{_render_estimate_range(row.eps_range)}"
./codex_feedback/codex_feedback_2026-07-12_231953.md:2900:+def build_preview_blocks(rows: list[PreviewRow], as_of: date) -> list[dict]:
./codex_feedback/codex_feedback_2026-07-12_231953.md:2971:+                "count": row.eps_range.count,
./codex_feedback/codex_feedback_2026-07-12_231953.md:2972:+                "low": row.eps_range.low,
./codex_feedback/codex_feedback_2026-07-12_231953.md:2973:+                "high": row.eps_range.high,
./codex_feedback/codex_feedback_2026-07-12_231953.md:3398:-        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:3843:-        rev_act = e.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_231953.md:3894:-            cached_yf = (existing or {}).get("event_hour_yf") if existing else None
./codex_feedback/codex_feedback_2026-07-12_231953.md:4123:-                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:4191:-                                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:4299:-                                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:4350:-                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:4791:-        rev_act = e.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_231953.md:4875:-                    hour_yf=existing.get("event_hour_yf") if existing else None,
./codex_feedback/codex_feedback_2026-07-12_231953.md:5462:-        rev_act = fh.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_231953.md:5531:-            event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:5675:-        hour_yf = db_row.get("event_hour_yf")
./codex_feedback/codex_feedback_2026-07-12_231953.md:6291:-        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:7246:+    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_231953.md:7312:+    build_preview_blocks,
./codex_feedback/codex_feedback_2026-07-12_231953.md:7590:+        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:8035:+        rev_act = e.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_231953.md:8086:+            cached_yf = (existing or {}).get("event_hour_yf") if existing else None
./codex_feedback/codex_feedback_2026-07-12_231953.md:8315:+                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:8383:+                                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:8491:+                                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:8542:+                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:8846:+    `fmp_preview_endpoints_ok` == "true"; otherwise they render n/a.
./codex_feedback/codex_feedback_2026-07-12_231953.md:8883:+    fmp_ok = bool(FMP_API_KEY) and (kv_get(conn, "fmp_preview_endpoints_ok") == "true")
./codex_feedback/codex_feedback_2026-07-12_231953.md:8907:+    blocks = build_preview_blocks(rows, as_of)
./codex_feedback/codex_feedback_2026-07-12_231953.md:8927:+    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_231953.md:8930:+            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_231953.md:8933:+    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_231953.md:9097:+        rev_act = e.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_231953.md:9181:+                    hour_yf=existing.get("event_hour_yf") if existing else None,
./codex_feedback/codex_feedback_2026-07-12_231953.md:9768:+        rev_act = fh.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_231953.md:9837:+            event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:9981:+        hour_yf = db_row.get("event_hour_yf")
./codex_feedback/codex_feedback_2026-07-12_231953.md:10597:+        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:11617:    19	key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_231953.md:11768:   170	    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_231953.md:11840:   242	def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./codex_feedback/codex_feedback_2026-07-12_231953.md:12076:   473	    rxn = reaction_date(event_date, event_hour)
./codex_feedback/codex_feedback_2026-07-12_231953.md:12251:    24	    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_231953.md:12317:    90	    build_preview_blocks,
./codex_feedback/codex_feedback_2026-07-12_231953.md:12382:  1624	    `fmp_preview_endpoints_ok` == "true"; otherwise they render n/a.
./codex_feedback/codex_feedback_2026-07-12_231953.md:12419:  1661	    fmp_ok = bool(FMP_API_KEY) and (kv_get(conn, "fmp_preview_endpoints_ok") == "true")
./codex_feedback/codex_feedback_2026-07-12_231953.md:12443:  1685	    blocks = build_preview_blocks(rows, as_of)
./codex_feedback/codex_feedback_2026-07-12_231953.md:12463:  1705	    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_231953.md:12466:  1708	            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_231953.md:12469:  1711	    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_231953.md:12634:   668	def fetch_fmp_estimate_range(
./codex_feedback/codex_feedback_2026-07-12_231953.md:12680:   714	def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_231953.md:12706:   740	        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_231953.md:12868:   902	        row.eps_range = fetch_fmp_estimate_range(rep.ticker, rep.event_date, fmp_key)
./codex_feedback/codex_feedback_2026-07-12_231953.md:12869:   903	        if not row.eps_range.ok:
./codex_feedback/codex_feedback_2026-07-12_231953.md:12871:   905	        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./codex_feedback/codex_feedback_2026-07-12_231953.md:13012:  1041	        f"{_render_estimate_range(row.eps_range)}"
./codex_feedback/codex_feedback_2026-07-12_231953.md:13058:  1087	def build_preview_blocks(rows: list[PreviewRow], as_of: date) -> list[dict]:
./codex_feedback/codex_feedback_2026-07-12_231953.md:13129:  1158	                "count": row.eps_range.count,
./codex_feedback/codex_feedback_2026-07-12_231953.md:13130:  1159	                "low": row.eps_range.low,
./codex_feedback/codex_feedback_2026-07-12_231953.md:13131:  1160	                "high": row.eps_range.high,
./codex_feedback/codex_feedback_2026-07-12_231953.md:13236:    41	    # rendering uses (event_hour or event_hour_yf) for the time block.
./codex_feedback/codex_feedback_2026-07-12_231953.md:13238:    43	        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
./codex_feedback/codex_feedback_2026-07-12_231953.md:13700:   309	                event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231953.md:13740:   349	        "event_hour_yf, call_datetime_utc, call_source "
./codex_feedback/codex_feedback_2026-07-12_231953.md:13755:   364	            "event_hour_yf": row[16],
./codex_feedback/codex_feedback_2026-07-12_231953.md:14430:from consensus_preview import reaction_date
./codex_feedback/codex_feedback_2026-07-12_231953.md:14432:    print(event_date, hour, reaction_date(event_date, hour))
./codex_feedback/codex_feedback_2026-07-12_231953.md:14437:from consensus_preview import PreviewRow, build_preview_blocks, build_preview_fallback, _fmt_rev, _fmt_eps, _fmt_pct
./codex_feedback/codex_feedback_2026-07-12_231953.md:14439:blocks = build_preview_blocks([row], date(2026,7,13))
./codex_feedback/codex_feedback_2026-07-12_231953.md:14467:from consensus_preview import PreviewRow, build_preview_blocks, build_preview_fallback, _fmt_rev, _fmt_eps, _fmt_pct
./codex_feedback/codex_feedback_2026-07-12_231953.md:14469:blocks = build_preview_blocks([row], date(2026,7,13))
./codex_feedback/codex_feedback_2026-07-12_231953.md:14476:from consensus_preview import reaction_date
./codex_feedback/codex_feedback_2026-07-12_231953.md:14478:    print(event_date, hour, reaction_date(event_date, hour))
./codex_feedback/codex_feedback_2026-07-12_231953.md:14742:    23	revenueEstimate, revenueActual (+ a 'source' tag for observability).
./codex_feedback/codex_feedback_2026-07-12_231953.md:14768:    49	_FMP_BASE = "https://financialmodelingprep.com/stable/earnings-calendar"
./codex_feedback/codex_feedback_2026-07-12_231953.md:14785:    66	        "revenueEstimate": row.get("revenueEstimated"),
./codex_feedback/codex_feedback_2026-07-12_231953.md:14786:    67	        "revenueActual": row.get("revenueActual"),
./codex_feedback/codex_feedback_2026-07-12_231953.md:14860:   141	    return ev.get("epsActual") is not None or ev.get("revenueActual") is not None
./codex_feedback/codex_feedback_2026-07-12_231953.md:14875:   156	    for k in ("epsEstimate", "epsActual", "revenueEstimate", "revenueActual"):
./codex_feedback/codex_feedback_2026-07-12_231953.md:15126:/bin/bash -lc 'rg -n "event_hour.*dmh|dmh|revenueEstimate|epsEstimate|upsert_event|call_datetime_utc|event_hour_yf" main.py fmp_client.py finnhub_client.py storage.py' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
./codex_feedback/codex_feedback_2026-07-12_231953.md:15129:storage.py:41:    # rendering uses (event_hour or event_hour_yf) for the time block.
./codex_feedback/codex_feedback_2026-07-12_231953.md:15130:storage.py:43:        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
./codex_feedback/codex_feedback_2026-07-12_231953.md:15134:storage.py:309:                event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15136:storage.py:349:        "event_hour_yf, call_datetime_utc, call_source "
./codex_feedback/codex_feedback_2026-07-12_231953.md:15137:storage.py:364:            "event_hour_yf": row[16],
./codex_feedback/codex_feedback_2026-07-12_231953.md:15139:storage.py:424:        "date_locked, event_hour_yf "
./codex_feedback/codex_feedback_2026-07-12_231953.md:15140:storage.py:439:            "event_hour_yf": row[13],
./codex_feedback/codex_feedback_2026-07-12_231953.md:15142:storage.py:460:    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15145:storage.py:471:    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231953.md:15146:storage.py:477:    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231953.md:15148:storage.py:498:                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231953.md:15150:storage.py:515:            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15151:storage.py:544:            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15153:storage.py:552:            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15156:fmp_client.py:23:revenueEstimate, revenueActual (+ a 'source' tag for observability).
./codex_feedback/codex_feedback_2026-07-12_231953.md:15158:fmp_client.py:66:        "revenueEstimate": row.get("revenueEstimated"),
./codex_feedback/codex_feedback_2026-07-12_231953.md:15159:fmp_client.py:156:    for k in ("epsEstimate", "epsActual", "revenueEstimate", "revenueActual"):
./codex_feedback/codex_feedback_2026-07-12_231953.md:15163:main.py:368:        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15168:main.py:864:            cached_yf = (existing or {}).get("event_hour_yf") if existing else None
./codex_feedback/codex_feedback_2026-07-12_231953.md:15173:main.py:1093:                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15176:main.py:1161:                                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15180:main.py:1269:                                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15184:main.py:1320:                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15189:main.py:1959:                    hour_yf=existing.get("event_hour_yf") if existing else None,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15198:main.py:2615:            event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15200:main.py:2759:        hour_yf = db_row.get("event_hour_yf")
./codex_feedback/codex_feedback_2026-07-12_231953.md:15205:main.py:3375:        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15209:The surrounding schema confirms `dmh` is a real stored timing value, not hypothetical. I’m checking whether the preview uses `event_hour_yf` anywhere, because a missing fallback could be another realistic wrong-timing path.
./codex_feedback/codex_feedback_2026-07-12_231953.md:15229:   460	    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15240:   471	    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231953.md:15246:   477	    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231953.md:15267:   498	                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231953.md:15284:   515	            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15313:   544	            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15321:   552	            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15400:  1959	                    hour_yf=existing.get("event_hour_yf") if existing else None,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15472:   813	        rev_act = e.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_231953.md:15523:   864	            cached_yf = (existing or {}).get("event_hour_yf") if existing else None
./codex_feedback/codex_feedback_2026-07-12_231953.md:15562:/bin/bash -lc "rg -n \"event_hour_yf|COALESCE\\(event_hour|hour_yf\" notifications.py calendar_sync.py main.py consensus_preview.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
./codex_feedback/codex_feedback_2026-07-12_231953.md:15577:main.py:368:        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15579:main.py:864:            cached_yf = (existing or {}).get("event_hour_yf") if existing else None
./codex_feedback/codex_feedback_2026-07-12_231953.md:15587:main.py:1093:                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15590:main.py:1161:                                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15595:main.py:1269:                                event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15599:main.py:1320:                    event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15600:main.py:1959:                    hour_yf=existing.get("event_hour_yf") if existing else None,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15606:main.py:2615:            event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15607:main.py:2759:        hour_yf = db_row.get("event_hour_yf")
./codex_feedback/codex_feedback_2026-07-12_231953.md:15615:main.py:3375:        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231953.md:15618:The selection path does ignore `event_hour_yf`, which the existing app explicitly uses as the effective timing fallback. That creates a realistic wrong BMO/AMC badge and wrong expiry selection for FMP/Finnhub rows with empty canonical hour but cached yfinance timing.
./codex_feedback/codex_feedback_2026-07-12_231953.md:15623:from consensus_preview import select_upcoming_reporters, reaction_date
./codex_feedback/codex_feedback_2026-07-12_231953.md:15625:conn.execute('create table events (ticker text,event_date text,event_hour text,event_hour_yf text,tier integer,company_name text,eps_estimate real,rev_estimate real,call_datetime_utc text,reported integer)')
./codex_feedback/codex_feedback_2026-07-12_231953.md:15628:print('selected_hour=', rep.event_hour, 'reaction=', reaction_date(rep.event_date, rep.event_hour))
./codex_feedback/codex_feedback_2026-07-12_231953.md:15632:from consensus_preview import reaction_date
./codex_feedback/codex_feedback_2026-07-12_231953.md:15633:print('dmh friday reaction=', reaction_date('2026-07-17', 'dmh'))
./codex_feedback/codex_feedback_2026-07-12_231953.md:15634:print('bmo friday reaction=', reaction_date('2026-07-17', 'bmo'))
./codex_feedback/codex_feedback_2026-07-12_231953.md:15639:from consensus_preview import PreviewRow, build_preview_blocks
./codex_feedback/codex_feedback_2026-07-12_231953.md:15645:blocks=build_preview_blocks(rows, date(2026,7,13))
./codex_feedback/codex_feedback_2026-07-12_231953.md:15744:1. **High: `event_hour_yf` fallback is ignored.**  
./codex_feedback/codex_feedback_2026-07-12_231953.md:15746:   Repro: DB row `event_hour=NULL,event_hour_yf='bmo',event_date='2026-07-17'` -> preview uses `None`, so `reaction_date(..., None)` returns `2026-07-20` instead of `2026-07-17`.
./codex_feedback/codex_feedback_2026-07-12_231953.md:15750:   Repro: `reaction_date("2026-07-17", "dmh")` returns `2026-07-20`, skipping the Friday expiry for a Friday intraday report.
./codex_feedback/codex_feedback_2026-07-12_231953.md:15759:1. **High: `event_hour_yf` fallback is ignored.**  
./codex_feedback/codex_feedback_2026-07-12_231953.md:15761:   Repro: DB row `event_hour=NULL,event_hour_yf='bmo',event_date='2026-07-17'` -> preview uses `None`, so `reaction_date(..., None)` returns `2026-07-20` instead of `2026-07-17`.
./codex_feedback/codex_feedback_2026-07-12_231953.md:15765:   Repro: `reaction_date("2026-07-17", "dmh")` returns `2026-07-20`, skipping the Friday expiry for a Friday intraday report.
./codex_feedback/codex_feedback_2026-07-12_233107.md:30:Round 1 found 3 Highs, all now FIXED in the code below: (1) event_hour_yf fallback now coalesced in _row_to_reporter + added to _SELECT_COLS; (2) reaction_date now treats bmo AND dmh as same-day (only amc/blank defer); (3) build_preview_blocks now returns (blocks, n_included) and stops at the block cap without truncating mid-row, and run_consensus_preview marks ONLY posted_rows=rows[:n_included] + warns on overflow. ALSO changed this pass: FMP revenue-beat rewired to the working /stable/earnings endpoint (v3 was dead/403); fetch_fmp_estimate_range short-circuits to n/a (Starter-gated 402); the FMP gate is now bool(FMP_API_KEY) only.
./codex_feedback/codex_feedback_2026-07-12_233107.md:32:VERIFY the 3 fixes are correct and complete, and hunt for any NEW Critical/High correctness/safety bug introduced by these changes (esp. the tuple-return contract at all call sites, the rows[:n_included] marking, the /stable/earnings parsing/sort/cap, and any place still assuming the old single-return build_preview_blocks or the removed row.eps_range assignment). Concrete file:line + one-line repro. IGNORE style/whitespace/line-endings. If clean, say exactly: 'No Critical/High issues.'
./codex_feedback/codex_feedback_2026-07-12_233107.md:47:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_233107.md:59:+    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_233107.md:70:+    build_preview_blocks,
./codex_feedback/codex_feedback_2026-07-12_233107.md:94:+    revenue beat rate additionally uses FMP's /stable/earnings when FMP_API_KEY
./codex_feedback/codex_feedback_2026-07-12_233107.md:132:+    # Revenue beat rate comes from FMP /stable/earnings (verified available on
./codex_feedback/codex_feedback_2026-07-12_233107.md:159:+    blocks, n_included = build_preview_blocks(rows, as_of)
./codex_feedback/codex_feedback_2026-07-12_233107.md:179:+    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_233107.md:182:+            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_233107.md:185:+    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_233107.md:187:+    # (build_preview_blocks stops at the Slack block cap and returns how many
./codex_feedback/codex_feedback_2026-07-12_233107.md:190:+    posted_rows = rows[:n_included]
./codex_feedback/codex_feedback_2026-07-12_233107.md:191:+    if n_included < len(rows):
./codex_feedback/codex_feedback_2026-07-12_233107.md:192:+        overflow = rows[n_included:]
./codex_feedback/codex_feedback_2026-07-12_233107.md:196:+            n_included, len(rows), len(overflow),
./codex_feedback/codex_feedback_2026-07-12_233107.md:273:key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_233107.md:424:    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_233107.md:496:def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./codex_feedback/codex_feedback_2026-07-12_233107.md:569:    (ticker, event_date, event_hour, event_hour_yf, db_tier, company_name,
./codex_feedback/codex_feedback_2026-07-12_233107.md:573:    # (v11 event_hour_yf column) when Finnhub left timing blank — the same
./codex_feedback/codex_feedback_2026-07-12_233107.md:575:    # timing only lives in event_hour_yf renders TBD and gets a wrong (next-
./codex_feedback/codex_feedback_2026-07-12_233107.md:577:    effective_hour = event_hour or event_hour_yf
./codex_feedback/codex_feedback_2026-07-12_233107.md:593:    "ticker, event_date, event_hour, event_hour_yf, tier, company_name, "
./codex_feedback/codex_feedback_2026-07-12_233107.md:737:    rxn = reaction_date(event_date, event_hour)
./codex_feedback/codex_feedback_2026-07-12_233107.md:932:def fetch_fmp_estimate_range(
./codex_feedback/codex_feedback_2026-07-12_233107.md:950:def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_233107.md:955:    Endpoint: FMP /stable/earnings (fields revenueActual / revenueEstimated).
./codex_feedback/codex_feedback_2026-07-12_233107.md:958:    /stable/earnings -> HTTP 200, verified 2026-07-12). Returns n/a on any
./codex_feedback/codex_feedback_2026-07-12_233107.md:963:    # /stable/earnings returns the upcoming (actual=None) quarter plus history;
./codex_feedback/codex_feedback_2026-07-12_233107.md:967:        f"https://financialmodelingprep.com/stable/earnings"
./codex_feedback/codex_feedback_2026-07-12_233107.md:985:        actual = row.get("revenueActual")
./codex_feedback/codex_feedback_2026-07-12_233107.md:986:        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_233107.md:1157:    # Revenue beat rate: reachable via /stable/earnings whenever a key is set.
./codex_feedback/codex_feedback_2026-07-12_233107.md:1159:        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./codex_feedback/codex_feedback_2026-07-12_233107.md:1294:        f"{_render_estimate_range(row.eps_range)}"
./codex_feedback/codex_feedback_2026-07-12_233107.md:1340:def build_preview_blocks(
./codex_feedback/codex_feedback_2026-07-12_233107.md:1343:    """Build the Block Kit payload and return (blocks, n_included).
./codex_feedback/codex_feedback_2026-07-12_233107.md:1347:    never truncates mid-row. `n_included` (rows actually in the payload, in
./codex_feedback/codex_feedback_2026-07-12_233107.md:1353:    n_included = 0
./codex_feedback/codex_feedback_2026-07-12_233107.md:1375:        n_included += 1
./codex_feedback/codex_feedback_2026-07-12_233107.md:1378:        f"{len(rows)} name(s)" if n_included == len(rows)
./codex_feedback/codex_feedback_2026-07-12_233107.md:1379:        else f"{n_included} of {len(rows)} name(s)"
./codex_feedback/codex_feedback_2026-07-12_233107.md:1404:    return lead + body, n_included
./codex_feedback/codex_feedback_2026-07-12_233107.md:1429:                "count": row.eps_range.count,
./codex_feedback/codex_feedback_2026-07-12_233107.md:1430:                "low": row.eps_range.low,
./codex_feedback/codex_feedback_2026-07-12_233107.md:1431:                "high": row.eps_range.high,
./codex_feedback/codex_feedback_2026-07-12_233107.md:1645:  1622	    revenue beat rate additionally uses FMP's /stable/earnings when FMP_API_KEY
./codex_feedback/codex_feedback_2026-07-12_233107.md:1683:  1660	    # Revenue beat rate comes from FMP /stable/earnings (verified available on
./codex_feedback/codex_feedback_2026-07-12_233107.md:1710:  1687	    blocks, n_included = build_preview_blocks(rows, as_of)
./codex_feedback/codex_feedback_2026-07-12_233107.md:1730:  1707	    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_233107.md:1733:  1710	            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_233107.md:1736:  1713	    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_233107.md:1738:  1715	    # (build_preview_blocks stops at the Slack block cap and returns how many
./codex_feedback/codex_feedback_2026-07-12_233107.md:1741:  1718	    posted_rows = rows[:n_included]
./codex_feedback/codex_feedback_2026-07-12_233107.md:1742:  1719	    if n_included < len(rows):
./codex_feedback/codex_feedback_2026-07-12_233107.md:1743:  1720	        overflow = rows[n_included:]
./codex_feedback/codex_feedback_2026-07-12_233107.md:1747:  1724	            n_included, len(rows), len(overflow),
./codex_feedback/codex_feedback_2026-07-12_233107.md:1791:    19	key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_233107.md:1942:   170	    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_231227.md:269:-- **yfinance hour fallback via `Ticker.info`.** When Finnhub's `hour` is empty for an upcoming Tier 1/2 event, infer from yfinance's `earningsTimestamp`/`Start`/`End` (Unix-seconds, time-of-day preserved). Reads via `Ticker.info`, NOT `Ticker.calendar` or `get_earnings_dates()` — those wrappers strip time-of-day. Market-session boundaries: `< 09:30 ET → bmo`, `>= 16:00 ET → amc`, mid-session → None (catches 8:30 ET pre-open releases the original `<9 / >16` heuristic missed). Hour stored separately in `event_hour_yf` (v11 column) so `event_hour` and `date_confirmed` keep Finnhub-canonical semantics.
./codex_feedback/codex_feedback_2026-07-12_231227.md:326:-- **Schema is at v12.** `storage.py CURRENT_SCHEMA_VERSION=12`. Migrations are non-destructive. Fresh-DB `CREATE TABLE` duplicates v2–v12 columns; when adding a column, update both the migration and the fresh-DB statement. The `test_fresh_db_schema_matches_migration_path` test in `test_dedup.py` enforces this invariant. Column history: v3=`unseen_run_count`, v4=`date_locked`, v5=`last_xcheck_yf_dates`, v6=`date_confirmed`, v7=backfill for v6, v8=`announcement_url`, v9=`slack_thread_ts` + `slack_question_kind` + `slack_last_reply_ts` + `question_state` + `question_snooze_until` + `question_first_seen` + `kv_store` table, v10=`slack_channel_id`, v11=`event_hour_yf` (yfinance-inferred hour), v12=`call_datetime_utc`+`call_source` (conference-call timestamp).
./codex_feedback/codex_feedback_2026-07-12_231227.md:459:+- **yfinance hour fallback via `Ticker.info`.** When Finnhub's `hour` is empty for an upcoming Tier 1/2 event, infer from yfinance's `earningsTimestamp`/`Start`/`End` (Unix-seconds, time-of-day preserved). Reads via `Ticker.info`, NOT `Ticker.calendar` or `get_earnings_dates()` — those wrappers strip time-of-day. Market-session boundaries: `< 09:30 ET → bmo`, `>= 16:00 ET → amc`, mid-session → None (catches 8:30 ET pre-open releases the original `<9 / >16` heuristic missed). Hour stored separately in `event_hour_yf` (v11 column) so `event_hour` and `date_confirmed` keep Finnhub-canonical semantics.
./codex_feedback/codex_feedback_2026-07-12_231227.md:516:+- **Schema is at v12.** `storage.py CURRENT_SCHEMA_VERSION=12`. Migrations are non-destructive. Fresh-DB `CREATE TABLE` duplicates v2–v12 columns; when adding a column, update both the migration and the fresh-DB statement. The `test_fresh_db_schema_matches_migration_path` test in `test_dedup.py` enforces this invariant. Column history: v3=`unseen_run_count`, v4=`date_locked`, v5=`last_xcheck_yf_dates`, v6=`date_confirmed`, v7=backfill for v6, v8=`announcement_url`, v9=`slack_thread_ts` + `slack_question_kind` + `slack_last_reply_ts` + `question_state` + `question_snooze_until` + `question_first_seen` + `kv_store` table, v10=`slack_channel_id`, v11=`event_hour_yf` (yfinance-inferred hour), v12=`call_datetime_utc`+`call_source` (conference-call timestamp).
./codex_feedback/codex_feedback_2026-07-12_231227.md:4642:-storage.py:424:        "date_locked, event_hour_yf "
./codex_feedback/codex_feedback_2026-07-12_231227.md:4708:+storage.py:424:        "date_locked, event_hour_yf "
./codex_feedback/codex_feedback_2026-07-12_231227.md:4894:-        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:5552:+        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6102:-    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6113:-    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231227.md:6119:-    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231227.md:6140:-                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231227.md:6157:-            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6186:-            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6194:-            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6275:-360-        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6325:+    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6336:+    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231227.md:6342:+    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231227.md:6363:+                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231227.md:6380:+            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6409:+            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6417:+            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6498:+360-        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6776:-  3253	        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:6807:+  3253	        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:7027:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:7134:+key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_231227.md:7285:+    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_231227.md:7357:+def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./codex_feedback/codex_feedback_2026-07-12_231227.md:7588:+    rxn = reaction_date(event_date, event_hour)
./codex_feedback/codex_feedback_2026-07-12_231227.md:7783:+def fetch_fmp_estimate_range(
./codex_feedback/codex_feedback_2026-07-12_231227.md:7829:+def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_231227.md:7855:+        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_231227.md:8017:+        row.eps_range = fetch_fmp_estimate_range(rep.ticker, rep.event_date, fmp_key)
./codex_feedback/codex_feedback_2026-07-12_231227.md:8018:+        if not row.eps_range.ok:
./codex_feedback/codex_feedback_2026-07-12_231227.md:8020:+        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./codex_feedback/codex_feedback_2026-07-12_231227.md:8156:+        f"{_render_estimate_range(row.eps_range)}"
./codex_feedback/codex_feedback_2026-07-12_231227.md:8202:+def build_preview_blocks(rows: list[PreviewRow], as_of: date) -> list[dict]:
./codex_feedback/codex_feedback_2026-07-12_231227.md:8273:+                "count": row.eps_range.count,
./codex_feedback/codex_feedback_2026-07-12_231227.md:8274:+                "low": row.eps_range.low,
./codex_feedback/codex_feedback_2026-07-12_231227.md:8275:+                "high": row.eps_range.high,
./codex_feedback/codex_feedback_2026-07-12_231227.md:12127:-            f"https://financialmodelingprep.com/stable/earnings-calendar"
./codex_feedback/codex_feedback_2026-07-12_231227.md:12178:-    fh = index_rows(fh_rows, "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:12179:-    fmp = index_rows(fmp_rows, "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:12287:-                       "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:12289:-                        "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:12367:+            f"https://financialmodelingprep.com/stable/earnings-calendar"
./codex_feedback/codex_feedback_2026-07-12_231227.md:12418:+    fh = index_rows(fh_rows, "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:12419:+    fmp = index_rows(fmp_rows, "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:12527:+                       "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:12529:+                        "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:13437:-    # rendering uses (event_hour or event_hour_yf) for the time block.
./codex_feedback/codex_feedback_2026-07-12_231227.md:13439:-        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
./codex_feedback/codex_feedback_2026-07-12_231227.md:13705:-                event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:13745:-        "event_hour_yf, call_datetime_utc, call_source "
./codex_feedback/codex_feedback_2026-07-12_231227.md:13760:-            "event_hour_yf": row[16],
./codex_feedback/codex_feedback_2026-07-12_231227.md:13820:-        "date_locked, event_hour_yf "
./codex_feedback/codex_feedback_2026-07-12_231227.md:13835:-            "event_hour_yf": row[13],
./codex_feedback/codex_feedback_2026-07-12_231227.md:13856:-    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231227.md:13867:-    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231227.md:13873:-    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231227.md:13894:-                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231227.md:13911:-            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231227.md:13940:-            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:13948:-            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:14248:+    # rendering uses (event_hour or event_hour_yf) for the time block.
./codex_feedback/codex_feedback_2026-07-12_231227.md:14250:+        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
./codex_feedback/codex_feedback_2026-07-12_231227.md:14516:+                event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:14556:+        "event_hour_yf, call_datetime_utc, call_source "
./codex_feedback/codex_feedback_2026-07-12_231227.md:14571:+            "event_hour_yf": row[16],
./codex_feedback/codex_feedback_2026-07-12_231227.md:14631:+        "date_locked, event_hour_yf "
./codex_feedback/codex_feedback_2026-07-12_231227.md:14646:+            "event_hour_yf": row[13],
./codex_feedback/codex_feedback_2026-07-12_231227.md:14667:+    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231227.md:14678:+    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231227.md:14684:+    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231227.md:14705:+                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231227.md:14722:+            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231227.md:14751:+            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:14759:+            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:15106:-            event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:15917:-            "revenueActual": kw.get("revenueActual")}
./codex_feedback/codex_feedback_2026-07-12_231227.md:15925:-            "revenueActual": kw.get("revenueActual"), "source": "fmp"}
./codex_feedback/codex_feedback_2026-07-12_231227.md:15934:-                revenueActual=1.285e9)]
./codex_feedback/codex_feedback_2026-07-12_231227.md:16009:-    fmp = [_fmp("XYZ", "2026-05-10", epsActual=1.1, revenueActual=5e8)]
./codex_feedback/codex_feedback_2026-07-12_231227.md:16012:-    assert m[0]["revenueActual"] == 5e8          # filled from FMP
./codex_feedback/codex_feedback_2026-07-12_231227.md:16939:+            event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:17750:+            "revenueActual": kw.get("revenueActual")}
./codex_feedback/codex_feedback_2026-07-12_231227.md:17758:+            "revenueActual": kw.get("revenueActual"), "source": "fmp"}
./codex_feedback/codex_feedback_2026-07-12_231227.md:17767:+                revenueActual=1.285e9)]
./codex_feedback/codex_feedback_2026-07-12_231227.md:17842:+    fmp = [_fmp("XYZ", "2026-05-10", epsActual=1.1, revenueActual=5e8)]
./codex_feedback/codex_feedback_2026-07-12_231227.md:17845:+    assert m[0]["revenueActual"] == 5e8          # filled from FMP
./codex_feedback/codex_feedback_2026-07-12_231227.md:19207:-            "revenueEstimate": rev_est, "revenueActual": rev_act}
./codex_feedback/codex_feedback_2026-07-12_231227.md:19404:-         "revenueEstimate": 1e9, "revenueActual": 1.1e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:19453:-         "revenueEstimate": 1.2e9, "revenueActual": 1.285e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:19496:-         "revenueEstimate": 1.2e9, "revenueActual": 1.285e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:19702:+            "revenueEstimate": rev_est, "revenueActual": rev_act}
./codex_feedback/codex_feedback_2026-07-12_231227.md:19899:+         "revenueEstimate": 1e9, "revenueActual": 1.1e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:19948:+         "revenueEstimate": 1.2e9, "revenueActual": 1.285e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:19991:+         "revenueEstimate": 1.2e9, "revenueActual": 1.285e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:22754:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:22782:+key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_231227.md:22933:+    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_231227.md:23005:+def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./codex_feedback/codex_feedback_2026-07-12_231227.md:23236:+    rxn = reaction_date(event_date, event_hour)
./codex_feedback/codex_feedback_2026-07-12_231227.md:23431:+def fetch_fmp_estimate_range(
./codex_feedback/codex_feedback_2026-07-12_231227.md:23477:+def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_231227.md:23503:+        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_231227.md:23665:+        row.eps_range = fetch_fmp_estimate_range(rep.ticker, rep.event_date, fmp_key)
./codex_feedback/codex_feedback_2026-07-12_231227.md:23666:+        if not row.eps_range.ok:
./codex_feedback/codex_feedback_2026-07-12_231227.md:23668:+        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./codex_feedback/codex_feedback_2026-07-12_231227.md:23804:+        f"{_render_estimate_range(row.eps_range)}"
./codex_feedback/codex_feedback_2026-07-12_231227.md:23850:+def build_preview_blocks(rows: list[PreviewRow], as_of: date) -> list[dict]:
./codex_feedback/codex_feedback_2026-07-12_231227.md:23921:+                "count": row.eps_range.count,
./codex_feedback/codex_feedback_2026-07-12_231227.md:23922:+                "low": row.eps_range.low,
./codex_feedback/codex_feedback_2026-07-12_231227.md:23923:+                "high": row.eps_range.high,
./codex_feedback/codex_feedback_2026-07-12_231227.md:23992:+    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:24003:+    build_preview_blocks,
./codex_feedback/codex_feedback_2026-07-12_231227.md:24029:+    `fmp_preview_endpoints_ok` == "true"; otherwise they render n/a.
./codex_feedback/codex_feedback_2026-07-12_231227.md:24066:+    fmp_ok = bool(FMP_API_KEY) and (kv_get(conn, "fmp_preview_endpoints_ok") == "true")
./codex_feedback/codex_feedback_2026-07-12_231227.md:24090:+    blocks = build_preview_blocks(rows, as_of)
./codex_feedback/codex_feedback_2026-07-12_231227.md:24110:+    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_231227.md:24113:+            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_231227.md:24116:+    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_231227.md:24197:    24	    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:24263:    90	    build_preview_blocks,
./codex_feedback/codex_feedback_2026-07-12_231227.md:24314:    19	key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./codex_feedback/codex_feedback_2026-07-12_231227.md:24465:   170	    eps_range: EstimateRange = field(
./codex_feedback/codex_feedback_2026-07-12_231227.md:24537:   242	def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./codex_feedback/codex_feedback_2026-07-12_231227.md:24594:  1624	    `fmp_preview_endpoints_ok` == "true"; otherwise they render n/a.
./codex_feedback/codex_feedback_2026-07-12_231227.md:24632:  1661	    fmp_ok = bool(FMP_API_KEY) and (kv_get(conn, "fmp_preview_endpoints_ok") == "true")
./codex_feedback/codex_feedback_2026-07-12_231227.md:24656:  1685	    blocks = build_preview_blocks(rows, as_of)
./codex_feedback/codex_feedback_2026-07-12_231227.md:24676:  1705	    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_231227.md:24679:  1708	            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_231227.md:24682:  1711	    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_231227.md:25005:   473	    rxn = reaction_date(event_date, event_hour)
./codex_feedback/codex_feedback_2026-07-12_231227.md:25203:   668	def fetch_fmp_estimate_range(
./codex_feedback/codex_feedback_2026-07-12_231227.md:25249:   714	def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_231227.md:25275:   740	        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_231227.md:25437:   902	        row.eps_range = fetch_fmp_estimate_range(rep.ticker, rep.event_date, fmp_key)
./codex_feedback/codex_feedback_2026-07-12_231227.md:25438:   903	        if not row.eps_range.ok:
./codex_feedback/codex_feedback_2026-07-12_231227.md:25440:   905	        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./codex_feedback/codex_feedback_2026-07-12_231227.md:25579:  1041	        f"{_render_estimate_range(row.eps_range)}"
./codex_feedback/codex_feedback_2026-07-12_231227.md:25625:  1087	def build_preview_blocks(rows: list[PreviewRow], as_of: date) -> list[dict]:
./codex_feedback/codex_feedback_2026-07-12_231227.md:25696:  1158	                "count": row.eps_range.count,
./codex_feedback/codex_feedback_2026-07-12_231227.md:25697:  1159	                "low": row.eps_range.low,
./codex_feedback/codex_feedback_2026-07-12_231227.md:25698:  1160	                "high": row.eps_range.high,
./codex_feedback/codex_feedback_2026-07-12_231227.md:25763:   714	def fetch_fmp_rev_beat_history(
./codex_feedback/codex_feedback_2026-07-12_231227.md:25789:   740	        est = row.get("revenueEstimated")
./codex_feedback/codex_feedback_2026-07-12_231227.md:25813:storage.py:349:        "event_hour_yf, call_datetime_utc, call_source "
./codex_feedback/codex_feedback_2026-07-12_231227.md:25829:storage.py:515:            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231227.md:26047:    41	    # rendering uses (event_hour or event_hour_yf) for the time block.
./codex_feedback/codex_feedback_2026-07-12_231227.md:26049:    43	        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
./codex_feedback/codex_feedback_2026-07-12_231227.md:26323:   309	                event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:27111:+- **yfinance hour fallback via `Ticker.info`.** When Finnhub's `hour` is empty for an upcoming Tier 1/2 event, infer from yfinance's `earningsTimestamp`/`Start`/`End` (Unix-seconds, time-of-day preserved). Reads via `Ticker.info`, NOT `Ticker.calendar` or `get_earnings_dates()` — those wrappers strip time-of-day. Market-session boundaries: `< 09:30 ET → bmo`, `>= 16:00 ET → amc`, mid-session → None (catches 8:30 ET pre-open releases the original `<9 / >16` heuristic missed). Hour stored separately in `event_hour_yf` (v11 column) so `event_hour` and `date_confirmed` keep Finnhub-canonical semantics.
./codex_feedback/codex_feedback_2026-07-12_231227.md:27225:+- **Schema is at v12.** `storage.py CURRENT_SCHEMA_VERSION=12`. Migrations are non-destructive. Fresh-DB `CREATE TABLE` duplicates v2–v12 columns; when adding a column, update both the migration and the fresh-DB statement. The `test_fresh_db_schema_matches_migration_path` test in `test_dedup.py` enforces this invariant. Column history: v3=`unseen_run_count`, v4=`date_locked`, v5=`last_xcheck_yf_dates`, v6=`date_confirmed`, v7=backfill for v6, v8=`announcement_url`, v9=`slack_thread_ts` + `slack_question_kind` + `slack_last_reply_ts` + `question_state` + `question_snooze_until` + `question_first_seen` + `kv_store` table, v10=`slack_channel_id`, v11=`event_hour_yf` (yfinance-inferred hour), v12=`call_datetime_utc`+`call_source` (conference-call timestamp).
./codex_feedback/codex_feedback_2026-07-12_231227.md:31403:+storage.py:424:        "date_locked, event_hour_yf "
./codex_feedback/codex_feedback_2026-07-12_231227.md:31767:+        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:32859:+    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231227.md:32881:+    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231227.md:32893:+    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231227.md:32935:+                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231227.md:32969:+            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231227.md:33027:+            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:33043:+            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:33205:+360-        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:33519:+  3253	        event_hour_yf=new_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:33665:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:36807:+revenueEstimate, revenueActual (+ a 'source' tag for observability).
./codex_feedback/codex_feedback_2026-07-12_231227.md:36859:+_FMP_BASE = "https://financialmodelingprep.com/stable/earnings-calendar"
./codex_feedback/codex_feedback_2026-07-12_231227.md:36893:+        "revenueEstimate": row.get("revenueEstimated"),
./codex_feedback/codex_feedback_2026-07-12_231227.md:36895:+        "revenueActual": row.get("revenueActual"),
./codex_feedback/codex_feedback_2026-07-12_231227.md:37043:+    return ev.get("epsActual") is not None or ev.get("revenueActual") is not None
./codex_feedback/codex_feedback_2026-07-12_231227.md:37073:+    for k in ("epsEstimate", "epsActual", "revenueEstimate", "revenueActual"):
./codex_feedback/codex_feedback_2026-07-12_231227.md:39364:+            f"https://financialmodelingprep.com/stable/earnings-calendar"
./codex_feedback/codex_feedback_2026-07-12_231227.md:39466:+    fh = index_rows(fh_rows, "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:39468:+    fmp = index_rows(fmp_rows, "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:39684:+                       "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:39688:+                        "symbol", "date", "epsActual", "revenueActual", universe)
./codex_feedback/codex_feedback_2026-07-12_231227.md:40630:+    # rendering uses (event_hour or event_hour_yf) for the time block.
./codex_feedback/codex_feedback_2026-07-12_231227.md:40634:+        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
./codex_feedback/codex_feedback_2026-07-12_231227.md:41166:+                event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:41246:+        "event_hour_yf, call_datetime_utc, call_source "
./codex_feedback/codex_feedback_2026-07-12_231227.md:41276:+            "event_hour_yf": row[16],
./codex_feedback/codex_feedback_2026-07-12_231227.md:41396:+        "date_locked, event_hour_yf "
./codex_feedback/codex_feedback_2026-07-12_231227.md:41426:+            "event_hour_yf": row[13],
./codex_feedback/codex_feedback_2026-07-12_231227.md:41468:+    event_hour_yf: str | None = None,
./codex_feedback/codex_feedback_2026-07-12_231227.md:41490:+    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./codex_feedback/codex_feedback_2026-07-12_231227.md:41502:+    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./codex_feedback/codex_feedback_2026-07-12_231227.md:41544:+                event_hour_yf     = COALESCE(?, event_hour_yf),
./codex_feedback/codex_feedback_2026-07-12_231227.md:41578:+            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./codex_feedback/codex_feedback_2026-07-12_231227.md:41636:+            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:41652:+            (ticker, event_date, event_hour, event_hour_yf,
./codex_feedback/codex_feedback_2026-07-12_231227.md:42336:+            event_hour_yf   TEXT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:43958:+            "revenueActual": kw.get("revenueActual")}
./codex_feedback/codex_feedback_2026-07-12_231227.md:43974:+            "revenueActual": kw.get("revenueActual"), "source": "fmp"}
./codex_feedback/codex_feedback_2026-07-12_231227.md:43992:+                revenueActual=1.285e9)]
./codex_feedback/codex_feedback_2026-07-12_231227.md:44142:+    fmp = [_fmp("XYZ", "2026-05-10", epsActual=1.1, revenueActual=5e8)]
./codex_feedback/codex_feedback_2026-07-12_231227.md:44148:+    assert m[0]["revenueActual"] == 5e8          # filled from FMP
./codex_feedback/codex_feedback_2026-07-12_231227.md:46470:+            "revenueEstimate": rev_est, "revenueActual": rev_act}
./codex_feedback/codex_feedback_2026-07-12_231227.md:46864:+         "revenueEstimate": 1e9, "revenueActual": 1.1e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:46962:+         "revenueEstimate": 1.2e9, "revenueActual": 1.285e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:47048:+         "revenueEstimate": 1.2e9, "revenueActual": 1.285e9},
./codex_feedback/codex_feedback_2026-07-12_231227.md:50020:    61	SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:50063:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:50233:./codex_feedback/codex_feedback_2026-07-12_231227.md:7027:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:50250:./codex_feedback/codex_feedback_2026-07-12_231227.md:22754:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:50252:./codex_feedback/codex_feedback_2026-07-12_231227.md:23992:+    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:50253:./codex_feedback/codex_feedback_2026-07-12_231227.md:24110:+    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_231227.md:50254:./codex_feedback/codex_feedback_2026-07-12_231227.md:24113:+            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_231227.md:50255:./codex_feedback/codex_feedback_2026-07-12_231227.md:24116:+    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_231227.md:50260:./codex_feedback/codex_feedback_2026-07-12_231227.md:24197:    24	    SLACK_WEBHOOK_STREET_ACCOUNT,
./codex_feedback/codex_feedback_2026-07-12_231227.md:50261:./codex_feedback/codex_feedback_2026-07-12_231227.md:24676:  1705	    if not SLACK_WEBHOOK_STREET_ACCOUNT:
./codex_feedback/codex_feedback_2026-07-12_231227.md:50262:./codex_feedback/codex_feedback_2026-07-12_231227.md:24679:  1708	            "SLACK_WEBHOOK_STREET_ACCOUNT not set — cannot post consensus preview"
./codex_feedback/codex_feedback_2026-07-12_231227.md:50263:./codex_feedback/codex_feedback_2026-07-12_231227.md:24682:  1711	    post_slack(SLACK_WEBHOOK_STREET_ACCOUNT, blocks, fallback)
./codex_feedback/codex_feedback_2026-07-12_231227.md:50272:./codex_feedback/codex_feedback_2026-07-12_231227.md:33665:+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:50282:./codex_feedback/codex_feedback_2026-07-12_231227.md:50020:    61	SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:50285:./config.py:61:SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
./codex_feedback/codex_feedback_2026-07-12_231227.md:50287:./main.py:24:    SLACK_WEBHOOK_STREET_ACCOUNT,

codex
The initial search shows the tuple-return is only referenced in the new preview path in live code, but old references exist inside archived feedback files. I’m narrowing the scan to source/test files now and checking the FMP and block-cap behavior directly.
exec
/bin/bash -lc "nl -ba consensus_preview.py | sed -n '668,745p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
   668	
   669	
   670	def _fmp_get_json(url: str, timeout: int = 20):
   671	    import json
   672	    import urllib.request
   673	
   674	    with urllib.request.urlopen(url, timeout=timeout) as resp:  # noqa: S310
   675	        return json.load(resp)
   676	
   677	
   678	def fetch_fmp_estimate_range(
   679	    ticker: str, event_date: str, api_key: str
   680	) -> EstimateRange:
   681	    """Analyst estimate count + low/high EPS range for the reporting quarter.
   682	
   683	    UNAVAILABLE on the current FMP Starter plan and therefore always n/a:
   684	    - /stable/analyst-estimates with period=quarter -> HTTP 402 ("this value
   685	      set for 'period' is not available under your current subscription")
   686	    - the v3 /api/v3/analyst-estimates endpoint is retired -> HTTP 403
   687	      ("Legacy Endpoint")
   688	    (Both verified 2026-07-12.) We short-circuit rather than fire a request
   689	    that can only burn a gated 402/403; the caller renders the honest
   690	    "not available on FMP Starter plan" reason. Kept as a documented seam so a
   691	    future plan upgrade can restore quarterly estimate ranges here.
   692	    """
   693	    return EstimateRange(None, None, None)
   694	
   695	
   696	def fetch_fmp_rev_beat_history(
   697	    ticker: str, api_key: str, max_q: int = 20
   698	) -> BeatRate:
   699	    """Revenue beat rate over up to `max_q` most-recent COMPLETED quarters.
   700	
   701	    Endpoint: FMP /stable/earnings (fields revenueActual / revenueEstimated).
   702	    This is the Starter-plan-available successor to the retired v3
   703	    /historical/earning_calendar endpoint (v3 -> HTTP 403 "Legacy Endpoint";
   704	    /stable/earnings -> HTTP 200, verified 2026-07-12). Returns n/a on any
   705	    error or unexpected shape — never a partial/fabricated number.
   706	    """
   707	    if not api_key:
   708	        return BeatRate(None, None, "fmp")
   709	    # /stable/earnings returns the upcoming (actual=None) quarter plus history;
   710	    # over-request so `max_q` COMPLETED quarters survive the null-actual skip.
   711	    limit = max(max_q * 2, 40)
   712	    url = (
   713	        f"https://financialmodelingprep.com/stable/earnings"
   714	        f"?symbol={ticker}&limit={limit}&apikey={api_key}"
   715	    )
   716	    try:
   717	        data = _fmp_get_json(url)
   718	    except Exception as exc:  # noqa: BLE001
   719	        logger.debug(f"FMP revenue-beat fetch failed for {ticker}: {exc}")
   720	        return BeatRate(None, None, "fmp")
   721	    if not isinstance(data, list) or not data:
   722	        return BeatRate(None, None, "fmp")
   723	
   724	    # Newest-first so the `max_q` cap keeps the MOST RECENT completed quarters,
   725	    # regardless of the API's return order.
   726	    data = sorted(data, key=lambda r: str(r.get("date", "")), reverse=True)
   727	
   728	    beats = 0
   729	    total = 0
   730	    for row in data:
   731	        actual = row.get("revenueActual")
   732	        est = row.get("revenueEstimated")
   733	        if actual is None or est is None:
   734	            continue
   735	        try:
   736	            actual_f, est_f = float(actual), float(est)
   737	        except (TypeError, ValueError):
   738	            continue
   739	        if est_f == 0:
   740	            continue
   741	        total += 1
   742	        if actual_f > est_f:
   743	            beats += 1
   744	        if total >= max_q:
   745	            break

exec
/bin/bash -lc "nl -ba consensus_preview.py | sed -n '242,355p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
   242	def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
   243	    """The trading day whose close reflects the market's reaction.
   244	
   245	    bmo / dmh           -> the event date itself. A pre-open (bmo) OR during-
   246	                           market-hours (dmh) release reacts the SAME session.
   247	    amc / blank         -> the next weekday after the event date (post-close or
   248	                           unknown-timing release reacts the following session;
   249	                           unknown is treated as amc so the chosen expiry can
   250	                           never fall BEFORE an actual post-close reaction).
   251	                           Weekend-skip only (no holiday calendar).
   252	    """
   253	    ed = _parse_iso_date(event_date)
   254	    if ed is None:
   255	        return None
   256	    # Mirror market_data.fetch_post_earnings_move: only amc defers to the next
   257	    # session; bmo and dmh both react same-day.
   258	    if (event_hour or "").lower() in ("bmo", "dmh"):
   259	        return ed
   260	    d = ed + timedelta(days=1)
   261	    while d.weekday() >= 5:  # Sat=5, Sun=6
   262	        d += timedelta(days=1)
   263	    return d
   264	
   265	
   266	def _last_close(ticker: str) -> Optional[float]:
   267	    """Most recent daily close from a 5d history (NOT intraday fast_info)."""
   268	    try:
   269	        with _silence():
   270	            hist = yf.Ticker(ticker).history(period="5d", auto_adjust=True)
   271	    except Exception as exc:  # noqa: BLE001
   272	        logger.debug(f"5d history failed for {ticker}: {exc}")
   273	        return None
   274	    if hist is None or hist.empty:
   275	        return None
   276	    closes = hist["Close"].dropna()
   277	    if closes.empty:
   278	        return None
   279	    return float(closes.iloc[-1])
   280	
   281	
   282	def _leg_mid(row) -> tuple[Optional[float], bool, Optional[float]]:
   283	    """Return (mid, is_stale, spread) for one option leg.
   284	
   285	    Rule (spec §2):
   286	      bid > 0 and ask >= bid  -> (bid + ask) / 2, not stale, spread = ask - bid
   287	      elif lastPrice > 0      -> lastPrice, STALE (no live two-sided quote),
   288	                                 spread unknown (None)
   289	      else                    -> (None, False, None)  -> leg unusable
   290	    """
   291	    def _f(key: str) -> float:
   292	        try:
   293	            v = row.get(key)
   294	        except AttributeError:  # pandas Series
   295	            v = row[key] if key in row else None
   296	        try:
   297	            return float(v) if v is not None else 0.0
   298	        except (TypeError, ValueError):
   299	            return 0.0
   300	
   301	    bid, ask, last = _f("bid"), _f("ask"), _f("lastPrice")
   302	    if bid > 0 and ask >= bid:
   303	        return (bid + ask) / 2.0, False, (ask - bid)
   304	    if last > 0:
   305	        return last, True, None
   306	    return None, False, None
   307	
   308	
   309	# ---------------------------------------------------------------------------
   310	# Reporter selection
   311	# ---------------------------------------------------------------------------
   312	
   313	
   314	def _row_to_reporter(raw: tuple, coverage_map: dict[str, TickerInfo]) -> Reporter:
   315	    (ticker, event_date, event_hour, event_hour_yf, db_tier, company_name,
   316	     eps_est, rev_est, call_dt) = raw
   317	    info = coverage_map.get(ticker)
   318	    # Finnhub-canonical hour first; fall back to the yfinance-inferred hour
   319	    # (v11 event_hour_yf column) when Finnhub left timing blank — the same
   320	    # fallback the rest of the pipeline uses. Without it a bmo/amc name whose
   321	    # timing only lives in event_hour_yf renders TBD and gets a wrong (next-
   322	    # session) reaction date for the implied-move expiry.
   323	    effective_hour = event_hour or event_hour_yf
   324	    return Reporter(
   325	        ticker=ticker,
   326	        company_name=company_name or (info.company_name if info else ""),
   327	        tier=(info.tier if info else (int(db_tier) if db_tier is not None else 99)),
   328	        event_date=event_date,
   329	        event_hour=effective_hour,
   330	        call_datetime_utc=call_dt,
   331	        eps_estimate=eps_est,
   332	        rev_estimate=rev_est,
   333	        sector=(info.sector if info else ""),
   334	        subsector=(info.subsector if info else ""),
   335	    )
   336	
   337	
   338	_SELECT_COLS = (
   339	    "ticker, event_date, event_hour, event_hour_yf, tier, company_name, "
   340	    "eps_estimate, rev_estimate, call_datetime_utc"
   341	)
   342	
   343	
   344	def select_upcoming_reporters(
   345	    conn: sqlite3.Connection,
   346	    coverage: list[TickerInfo],
   347	    *,
   348	    days_ahead: int = 3,
   349	    max_tier: int = 1,
   350	    ticker: Optional[str] = None,
   351	) -> list[Reporter]:
   352	    """Select upcoming, not-yet-reported reporters for previewing.
   353	
   354	    When `ticker` is given, returns that ticker's single nearest upcoming
   355	    unreported event regardless of tier or window (on-demand preview).

exec
/bin/bash -lc "nl -ba consensus_preview.py | sed -n '880,1160p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
   880	        row.post_moves = fetch_recent_post_moves(conn, rep.ticker, n=4)
   881	    except Exception as exc:  # noqa: BLE001
   882	        row.post_moves = []
   883	        logger.debug(f"post-move error {rep.ticker}: {exc}")
   884	    if not row.post_moves:
   885	        row.mark_na("post_earnings_moves", "no computable prior reactions")
   886	
   887	    # --- EPS beat rate ---
   888	    try:
   889	        row.eps_beat = fetch_eps_beat_history(rep.ticker, max_q=20)
   890	    except Exception as exc:  # noqa: BLE001
   891	        row.eps_beat = BeatRate(None, None, "yfinance")
   892	        logger.debug(f"eps beat error {rep.ticker}: {exc}")
   893	    if not row.eps_beat.ok:
   894	        row.mark_na("beat_rates.eps", "no yfinance surprise history")
   895	
   896	    # --- FMP B-fields (independently gated) ---
   897	    # Estimate count/range: permanently unavailable on the FMP Starter plan
   898	    # (quarterly analyst-estimates -> 402; verified 2026-07-12) -> always n/a.
   899	    row.mark_na(
   900	        "consensus.eps.range",
   901	        "estimate range not available on FMP Starter plan",
   902	    )
   903	    # Revenue beat rate: reachable via /stable/earnings whenever a key is set.
   904	    if fmp_ok and fmp_key:
   905	        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
   906	        if not row.rev_beat.ok:
   907	            row.mark_na("beat_rates.revenue", "FMP revenue-beat history unavailable")
   908	    else:
   909	        row.mark_na("beat_rates.revenue", "no FMP_API_KEY — revenue beat n/a")
   910	
   911	    return row
   912	
   913	
   914	# ---------------------------------------------------------------------------
   915	# Rendering helpers
   916	# ---------------------------------------------------------------------------
   917	
   918	
   919	def _fmt_short_date(iso: Optional[str]) -> str:
   920	    d = _parse_iso_date(iso or "")
   921	    if d is None:
   922	        return "n/a"
   923	    return f"{d.strftime('%a %b')} {d.day}"
   924	
   925	
   926	def _fmt_expiry(iso: Optional[str]) -> str:
   927	    d = _parse_iso_date(iso or "")
   928	    if d is None:
   929	        return "n/a"
   930	    return f"{d.strftime('%b')} {d.day}"
   931	
   932	
   933	def _timing_badge(hour: Optional[str]) -> str:
   934	    if not hour:
   935	        return "TBD"
   936	    h = hour.lower()
   937	    return {"bmo": "BMO", "amc": "AMC", "dmh": "DMH"}.get(h, hour.upper())
   938	
   939	
   940	def _fmt_pct(v: Optional[float], *, signed: bool = True, na: str = "n/a") -> str:
   941	    if v is None:
   942	        return na
   943	    if signed:
   944	        return f"{v:+.1f}%"
   945	    return f"{v:.1f}%"
   946	
   947	
   948	def _fmt_price(v: Optional[float]) -> str:
   949	    if v is None:
   950	        return "n/a"
   951	    return f"${v:,.2f}"
   952	
   953	
   954	def _fmt_eps(v: Optional[float]) -> str:
   955	    if v is None:
   956	        return "n/a"
   957	    return f"${v:.2f}"
   958	
   959	
   960	def _fmt_rev(v: Optional[float]) -> str:
   961	    if v is None:
   962	        return "n/a"
   963	    if v >= 1e9:
   964	        return f"${v / 1e9:.2f}B"
   965	    if v >= 1e6:
   966	        return f"${v / 1e6:.1f}M"
   967	    return f"${v:,.0f}"
   968	
   969	
   970	def _fmt_call_et(call_iso: Optional[str], event_date: Optional[str]) -> str:
   971	    """Conference-call time in ET, noting same-day vs next-day."""
   972	    if not call_iso:
   973	        return "n/a"
   974	    try:
   975	        raw = call_iso.replace("Z", "+00:00")
   976	        dt = datetime.fromisoformat(raw)
   977	        if dt.tzinfo is None:
   978	            dt = dt.replace(tzinfo=timezone.utc)
   979	    except (ValueError, TypeError):
   980	        return "n/a"
   981	    if ZoneInfo is None:
   982	        return dt.strftime("%b %d %H:%M UTC")
   983	    try:
   984	        et = dt.astimezone(ZoneInfo("America/New_York"))
   985	    except Exception:  # noqa: BLE001
   986	        return dt.strftime("%b %d %H:%M UTC")
   987	    hour12 = et.strftime("%I:%M %p").lstrip("0")
   988	    ev = _parse_iso_date(event_date or "")
   989	    if ev is not None and et.date() == ev:
   990	        return f"{hour12} ET (same day)"
   991	    return f"{et.strftime('%a %b')} {et.day} {hour12} ET"
   992	
   993	
   994	def _render_implied_move(im: ImpliedMove) -> str:
   995	    if not im.ok:
   996	        reason = im.notes[0] if im.notes else "n/a"
   997	        return f"Options-implied move: {reason}"
   998	    line = f"Options imply ~{im.pct:.1f}% move ({_fmt_expiry(im.expiry)} exp straddle)"
   999	    annotations = [n for n in im.notes if n]
  1000	    if annotations:
  1001	        line += " — " + ", ".join(annotations)
  1002	    return line
  1003	
  1004	
  1005	def _render_post_moves(moves: list[QuarterMove]) -> str:
  1006	    if not moves:
  1007	        return "Last post-earnings moves: n/a (no computable prior reactions)"
  1008	    parts = [f"{_fmt_expiry(m.event_date)} {m.move_pct:+.1f}%" for m in moves]
  1009	    return f"Last {len(moves)} post-earnings moves: " + ", ".join(parts)
  1010	
  1011	
  1012	def _render_beat_rate(br: BeatRate, label: str) -> str:
  1013	    if not br.ok:
  1014	        return f"{label}: n/a (FMP endpoint unavailable)" if br.source == "fmp" \
  1015	            else f"{label}: n/a"
  1016	    return f"{label}: {br.beats}/{br.total} ({br.source})"
  1017	
  1018	
  1019	def _render_estimate_range(er: EstimateRange) -> str:
  1020	    if not er.ok:
  1021	        return "est. range n/a (FMP endpoint unavailable)"
  1022	    lo = _fmt_eps(er.low)
  1023	    hi = _fmt_eps(er.high)
  1024	    return f"est. from {er.count} analysts ({lo}-{hi})"
  1025	
  1026	
  1027	def _render_row_lines(row: PreviewRow) -> list[str]:
  1028	    """The full set of spec §-scope lines for one reporter (fallback text)."""
  1029	    badge = _timing_badge(row.event_hour)
  1030	    name = row.company_name or row.ticker
  1031	    day = f" ({_fmt_pct(row.day_change_pct)} day)" if row.day_change_pct is not None else ""
  1032	
  1033	    header = (
  1034	        f"`{row.ticker}` {name} — {_fmt_short_date(row.event_date)} · {badge} · "
  1035	        f"{_fmt_price(row.last_price)}{day}"
  1036	    )
  1037	
  1038	    consensus = (
  1039	        f"Consensus: EPS {_fmt_eps(row.eps_mean)} · Rev {_fmt_rev(row.rev_mean)} · "
  1040	        f"{_render_estimate_range(row.eps_range)}"
  1041	    )
  1042	
  1043	    qtd_bits = [f"{row.ticker} {_fmt_pct(row.qtd_ticker_pct)}",
  1044	                f"S&P 500 (SPY) {_fmt_pct(row.qtd_spx_pct)}"]
  1045	    if row.sector_etf:
  1046	        qtd_bits.append(
  1047	            f"{row.sector_etf_label} ({row.sector_etf}) {_fmt_pct(row.sector_etf_pct)}"
  1048	        )
  1049	    else:
  1050	        qtd_bits.append("sector ETF n/a (no mapped ETF)")
  1051	    qtd = f"QTD since {_fmt_short_date(row.qtd_since)}: " + " · ".join(qtd_bits)
  1052	
  1053	    implied = _render_implied_move(row.implied_move)
  1054	    post = _render_post_moves(row.post_moves)
  1055	    beats = (
  1056	        f"{_render_beat_rate(row.eps_beat, 'EPS beat rate')} · "
  1057	        f"{_render_beat_rate(row.rev_beat, 'Rev beat rate')}"
  1058	    )
  1059	    call = f"Conference call: {_fmt_call_et(row.call_datetime_utc, row.event_date)}"
  1060	
  1061	    return [header, consensus, qtd, implied, post, beats, call]
  1062	
  1063	
  1064	# ---------------------------------------------------------------------------
  1065	# Slack builders
  1066	# ---------------------------------------------------------------------------
  1067	
  1068	
  1069	SLACK_MAX_BLOCKS = 48
  1070	
  1071	
  1072	def build_preview_fallback(rows: list[PreviewRow], as_of: date) -> str:
  1073	    lines = [
  1074	        f"Consensus previews — {len(rows)} name(s) (as of {_fmt_short_date(as_of.isoformat())})",
  1075	        "",
  1076	    ]
  1077	    if not rows:
  1078	        lines.append("(no upcoming reporters in window)")
  1079	        return "\n".join(lines)
  1080	    for row in rows:
  1081	        lines.extend(_render_row_lines(row))
  1082	        lines.append("")
  1083	    return "\n".join(lines).rstrip()
  1084	
  1085	
  1086	def build_preview_blocks(
  1087	    rows: list[PreviewRow], as_of: date
  1088	) -> tuple[list[dict], int]:
  1089	    """Build the Block Kit payload and return (blocks, n_included).
  1090	
  1091	    Rows are added WHOLE (section + optional degraded-context) until the next
  1092	    row would push the payload past SLACK_MAX_BLOCKS; the loop then stops. It
  1093	    never truncates mid-row. `n_included` (rows actually in the payload, in
  1094	    order) lets the caller mark ONLY posted rows for dedup and warn about the
  1095	    overflow — instead of the old truncate-then-mark-all, which silently
  1096	    dropped overflow rows yet recorded them as posted.
  1097	    """
  1098	    body: list[dict] = []
  1099	    n_included = 0
  1100	    # Reserve the 3 lead blocks (header + context + divider).
  1101	    body_budget = SLACK_MAX_BLOCKS - 3
  1102	    for row in rows:
  1103	        header, *rest = _render_row_lines(row)
  1104	        row_blocks: list[dict] = [{
  1105	            "type": "section",
  1106	            "text": {"type": "mrkdwn", "text": f"{header}\n{chr(10).join(rest)}"[:2900]},
  1107	        }]
  1108	        if row.na_notes:
  1109	            row_blocks.append({
  1110	                "type": "context",
  1111	                "elements": [
  1112	                    {
  1113	                        "type": "mrkdwn",
  1114	                        "text": "Degraded: " + "; ".join(row.na_notes)[:1900],
  1115	                    }
  1116	                ],
  1117	            })
  1118	        if len(body) + len(row_blocks) > body_budget:
  1119	            break
  1120	        body.extend(row_blocks)
  1121	        n_included += 1
  1122	
  1123	    count_label = (
  1124	        f"{len(rows)} name(s)" if n_included == len(rows)
  1125	        else f"{n_included} of {len(rows)} name(s)"
  1126	    )
  1127	    lead: list[dict] = [
  1128	        {
  1129	            "type": "header",
  1130	            "text": {
  1131	                "type": "plain_text",
  1132	                "text": f"Consensus previews — {count_label}"[:150],
  1133	            },
  1134	        },
  1135	        {
  1136	            "type": "context",
  1137	            "elements": [
  1138	                {
  1139	                    "type": "mrkdwn",
  1140	                    "text": (
  1141	                        f"As of {_fmt_short_date(as_of.isoformat())} · "
  1142	                        "consensus + options-implied moves · free data "
  1143	                        "(yfinance + SQLite)"
  1144	                    ),
  1145	                }
  1146	            ],
  1147	        },
  1148	        {"type": "divider"},
  1149	    ]
  1150	    return lead + body, n_included
  1151	
  1152	
  1153	# ---------------------------------------------------------------------------
  1154	# JSON export (mirrors scripts/export_upcoming_events.py contract)
  1155	# ---------------------------------------------------------------------------
  1156	
  1157	
  1158	def _beat_rate_json(br: BeatRate) -> dict:
  1159	    return {"beats": br.beats, "total": br.total, "source": br.source}
  1160	

exec
/bin/bash -lc "rg -n --glob '"'!codex_feedback/**'"' --glob '"'!scripts/_codex_consensus_preview_review.md'"' \"build_preview_blocks|eps_range\\s*=|fetch_fmp_estimate_range\\(|fetch_fmp_rev_beat_history\\(|fmp_preview_endpoints_ok|row\\.eps_range|n_included|rows\\[:n_included\\]|SLACK_MAX_BLOCKS|event_hour_yf|reaction_date\\(\" ." in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
./notifications.py:50:SLACK_MAX_BLOCKS = 48  # Slack hard cap is 50; leave headroom
./notifications.py:295:    if len(blocks) > SLACK_MAX_BLOCKS:
./notifications.py:296:        blocks = blocks[:SLACK_MAX_BLOCKS]
./notifications.py:793:            if len(blocks) >= SLACK_MAX_BLOCKS:
./test_dedup.py:83:            event_hour_yf   TEXT,
./CLAUDE.md:64:- **yfinance hour fallback via `Ticker.info`.** When Finnhub's `hour` is empty for an upcoming Tier 1/2 event, infer from yfinance's `earningsTimestamp`/`Start`/`End` (Unix-seconds, time-of-day preserved). Reads via `Ticker.info`, NOT `Ticker.calendar` or `get_earnings_dates()` — those wrappers strip time-of-day. Market-session boundaries: `< 09:30 ET → bmo`, `>= 16:00 ET → amc`, mid-session → None (catches 8:30 ET pre-open releases the original `<9 / >16` heuristic missed). Hour stored separately in `event_hour_yf` (v11 column) so `event_hour` and `date_confirmed` keep Finnhub-canonical semantics.
./CLAUDE.md:121:- **Schema is at v12.** `storage.py CURRENT_SCHEMA_VERSION=12`. Migrations are non-destructive. Fresh-DB `CREATE TABLE` duplicates v2–v12 columns; when adding a column, update both the migration and the fresh-DB statement. The `test_fresh_db_schema_matches_migration_path` test in `test_dedup.py` enforces this invariant. Column history: v3=`unseen_run_count`, v4=`date_locked`, v5=`last_xcheck_yf_dates`, v6=`date_confirmed`, v7=backfill for v6, v8=`announcement_url`, v9=`slack_thread_ts` + `slack_question_kind` + `slack_last_reply_ts` + `question_state` + `question_snooze_until` + `question_first_seen` + `kv_store` table, v10=`slack_channel_id`, v11=`event_hour_yf` (yfinance-inferred hour), v12=`call_datetime_utc`+`call_source` (conference-call timestamp).
./consensus_preview.py:19:key AND the kv_store probe flag `fmp_preview_endpoints_ok` == "true"; until
./consensus_preview.py:242:def reaction_date(event_date: str, event_hour: Optional[str]) -> Optional[date]:
./consensus_preview.py:315:    (ticker, event_date, event_hour, event_hour_yf, db_tier, company_name,
./consensus_preview.py:319:    # (v11 event_hour_yf column) when Finnhub left timing blank — the same
./consensus_preview.py:321:    # timing only lives in event_hour_yf renders TBD and gets a wrong (next-
./consensus_preview.py:323:    effective_hour = event_hour or event_hour_yf
./consensus_preview.py:339:    "ticker, event_date, event_hour, event_hour_yf, tier, company_name, "
./consensus_preview.py:483:    rxn = reaction_date(event_date, event_hour)
./consensus_preview.py:678:def fetch_fmp_estimate_range(
./consensus_preview.py:696:def fetch_fmp_rev_beat_history(
./consensus_preview.py:905:        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
./consensus_preview.py:1040:        f"{_render_estimate_range(row.eps_range)}"
./consensus_preview.py:1069:SLACK_MAX_BLOCKS = 48
./consensus_preview.py:1086:def build_preview_blocks(
./consensus_preview.py:1089:    """Build the Block Kit payload and return (blocks, n_included).
./consensus_preview.py:1092:    row would push the payload past SLACK_MAX_BLOCKS; the loop then stops. It
./consensus_preview.py:1093:    never truncates mid-row. `n_included` (rows actually in the payload, in
./consensus_preview.py:1099:    n_included = 0
./consensus_preview.py:1101:    body_budget = SLACK_MAX_BLOCKS - 3
./consensus_preview.py:1121:        n_included += 1
./consensus_preview.py:1124:        f"{len(rows)} name(s)" if n_included == len(rows)
./consensus_preview.py:1125:        else f"{n_included} of {len(rows)} name(s)"
./consensus_preview.py:1150:    return lead + body, n_included
./consensus_preview.py:1175:                "count": row.eps_range.count,
./consensus_preview.py:1176:                "low": row.eps_range.low,
./consensus_preview.py:1177:                "high": row.eps_range.high,
./main.py:90:    build_preview_blocks,
./main.py:368:        source_fingerprint=source_fingerprint, event_hour_yf=hour_yf,
./main.py:864:            cached_yf = (existing or {}).get("event_hour_yf") if existing else None
./main.py:1093:                event_hour_yf=hour_yf,
./main.py:1161:                                    event_hour_yf=hour_yf,
./main.py:1269:                                event_hour_yf=hour_yf,
./main.py:1320:                    event_hour_yf=hour_yf,
./main.py:1687:    blocks, n_included = build_preview_blocks(rows, as_of)
./main.py:1715:    # (build_preview_blocks stops at the Slack block cap and returns how many
./main.py:1718:    posted_rows = rows[:n_included]
./main.py:1719:    if n_included < len(rows):
./main.py:1720:        overflow = rows[n_included:]
./main.py:1724:            n_included, len(rows), len(overflow),
./main.py:1973:                    hour_yf=existing.get("event_hour_yf") if existing else None,
./main.py:2629:            event_hour_yf=hour_yf,
./main.py:2773:        hour_yf = db_row.get("event_hour_yf")
./main.py:3389:        event_hour_yf=new_hour_yf,
./storage.py:41:    # rendering uses (event_hour or event_hour_yf) for the time block.
./storage.py:43:        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
./storage.py:309:                event_hour_yf   TEXT,
./storage.py:349:        "event_hour_yf, call_datetime_utc, call_source "
./storage.py:364:            "event_hour_yf": row[16],
./storage.py:424:        "date_locked, event_hour_yf "
./storage.py:439:            "event_hour_yf": row[13],
./storage.py:460:    event_hour_yf: str | None = None,
./storage.py:471:    `event_hour_yf` is a separate column carrying yfinance-inferred timing
./storage.py:477:    Pass `event_hour_yf=""` to explicitly clear it; pass `None` to leave
./storage.py:498:                event_hour_yf     = COALESCE(?, event_hour_yf),
./storage.py:515:            (event_hour, event_hour_yf, call_datetime_utc, call_source,
./storage.py:544:            INSERT INTO events (ticker, event_date, event_hour, event_hour_yf,
./storage.py:552:            (ticker, event_date, event_hour, event_hour_yf,

codex
The three named fixes look mechanically present: `event_hour_yf` is selected and coalesced, BMO/DMH are same-day, and the only live caller unpacks `(blocks, n_included)`. I’m still checking edge behavior around Slack overflow and FMP response shapes before calling it clean.
exec
/bin/bash -lc "nl -ba consensus_preview.py | sed -n '745,915p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
   745	            break
   746	    if total == 0:
   747	        return BeatRate(None, None, "fmp")
   748	    return BeatRate(beats, total, "fmp")
   749	
   750	
   751	# ---------------------------------------------------------------------------
   752	# Row assembly
   753	# ---------------------------------------------------------------------------
   754	
   755	
   756	def assemble_preview_rows(
   757	    conn: sqlite3.Connection,
   758	    reporters: list[Reporter],
   759	    coverage_map: dict[str, TickerInfo],
   760	    *,
   761	    fmp_key: Optional[str] = None,
   762	    fmp_ok: bool = False,
   763	) -> list[PreviewRow]:
   764	    """Assemble one PreviewRow per reporter.
   765	
   766	    QTD is batched across every reporter + SPY + sector ETFs in a single
   767	    yfinance download. Each per-field fetch is wrapped so a single failure
   768	    degrades that field to n/a rather than losing the row; a catastrophic
   769	    per-ticker error still yields a skeleton row flagged in na_fields.
   770	    """
   771	    today = date.today()
   772	    since = _prior_quarter_end(today)
   773	
   774	    # Batch the QTD panel: reporters + SPY + every mapped sector ETF.
   775	    qtd_universe: set[str] = {"SPY"}
   776	    for rep in reporters:
   777	        qtd_universe.add(rep.ticker.upper())
   778	        etf = sector_etf_for(rep.sector, rep.subsector)
   779	        if etf:
   780	            qtd_universe.add(etf[1].upper())
   781	    qtd_map = fetch_quarter_performance(sorted(qtd_universe), since)
   782	
   783	    rows: list[PreviewRow] = []
   784	    for rep in reporters:
   785	        try:
   786	            rows.append(
   787	                _build_row(conn, rep, since, qtd_map, fmp_key=fmp_key, fmp_ok=fmp_ok)
   788	            )
   789	        except Exception as exc:  # noqa: BLE001 - never drop a row
   790	            logger.warning(f"Preview assembly failed for {rep.ticker}: {exc}")
   791	            row = PreviewRow(
   792	                ticker=rep.ticker,
   793	                company_name=rep.company_name,
   794	                tier=rep.tier,
   795	                event_date=rep.event_date,
   796	                event_hour=rep.event_hour,
   797	                call_datetime_utc=rep.call_datetime_utc,
   798	                eps_mean=rep.eps_estimate,
   799	                rev_mean=rep.rev_estimate,
   800	            )
   801	            row.mark_na("row", f"assembly failed ({exc.__class__.__name__})")
   802	            rows.append(row)
   803	    return rows
   804	
   805	
   806	def _build_row(
   807	    conn: sqlite3.Connection,
   808	    rep: Reporter,
   809	    since: date,
   810	    qtd_map: dict[str, Optional[float]],
   811	    *,
   812	    fmp_key: Optional[str],
   813	    fmp_ok: bool,
   814	) -> PreviewRow:
   815	    row = PreviewRow(
   816	        ticker=rep.ticker,
   817	        company_name=rep.company_name,
   818	        tier=rep.tier,
   819	        event_date=rep.event_date,
   820	        event_hour=rep.event_hour,
   821	        call_datetime_utc=rep.call_datetime_utc,
   822	        eps_mean=rep.eps_estimate,
   823	        rev_mean=rep.rev_estimate,
   824	        qtd_since=since.isoformat(),
   825	    )
   826	
   827	    if rep.eps_estimate is None:
   828	        row.mark_na("consensus.eps.mean", "no EPS estimate in DB")
   829	    if rep.rev_estimate is None:
   830	        row.mark_na("consensus.revenue.mean", "no revenue estimate in DB")
   831	
   832	    # --- price snapshot ---
   833	    try:
   834	        last, day_change = fetch_price_snapshot(rep.ticker)
   835	    except Exception as exc:  # noqa: BLE001
   836	        last, day_change = None, None
   837	        logger.debug(f"price snapshot error {rep.ticker}: {exc}")
   838	    row.last_price = last
   839	    row.day_change_pct = day_change
   840	    if last is None:
   841	        row.mark_na("last_price", "yfinance price unavailable")
   842	
   843	    # --- QTD ---
   844	    row.qtd_ticker_pct = qtd_map.get(rep.ticker.upper())
   845	    row.qtd_spx_pct = qtd_map.get("SPY")
   846	    if row.qtd_ticker_pct is None:
   847	        row.mark_na("qtd.ticker_pct", "yfinance QTD unavailable")
   848	    if row.qtd_spx_pct is None:
   849	        row.mark_na("qtd.spx_pct", "yfinance QTD unavailable for SPY")
   850	    etf = sector_etf_for(rep.sector, rep.subsector)
   851	    if etf is None:
   852	        row.mark_na(
   853	            "qtd.sector_etf",
   854	            f"no mapped ETF for sector '{rep.sector or 'unknown'}'",
   855	        )
   856	    else:
   857	        label, etf_ticker = etf
   858	        row.sector_etf = etf_ticker
   859	        row.sector_etf_label = label
   860	        row.sector_etf_pct = qtd_map.get(etf_ticker.upper())
   861	        if row.sector_etf_pct is None:
   862	            row.mark_na("qtd.sector_etf_pct", f"yfinance QTD unavailable for {etf_ticker}")
   863	
   864	    # --- implied move ---
   865	    try:
   866	        row.implied_move = compute_implied_move(
   867	            rep.ticker, rep.event_date, rep.event_hour
   868	        )
   869	    except Exception as exc:  # noqa: BLE001 - defensive; compute_* shouldn't raise
   870	        row.implied_move = ImpliedMove(
   871	            None, None, "ATM straddle",
   872	            [f"n/a (unexpected error: {exc.__class__.__name__})"],
   873	        )
   874	    if not row.implied_move.ok:
   875	        reason = row.implied_move.notes[0] if row.implied_move.notes else "n/a"
   876	        row.mark_na("implied_move", reason)
   877	
   878	    # --- post-earnings moves ---
   879	    try:
   880	        row.post_moves = fetch_recent_post_moves(conn, rep.ticker, n=4)
   881	    except Exception as exc:  # noqa: BLE001
   882	        row.post_moves = []
   883	        logger.debug(f"post-move error {rep.ticker}: {exc}")
   884	    if not row.post_moves:
   885	        row.mark_na("post_earnings_moves", "no computable prior reactions")
   886	
   887	    # --- EPS beat rate ---
   888	    try:
   889	        row.eps_beat = fetch_eps_beat_history(rep.ticker, max_q=20)
   890	    except Exception as exc:  # noqa: BLE001
   891	        row.eps_beat = BeatRate(None, None, "yfinance")
   892	        logger.debug(f"eps beat error {rep.ticker}: {exc}")
   893	    if not row.eps_beat.ok:
   894	        row.mark_na("beat_rates.eps", "no yfinance surprise history")
   895	
   896	    # --- FMP B-fields (independently gated) ---
   897	    # Estimate count/range: permanently unavailable on the FMP Starter plan
   898	    # (quarterly analyst-estimates -> 402; verified 2026-07-12) -> always n/a.
   899	    row.mark_na(
   900	        "consensus.eps.range",
   901	        "estimate range not available on FMP Starter plan",
   902	    )
   903	    # Revenue beat rate: reachable via /stable/earnings whenever a key is set.
   904	    if fmp_ok and fmp_key:
   905	        row.rev_beat = fetch_fmp_rev_beat_history(rep.ticker, fmp_key, max_q=20)
   906	        if not row.rev_beat.ok:
   907	            row.mark_na("beat_rates.revenue", "FMP revenue-beat history unavailable")
   908	    else:
   909	        row.mark_na("beat_rates.revenue", "no FMP_API_KEY — revenue beat n/a")
   910	
   911	    return row
   912	
   913	
   914	# ---------------------------------------------------------------------------
   915	# Rendering helpers

exec
/bin/bash -lc "nl -ba notifications.py | sed -n '1,90p;270,310p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
     1	"""
     2	Outbound notification surfaces — Slack webhook posting and email rendering.
     3	
     4	Slack posts are synchronous HTTP calls to the configured incoming webhook.
     5	Email sending is not done here; this module produces HTML + plaintext that
     6	the Gmail MCP path turns into a draft for the user to review and send.
     7	"""
     8	
     9	from __future__ import annotations
    10	
    11	import html
    12	import json
    13	import logging
    14	from dataclasses import dataclass
    15	from datetime import date, datetime
    16	
    17	try:
    18	    from zoneinfo import ZoneInfo
    19	except ImportError:  # pragma: no cover - py<3.9 fallback
    20	    ZoneInfo = None
    21	
    22	import requests
    23	
    24	from digest import DigestData, EventRow
    25	from market_data import PostEarningsMove
    26	
    27	
    28	@dataclass
    29	class ResultRow:
    30	    ticker: str
    31	    company_name: str
    32	    event_date: str
    33	    event_hour: str | None
    34	    eps_actual: float | None
    35	    eps_estimate: float | None
    36	    rev_actual: float | None
    37	    rev_estimate: float | None
    38	    tier: int
    39	    move: PostEarningsMove | None = None
    40	    sector: str = ""
    41	    subsector: str = ""
    42	    position: str = ""
    43	    # ISO-8601 datetime (with offset) of the conference call, or None. Drives
    44	    # the "call same day / next morning" context in the results line.
    45	    call_datetime_utc: str | None = None
    46	
    47	logger = logging.getLogger("earnings_agent")
    48	
    49	SLACK_TIMEOUT = 10
    50	SLACK_MAX_BLOCKS = 48  # Slack hard cap is 50; leave headroom
    51	
    52	
    53	class NotificationError(RuntimeError):
    54	    pass
    55	
    56	
    57	# ---------------------------------------------------------------------------
    58	# Transient-network resilience (shared by every outbound Slack POST)
    59	# ---------------------------------------------------------------------------
    60	# A transient DNS/network blip shouldn't lose a Slack post. We make 4 attempts
    61	# total, sleeping 5s/15s/30s BEFORE attempts 2/3/4, and retry ONLY on transient
    62	# transport errors (the requests/urllib network-error families). A successful
    63	# HTTP exchange that carries a bad status is NOT retried here — callers keep
    64	# their own success/non-200 handling, so failure behavior is unchanged once the
    65	# transport itself succeeds. See the Coverage Manager reporting/slack.py
    66	# reference impl (urlopen_with_retry).
    67	_RETRY_BACKOFF = (5, 15, 30)  # seconds to wait BEFORE retry attempts 2..N
    68	
    69	
    70	def _retry_sleep(seconds: float) -> None:
    71	    # Skip the wall-clock wait under pytest (which sets PYTEST_CURRENT_TEST) so
    72	    # the network-error tests don't sleep through the full backoff.
    73	    import os
    74	    import time
    75	    if not os.environ.get("PYTEST_CURRENT_TEST"):
    76	        time.sleep(seconds)
    77	
    78	
    79	def post_with_retry(*args, attempts: int = 4, label: str = "slack post", **kwargs):
    80	    """``requests.post`` with backoff retry on transient transport errors.
    81	
    82	    Re-raises the last ``requests.exceptions.RequestException`` if every attempt
    83	    fails, so existing callers keep their fallback/raise behavior on a genuine
    84	    outage — this only rides through momentary blips. Calls ``requests.post`` by
    85	    name so test monkeypatches still intercept it. Does NOT inspect the response
    86	    status; a successful-but-bad-status response is returned to the caller as-is.
    87	    """
    88	    last = None
    89	    for i in range(attempts):
    90	        try:
   270	                {
   271	                    "type": "mrkdwn",
   272	                    "text": (
   273	                        f"Through {week_end_str} · "
   274	                        f"{digest.month_tracked_count} tracked names in next 30 days"
   275	                    ),
   276	                }
   277	            ],
   278	        },
   279	        {"type": "divider"},
   280	    ]
   281	
   282	    for block in (
   283	        _slack_tier_block("Core Watchlist", digest.tier1_week, show_company=True),
   284	        _slack_tier_block("HC Services + MedTech", digest.tier2_week, show_company=True),
   285	        _slack_tier_block("Other", digest.tier3_week, show_company=True),
   286	    ):
   287	        if block:
   288	            blocks.append(block)
   289	
   290	    ctx = _slack_context_block(digest)
   291	    if ctx:
   292	        blocks.append({"type": "divider"})
   293	        blocks.append(ctx)
   294	
   295	    if len(blocks) > SLACK_MAX_BLOCKS:
   296	        blocks = blocks[:SLACK_MAX_BLOCKS]
   297	    return blocks
   298	
   299	
   300	def post_slack(webhook_url: str, blocks: list[dict], fallback_text: str) -> None:
   301	    """POST a Block Kit message. Raises NotificationError on failure."""
   302	    payload = {"text": fallback_text, "blocks": blocks}
   303	    try:
   304	        # Retry transient network blips; non-200 handling stays below unchanged.
   305	        resp = post_with_retry(
   306	            webhook_url,
   307	            data=json.dumps(payload),
   308	            headers={"Content-Type": "application/json"},
   309	            timeout=SLACK_TIMEOUT,
   310	            label="Slack webhook",

exec
/bin/bash -lc "nl -ba config.py | sed -n '50,70p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
    50	# Earnings channel: heartbeat, weekly digest, results, urgent-T1 moves.
    51	SLACK_WEBHOOK_EARNINGS = os.getenv("SLACK_WEBHOOK_EARNINGS")
    52	# Status-reports channel: date-disagreement notices (cross-check, unseen,
    53	# reconcile auto-fix). Routed away from the earnings channel so user can
    54	# focus on actual earnings updates there. Falls back to the earnings
    55	# channel when unset.
    56	SLACK_WEBHOOK_STATUS = os.getenv("SLACK_WEBHOOK_STATUS")
    57	SLACK_STATUS_CHANNEL_ID = os.getenv("SLACK_STATUS_CHANNEL_ID")
    58	# StreetAccount channel: consensus-metrics pre-earnings previews (§9A).
    59	# Secret name matches sa-monitor's. Unset -> --consensus-preview posts raise
    60	# (fail-loud) rather than silently dropping the brief.
    61	SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
    62	# Bot-token Slack client (powers per-question threads + reply polling).
    63	# Webhook remains the path for heartbeat / digest posts that don't need
    64	# replies. When SLACK_BOT_TOKEN + SLACK_CHANNEL_ID are unset, threaded
    65	# question paths fall back to webhook posting (no replies possible).
    66	SLACK_BOT_TOKEN = os.getenv("SLACK_BOT_TOKEN")
    67	SLACK_CHANNEL_ID = os.getenv("SLACK_CHANNEL_ID")
    68	EMAIL_TO = os.getenv("EMAIL_TO")
    69	
    70	# SEC EDGAR fair-access policy requires a User-Agent with contact info.

exec
/bin/bash -lc "git diff -- main.py config.py consensus_preview.py | sed -n '1,260p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
diff --git a/config.py b/config.py
index 66796a2..b29e12f 100644
--- a/config.py
+++ b/config.py
@@ -1,139 +1,143 @@
-"""
-Configuration for the Earnings Intelligence System.
-
-Loads environment variables, defines paths, constants, and logging setup.
-All configuration is centralized here — other modules import from this file.
-"""
-
-import os
-import logging
-from pathlib import Path
-
-from dotenv import load_dotenv
-
-# ---------------------------------------------------------------------------
-# Environment
-# ---------------------------------------------------------------------------
-
-load_dotenv()
-
-# Finnhub
-FINNHUB_API_KEY = os.getenv("FINNHUB_API_KEY")
-
-# FMP (Financial Modeling Prep) — co-primary earnings source merged with
-# Finnhub for breadth + actuals timeliness. Optional: when unset, the agent
-# degrades to Finnhub-only (logged loudly, not silent). Same key as Coverage
-# Manager's Starter plan.
-FMP_API_KEY = os.getenv("FMP_API_KEY")
-
-# Anthropic key for the cross-check web-search resolver (web_resolver.py).
-# Optional: unset -> disagreements fall back to ask-the-operator Slack threads.
-ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY")
-
-# Google Calendar
-GOOGLE_CALENDAR_ID = os.getenv("GOOGLE_CALENDAR_ID")
-GOOGLE_CREDENTIALS_PATH = os.getenv(
-    "GOOGLE_CREDENTIALS_PATH",
-    str(Path(__file__).parent / "credentials.json"),
-)
-
-# Coverage Manager (source of truth for tickers and tiers)
-COVERAGE_MANAGER_PATH = os.getenv(
-    "COVERAGE_MANAGER_PATH",
-    str(Path(__file__).parent.parent / "Coverage Manager"),
-)
-
-# Timezone
-TIMEZONE = os.getenv("TIMEZONE", "America/New_York")
-
-# Notifications
-# Earnings channel: heartbeat, weekly digest, results, urgent-T1 moves.
-SLACK_WEBHOOK_EARNINGS = os.getenv("SLACK_WEBHOOK_EARNINGS")
-# Status-reports channel: date-disagreement notices (cross-check, unseen,
-# reconcile auto-fix). Routed away from the earnings channel so user can
-# focus on actual earnings updates there. Falls back to the earnings
-# channel when unset.
-SLACK_WEBHOOK_STATUS = os.getenv("SLACK_WEBHOOK_STATUS")
-SLACK_STATUS_CHANNEL_ID = os.getenv("SLACK_STATUS_CHANNEL_ID")
-# Bot-token Slack client (powers per-question threads + reply polling).
-# Webhook remains the path for heartbeat / digest posts that don't need
-# replies. When SLACK_BOT_TOKEN + SLACK_CHANNEL_ID are unset, threaded
-# question paths fall back to webhook posting (no replies possible).
-SLACK_BOT_TOKEN = os.getenv("SLACK_BOT_TOKEN")
-SLACK_CHANNEL_ID = os.getenv("SLACK_CHANNEL_ID")
-EMAIL_TO = os.getenv("EMAIL_TO")
-
-# SEC EDGAR fair-access policy requires a User-Agent with contact info.
-# Default uses the repo owner's public email; override via env var if needed.
-# NB: use `or` not getenv's default arg — CI passes the env var through as an
-# EMPTY string when the (optional) SEC_EDGAR_USER_AGENT secret is unset, and
-# os.getenv treats "" as present, which would send an empty User-Agent and
-# get every EDGAR request rejected (403/malformed) — silently breaking the
-# 8-K auto-correction and the missed-results backstop.
-SEC_EDGAR_USER_AGENT = (
-    os.getenv("SEC_EDGAR_USER_AGENT")
-    or "earnings-agent (jroypeterson@gmail.com)"
-)
-
-# Database
-DB_PATH = Path(__file__).parent / "earnings_events.db"
-
-# Where --weekly-digest writes rendered HTML for Gmail MCP drafting
-DIGEST_HTML_PATH = Path(__file__).parent / "last_digest.html"
-
-# Legacy tickers file (fallback only — Coverage Manager is preferred)
-TICKERS_FILE = Path(__file__).parent / "tickers.txt"
-
-# ---------------------------------------------------------------------------
-# Finnhub query settings
-# ---------------------------------------------------------------------------
-
-CHUNK_DAYS = 7          # Days per Finnhub earnings calendar query
-CHUNK_SLEEP = 1         # Seconds between Finnhub API chunks
-FINNHUB_MAX_RESULTS = 1500  # Finnhub's per-query result cap
-
-# ---------------------------------------------------------------------------
-# Retry settings
-# ---------------------------------------------------------------------------
-
-RETRY_MAX_ATTEMPTS = 3
-RETRY_BASE_DELAY = 2    # Seconds; doubles each retry (2, 4, 8)
-
-# ---------------------------------------------------------------------------
-# Google Calendar API
-# ---------------------------------------------------------------------------
-
-CALENDAR_SCOPES = ["https://www.googleapis.com/auth/calendar.events"]
-CALENDAR_PAGE_SIZE = 250  # Max results per Calendar API page
-
-# ---------------------------------------------------------------------------
-# Timing labels
-# ---------------------------------------------------------------------------
-
-TIMING_LABELS = {
-    "bmo": "Before Market Open",
-    "amc": "After Market Close",
-    "dmh": "During Market Hours",
-}
-
-# ---------------------------------------------------------------------------
-# Tier definitions
-# ---------------------------------------------------------------------------
-
-TIER_1_LABEL = "Core Watchlist"
-TIER_2_LABEL = "HC Services + MedTech"
-TIER_3_LABEL = "Other"
-
-TIER_2_SECTORS = {"Healthcare Services", "MedTech"}
-
-# ---------------------------------------------------------------------------
-# Logging
-# ---------------------------------------------------------------------------
-
-LOG_FORMAT = "%(asctime)s [%(levelname)s] %(message)s"
-
-logging.basicConfig(level=logging.INFO, format=LOG_FORMAT)
-logger = logging.getLogger("earnings_agent")
-
-# Suppress noisy Google API cache warning
-logging.getLogger("googleapiclient.discovery_cache").setLevel(logging.ERROR)
+"""
+Configuration for the Earnings Intelligence System.
+
+Loads environment variables, defines paths, constants, and logging setup.
+All configuration is centralized here — other modules import from this file.
+"""
+
+import os
+import logging
+from pathlib import Path
+
+from dotenv import load_dotenv
+
+# ---------------------------------------------------------------------------
+# Environment
+# ---------------------------------------------------------------------------
+
+load_dotenv()
+
+# Finnhub
+FINNHUB_API_KEY = os.getenv("FINNHUB_API_KEY")
+
+# FMP (Financial Modeling Prep) — co-primary earnings source merged with
+# Finnhub for breadth + actuals timeliness. Optional: when unset, the agent
+# degrades to Finnhub-only (logged loudly, not silent). Same key as Coverage
+# Manager's Starter plan.
+FMP_API_KEY = os.getenv("FMP_API_KEY")
+
+# Anthropic key for the cross-check web-search resolver (web_resolver.py).
+# Optional: unset -> disagreements fall back to ask-the-operator Slack threads.
+ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY")
+
+# Google Calendar
+GOOGLE_CALENDAR_ID = os.getenv("GOOGLE_CALENDAR_ID")
+GOOGLE_CREDENTIALS_PATH = os.getenv(
+    "GOOGLE_CREDENTIALS_PATH",
+    str(Path(__file__).parent / "credentials.json"),
+)
+
+# Coverage Manager (source of truth for tickers and tiers)
+COVERAGE_MANAGER_PATH = os.getenv(
+    "COVERAGE_MANAGER_PATH",
+    str(Path(__file__).parent.parent / "Coverage Manager"),
+)
+
+# Timezone
+TIMEZONE = os.getenv("TIMEZONE", "America/New_York")
+
+# Notifications
+# Earnings channel: heartbeat, weekly digest, results, urgent-T1 moves.
+SLACK_WEBHOOK_EARNINGS = os.getenv("SLACK_WEBHOOK_EARNINGS")
+# Status-reports channel: date-disagreement notices (cross-check, unseen,
+# reconcile auto-fix). Routed away from the earnings channel so user can
+# focus on actual earnings updates there. Falls back to the earnings
+# channel when unset.
+SLACK_WEBHOOK_STATUS = os.getenv("SLACK_WEBHOOK_STATUS")
+SLACK_STATUS_CHANNEL_ID = os.getenv("SLACK_STATUS_CHANNEL_ID")
+# StreetAccount channel: consensus-metrics pre-earnings previews (§9A).
+# Secret name matches sa-monitor's. Unset -> --consensus-preview posts raise
+# (fail-loud) rather than silently dropping the brief.
+SLACK_WEBHOOK_STREET_ACCOUNT = os.getenv("SLACK_WEBHOOK_STREET_ACCOUNT")
+# Bot-token Slack client (powers per-question threads + reply polling).
+# Webhook remains the path for heartbeat / digest posts that don't need
+# replies. When SLACK_BOT_TOKEN + SLACK_CHANNEL_ID are unset, threaded
+# question paths fall back to webhook posting (no replies possible).
+SLACK_BOT_TOKEN = os.getenv("SLACK_BOT_TOKEN")
+SLACK_CHANNEL_ID = os.getenv("SLACK_CHANNEL_ID")
+EMAIL_TO = os.getenv("EMAIL_TO")
+
+# SEC EDGAR fair-access policy requires a User-Agent with contact info.
+# Default uses the repo owner's public email; override via env var if needed.
+# NB: use `or` not getenv's default arg — CI passes the env var through as an
+# EMPTY string when the (optional) SEC_EDGAR_USER_AGENT secret is unset, and
+# os.getenv treats "" as present, which would send an empty User-Agent and
+# get every EDGAR request rejected (403/malformed) — silently breaking the
+# 8-K auto-correction and the missed-results backstop.
+SEC_EDGAR_USER_AGENT = (
+    os.getenv("SEC_EDGAR_USER_AGENT")
+    or "earnings-agent (jroypeterson@gmail.com)"
+)
+
+# Database
+DB_PATH = Path(__file__).parent / "earnings_events.db"
+
+# Where --weekly-digest writes rendered HTML for Gmail MCP drafting
+DIGEST_HTML_PATH = Path(__file__).parent / "last_digest.html"
+
+# Legacy tickers file (fallback only — Coverage Manager is preferred)
+TICKERS_FILE = Path(__file__).parent / "tickers.txt"
+
+# ---------------------------------------------------------------------------
+# Finnhub query settings
+# ---------------------------------------------------------------------------
+
+CHUNK_DAYS = 7          # Days per Finnhub earnings calendar query
+CHUNK_SLEEP = 1         # Seconds between Finnhub API chunks
+FINNHUB_MAX_RESULTS = 1500  # Finnhub's per-query result cap
+
+# ---------------------------------------------------------------------------
+# Retry settings
+# ---------------------------------------------------------------------------
+
+RETRY_MAX_ATTEMPTS = 3
+RETRY_BASE_DELAY = 2    # Seconds; doubles each retry (2, 4, 8)
+
+# ---------------------------------------------------------------------------
+# Google Calendar API
+# ---------------------------------------------------------------------------
+
+CALENDAR_SCOPES = ["https://www.googleapis.com/auth/calendar.events"]
+CALENDAR_PAGE_SIZE = 250  # Max results per Calendar API page
+
+# ---------------------------------------------------------------------------
+# Timing labels
+# ---------------------------------------------------------------------------
+

web search: 
web search: Financial Modeling Prep stable earnings endpoint revenueActual revenueEstimated documentation
web search: 
web search: site:site.financialmodelingprep.com/developer/docs stable earnings FMP
web search: 
web search: https://financialmodelingprep.com/stable/earnings?symbol=AAPL&limit=1&apikey=demo
web search: 
web search: https://financialmodelingprep.com/stable/earnings?symbol=AAPL&limit=1&apikey=demo
codex
I’m going to run a small in-memory probe with monkeypatch-style stubs. That lets me verify the date logic, block count contract, and FMP sort/cap behavior without touching files or the network.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python - <<'PY'
from datetime import date
import consensus_preview as cp

print('reaction bmo', cp.reaction_date('2026-07-17','bmo'))
print('reaction dmh', cp.reaction_date('2026-07-17','dmh'))
print('reaction amc', cp.reaction_date('2026-07-17','amc'))
print('reaction blank', cp.reaction_date('2026-07-17',None))

row = cp.PreviewRow('T','Test Co',1,'2026-07-17','bmo',None)
row.na_notes = ['x']
blocks, n = cp.build_preview_blocks([row] * 40, date(2026,7,13))
print('blocks', len(blocks), 'n_included', n, 'max', cp.SLACK_MAX_BLOCKS)

sample = [
    {'date': '2024-01-01', 'revenueActual': 11, 'revenueEstimated': 10},
    {'date': '2026-01-01', 'revenueActual': None, 'revenueEstimated': 100},
    {'date': '2025-01-01', 'revenueActual': 9, 'revenueEstimated': 10},
    {'date': '2023-01-01', 'revenueActual': 13, 'revenueEstimated': 10},
]
cp._fmp_get_json = lambda url: sample
br = cp.fetch_fmp_rev_beat_history('T','key',max_q=2)
print('fmp', br.beats, br.total, br.source)
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 exited 127 in 0ms:
/bin/bash: line 1: python: command not found

codex
`python` is not installed under that name in this shell, so I’m rerunning the same probe with `python3`.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
from datetime import date
import consensus_preview as cp

print('reaction bmo', cp.reaction_date('2026-07-17','bmo'))
print('reaction dmh', cp.reaction_date('2026-07-17','dmh'))
print('reaction amc', cp.reaction_date('2026-07-17','amc'))
print('reaction blank', cp.reaction_date('2026-07-17',None))

row = cp.PreviewRow('T','Test Co',1,'2026-07-17','bmo',None)
row.na_notes = ['x']
blocks, n = cp.build_preview_blocks([row] * 40, date(2026,7,13))
print('blocks', len(blocks), 'n_included', n, 'max', cp.SLACK_MAX_BLOCKS)

sample = [
    {'date': '2024-01-01', 'revenueActual': 11, 'revenueEstimated': 10},
    {'date': '2026-01-01', 'revenueActual': None, 'revenueEstimated': 100},
    {'date': '2025-01-01', 'revenueActual': 9, 'revenueEstimated': 10},
    {'date': '2023-01-01', 'revenueActual': 13, 'revenueEstimated': 10},
]
cp._fmp_get_json = lambda url: sample
br = cp.fetch_fmp_rev_beat_history('T','key',max_q=2)
print('fmp', br.beats, br.total, br.source)
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 619ms:
reaction bmo 2026-07-17
reaction dmh 2026-07-17
reaction amc 2026-07-20
reaction blank 2026-07-20
blocks 47 n_included 22 max 48
fmp 1 2 fmp

codex
The probe matched the intended behavior: Friday BMO/DMH stay Friday, AMC/blank move to Monday, 40 degraded rows produce 22 included rows with 47 blocks, and FMP sorting skips the upcoming null-actual row before capping. I’m doing one last import/call-site scan for stale assumptions.
exec
/bin/bash -lc "rg -n --glob '"'!codex_feedback/**'"' --glob '"'!scripts/_codex_consensus_preview_review.md'"' \"from consensus_preview|import consensus_preview|build_preview_blocks\\(|build_preview_fallback\\(|fetch_fmp_estimate_range\\(|fetch_fmp_rev_beat_history\\(|eps_range|rev_beat|FMP revenue-beat|fmp_ok\" ." in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
exec
/bin/bash -lc 'git status --short' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 125ms:
./main.py:87:from consensus_preview import (
./main.py:1663:    fmp_ok = bool(FMP_API_KEY)
./main.py:1665:        "FMP revenue-beat %s", "ON" if fmp_ok else "OFF (no FMP_API_KEY)"
./main.py:1671:        fmp_key=(FMP_API_KEY if fmp_ok else None),
./main.py:1672:        fmp_ok=fmp_ok,
./main.py:1687:    blocks, n_included = build_preview_blocks(rows, as_of)
./main.py:1688:    fallback = build_preview_fallback(rows, as_of)

 succeeded in 875ms:
 M .env.example
 M CLAUDE.md
 M LICENSE
 M PLAN.md
 M README.md
 M calendar_sync.py
 M chatfeedback.md
 M codex_feedback/codex_feedback_2026-07-08_204920.md
 M config.py
 A consensus_preview.py
 M coverage.py
 M earnings_agent.py
 M edgar_client.py
 M exports/upcoming_events.json
 M finnhub_client.py
 M fmp_client.py
 M main.py
 M notifications.py
 M requirements.txt
 M scripts/compare_providers.py
 M scripts/migrate_calendar.py
 M scripts/restamp_calendar_props.py
 M slack_api.py
 M storage.py
 M test_dedup.py
 M test_edgar_6k.py
 M test_results_timing.py
 M test_run_integration.py
 M test_subcategories.py
 M test_web_resolver.py
 M tickers.txt
 M ticktick.py
 M web_resolver.py
?? codex_feedback/codex_feedback_2026-07-12_231227.md
?? codex_feedback/codex_feedback_2026-07-12_231953.md
?? codex_feedback/codex_feedback_2026-07-12_233107.md
?? exports/consensus_preview.json
?? scripts/_codex_consensus_preview_review.md

codex
The direct `py_compile` check tried to create `__pycache__` and was blocked by the read-only filesystem. I’m switching to an in-memory `compile()` check so validation stays read-only.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
from pathlib import Path
for p in ['consensus_preview.py', 'main.py', 'config.py']:
    src = Path(p).read_text(encoding='utf-8')
    compile(src, p, 'exec')
    print(p, 'ok')
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
consensus_preview.py ok
main.py ok
config.py ok

exec
/bin/bash -lc "nl -ba storage.py | sed -n '1,90p;280,360p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
     1	"""
     2	SQLite storage layer — schema management, non-destructive migrations,
     3	and query functions for the earnings events database.
     4	
     5	The database is the source of truth for workflow state and historical memory.
     6	Google Calendar is the source of truth for published event state.
     7	"""
     8	
     9	import sqlite3
    10	import logging
    11	from pathlib import Path
    12	
    13	from config import DB_PATH
    14	
    15	logger = logging.getLogger("earnings_agent")
    16	
    17	# ---------------------------------------------------------------------------
    18	# Schema version tracking
    19	# ---------------------------------------------------------------------------
    20	
    21	CURRENT_SCHEMA_VERSION = 12  # Bump when adding migrations
    22	
    23	_MIGRATIONS = {
    24	    # Version 11 → 12: track the earnings conference call timestamp
    25	    # alongside the press-release date. The calendar event itself is
    26	    # anchored to the press release (event_date / event_hour); the call
    27	    # is descriptive context shown in the description. yfinance's
    28	    # earningsCallTimestampStart/End are the primary source. Stored as
    29	    # ISO-8601 with offset (full instant); call_source records which
    30	    # source provided it ('yfinance' for now; 'ir_rss' future).
    31	    12: [
    32	        "ALTER TABLE events ADD COLUMN call_datetime_utc TEXT",
    33	        "ALTER TABLE events ADD COLUMN call_source TEXT",
    34	    ],
    35	
    36	    # Version 10 → 11: yfinance-sourced hour fallback. When Finnhub
    37	    # returns event_hour='' for an upcoming event, we infer 'bmo'/'amc'
    38	    # from yfinance's earnings datetime time-of-day and persist it here.
    39	    # event_hour stays Finnhub-canonical so date_confirmed semantics
    40	    # (notifications.py:765 cross-check verdict) are unchanged. Calendar
    41	    # rendering uses (event_hour or event_hour_yf) for the time block.
    42	    11: [
    43	        "ALTER TABLE events ADD COLUMN event_hour_yf TEXT",
    44	    ],
    45	
    46	    # Version 9 → 10: Track which Slack channel each open-question thread
    47	    # lives in. Cross-check / unseen / reconcile alerts now post to the
    48	    # status-reports channel instead of the earnings channel; the reply
    49	    # poller needs to know which channel to query for each thread. NULL
    50	    # for legacy rows; the poller falls back to SLACK_CHANNEL_ID then.
    51	    10: [
    52	        "ALTER TABLE events ADD COLUMN slack_channel_id TEXT",
    53	    ],
    54	
    55	    # Version 8 → 9: Slack-reply state. Each open question (cross-check
    56	    # disagreement, unseen-ticker, urgent-move) gets its own threaded
    57	    # parent message; replies in that thread drive resolution actions.
    58	    # Also adds a generic kv_store table so things like IR feed URLs can
    59	    # be mutated via Slack reply without committing to the repo.
    60	    9: [
    61	        "ALTER TABLE events ADD COLUMN slack_thread_ts TEXT",
    62	        "ALTER TABLE events ADD COLUMN slack_question_kind TEXT",
    63	        "ALTER TABLE events ADD COLUMN slack_last_reply_ts TEXT",
    64	        "ALTER TABLE events ADD COLUMN question_state TEXT",
    65	        "ALTER TABLE events ADD COLUMN question_snooze_until TEXT",
    66	        "ALTER TABLE events ADD COLUMN question_first_seen TEXT",
    67	        """CREATE TABLE IF NOT EXISTS kv_store (
    68	            key   TEXT PRIMARY KEY,
    69	            value TEXT NOT NULL,
    70	            updated_at TEXT NOT NULL DEFAULT (datetime('now'))
    71	        )""",
    72	    ],
    73	
    74	    # Version 7 → 8: URL of the press release that confirmed this event's
    75	    # date, when one is detected by the RSS announcement scanner. Stored
    76	    # so we can show provenance in alerts and avoid re-confirming via RSS
    77	    # on events that are already anchored to a specific announcement.
    78	    8: [
    79	        "ALTER TABLE events ADD COLUMN announcement_url TEXT",
    80	    ],
    81	
    82	    # Version 6 → 7: Backfill date_confirmed from event_hour. Every row
    83	    # whose hour is bmo/amc/dmh is retroactively flagged confirmed — the
    84	    # v6 migration only added the column, not the initial values, so
    85	    # rows that don't upsert on the next sync (skip_count path) would
    86	    # otherwise stay at the default 0.
    87	    7: [
    88	        "UPDATE events SET date_confirmed = 1 "
    89	        "WHERE LOWER(COALESCE(event_hour, '')) IN ('bmo', 'amc', 'dmh')",
    90	    ],
   280	                id              INTEGER PRIMARY KEY AUTOINCREMENT,
   281	                ticker          TEXT    NOT NULL,
   282	                quarter         TEXT,
   283	                event_date      TEXT    NOT NULL,
   284	                event_hour      TEXT,
   285	                gcal_id         TEXT,
   286	                eps_estimate    REAL,
   287	                eps_actual      REAL,
   288	                rev_estimate    REAL,
   289	                rev_actual      REAL,
   290	                reported        INTEGER NOT NULL DEFAULT 0,
   291	                tier            INTEGER NOT NULL DEFAULT 3,
   292	                source_fingerprint TEXT,
   293	                company_name    TEXT,
   294	                ir_url          TEXT,
   295	                call_url        TEXT,
   296	                ticktick_task_id TEXT,
   297	                unseen_run_count INTEGER NOT NULL DEFAULT 0,
   298	                date_locked     INTEGER NOT NULL DEFAULT 0,
   299	                last_xcheck_yf_dates TEXT,
   300	                date_confirmed  INTEGER NOT NULL DEFAULT 0,
   301	                announcement_url TEXT,
   302	                slack_thread_ts TEXT,
   303	                slack_question_kind TEXT,
   304	                slack_last_reply_ts TEXT,
   305	                question_state  TEXT,
   306	                question_snooze_until TEXT,
   307	                question_first_seen TEXT,
   308	                slack_channel_id TEXT,
   309	                event_hour_yf   TEXT,
   310	                call_datetime_utc TEXT,
   311	                call_source     TEXT,
   312	                created_at      TEXT    NOT NULL DEFAULT (datetime('now')),
   313	                updated_at      TEXT    NOT NULL DEFAULT (datetime('now')),
   314	                UNIQUE(ticker, event_date)
   315	            )
   316	        """)
   317	
   318	        # Create all other tables
   319	        for sql_list in _MIGRATIONS.values():
   320	            for sql in sql_list:
   321	                if sql.strip().upper().startswith("CREATE TABLE"):
   322	                    try:
   323	                        conn.execute(sql)
   324	                    except sqlite3.OperationalError:
   325	                        pass  # Already exists
   326	
   327	        _set_schema_version(conn, CURRENT_SCHEMA_VERSION)
   328	        conn.commit()
   329	        logger.info(f"Created fresh database at schema version {CURRENT_SCHEMA_VERSION}")
   330	    elif current_version < CURRENT_SCHEMA_VERSION:
   331	        _run_migrations(conn, current_version)
   332	    else:
   333	        logger.debug(f"Database at schema version {current_version}, no migrations needed")
   334	
   335	    return conn
   336	
   337	
   338	# ---------------------------------------------------------------------------
   339	# Event queries
   340	# ---------------------------------------------------------------------------
   341	
   342	
   343	def find_existing_event(conn: sqlite3.Connection, ticker: str, event_date: str) -> dict | None:
   344	    """Look up an existing event by ticker + event_date. Returns dict or None."""
   345	    cur = conn.execute(
   346	        "SELECT id, ticker, quarter, event_date, event_hour, gcal_id, "
   347	        "eps_estimate, eps_actual, rev_estimate, rev_actual, reported, "
   348	        "tier, company_name, ticktick_task_id, date_locked, date_confirmed, "
   349	        "event_hour_yf, call_datetime_utc, call_source "
   350	        "FROM events WHERE ticker = ? AND event_date = ?",
   351	        (ticker, event_date),
   352	    )
   353	    row = cur.fetchone()
   354	    if row:
   355	        return {
   356	            "id": row[0], "ticker": row[1], "quarter": row[2],
   357	            "event_date": row[3], "event_hour": row[4], "gcal_id": row[5],
   358	            "eps_estimate": row[6], "eps_actual": row[7],
   359	            "rev_estimate": row[8], "rev_actual": row[9],
   360	            "reported": bool(row[10]), "tier": row[11],

exec
/bin/bash -lc 'rg -n "CURRENT_SCHEMA_VERSION|def init_db|migrate|schema" storage.py' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
2:SQLite storage layer — schema management, non-destructive migrations,
21:CURRENT_SCHEMA_VERSION = 12  # Bump when adding migrations
182:def _get_schema_version(conn: sqlite3.Connection) -> int:
183:    """Get the current schema version from the database."""
185:        cur = conn.execute("SELECT version FROM schema_version ORDER BY version DESC LIMIT 1")
189:        # schema_version table doesn't exist yet
193:def _set_schema_version(conn: sqlite3.Connection, version: int):
194:    """Record the schema version."""
196:        "INSERT INTO schema_version (version, applied_at) VALUES (?, datetime('now'))",
223:        logger.info(f"Running migration to schema version {version}...")
251:        _set_schema_version(conn, version)
252:        logger.info(f"Migration to schema version {version} complete.")
255:def init_db(db_path: Path = DB_PATH) -> sqlite3.Connection:
259:    - Creates the schema from scratch if the database is new.
265:    # Ensure schema_version table exists
267:        CREATE TABLE IF NOT EXISTS schema_version (
274:    current_version = _get_schema_version(conn)
277:        # Fresh database — create the full schema
327:        _set_schema_version(conn, CURRENT_SCHEMA_VERSION)
329:        logger.info(f"Created fresh database at schema version {CURRENT_SCHEMA_VERSION}")
330:    elif current_version < CURRENT_SCHEMA_VERSION:
333:        logger.debug(f"Database at schema version {current_version}, no migrations needed")

exec
/bin/bash -lc "nl -ba main.py | sed -n '1,105p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
     1	"""
     2	Earnings Intelligence System — CLI entry point and orchestrator.
     3	
     4	Usage:
     5	    python main.py                     # Daily sync (normal mode)
     6	    python main.py --dry-run           # Preview without creating calendar events
     7	    python main.py --backfill          # Also look back 30 days for missed earnings
     8	    python main.py --cleanup           # Delete duplicate events from Google Calendar
     9	    python main.py --cleanup --dry-run # Preview which duplicates would be deleted
    10	"""
    11	
    12	import re
    13	import sys
    14	import time
    15	import argparse
    16	import logging
    17	from datetime import date, timedelta
    18	
    19	from config import (
    20	    GOOGLE_CALENDAR_ID,
    21	    FINNHUB_API_KEY,
    22	    SLACK_WEBHOOK_EARNINGS,
    23	    SLACK_WEBHOOK_STATUS,
    24	    SLACK_WEBHOOK_STREET_ACCOUNT,
    25	    SLACK_BOT_TOKEN,
    26	    SLACK_CHANNEL_ID,
    27	    SLACK_STATUS_CHANNEL_ID,
    28	    DIGEST_HTML_PATH,
    29	)
    30	from coverage import (
    31	    load_coverage,
    32	    get_tickers_by_tier,
    33	    get_ticker_info,
    34	    TickerInfo,
    35	    compute_coverage_freshness,
    36	    CoverageHealth,
    37	    COVERAGE_STALENESS_DAYS,
    38	)
    39	from storage import (
    40	    init_db,
    41	    find_existing_event,
    42	    find_event_for_ticker_near_date,
    43	    find_reported_event_for_quarter,
    44	    upsert_event,
    45	    record_estimate_snapshot,
    46	    date_to_quarter,
    47	    set_date_lock,
    48	    list_locked_events,
    49	    is_ticker_date_locked,
    50	    open_question,
    51	    update_question_state,
    52	    advance_reply_watermark,
    53	    list_open_questions,
    54	    kv_get,
    55	    kv_set,
    56	)
    57	from finnhub_client import get_client as get_finnhub_client, fetch_earnings, FinnhubError
    58	from fmp_client import fetch_fmp_earnings, merge_earnings
    59	from config import FMP_API_KEY
    60	from config import ANTHROPIC_API_KEY
    61	from web_resolver import WebVerdict, resolve_disagreement
    62	
    63	# Max web-search resolutions per cross-check run (cost bound; overflow rows
    64	# fall back to the ask-the-operator Slack thread unchanged).
    65	_WEB_RESOLVE_MAX = 6
    66	from calendar_sync import (
    67	    get_calendar_service,
    68	    find_calendar_event,
    69	    create_calendar_event,
    70	    update_calendar_event_description,
    71	    delete_calendar_event,
    72	    build_description,
    73	    cleanup_duplicates,
    74	    _is_confirmed_hour,
    75	    expected_calendar_state,
    76	    calendar_event_drift_kind,
    77	    CalendarError,
    78	)
    79	from ticktick import (
    80	    sync_ticktick_tasks,
    81	    get_ticktick_config,
    82	    show_ticktick_status,
    83	    mark_task_reported,
    84	    TickTickTokenExpired,
    85	)
    86	from digest import build_weekly_digest
    87	from consensus_preview import (
    88	    select_upcoming_reporters,
    89	    assemble_preview_rows,
    90	    build_preview_blocks,
    91	    build_preview_fallback,
    92	    write_preview_export,
    93	)
    94	from notifications import (
    95	    build_slack_blocks,
    96	    build_slack_fallback_text,
    97	    build_email_html,
    98	    build_email_text,
    99	    build_results_slack_blocks,
   100	    build_results_fallback_text,
   101	    build_move_unavailable_blocks,
   102	    build_move_unavailable_fallback,
   103	    build_reconcile_blocks,
   104	    build_reconcile_fallback,
   105	    build_unseen_blocks,

codex
One edge I’m checking is whether malformed FMP list rows can escape the “never raise” contract. The normal sort/cap path is fine, but malformed external response shapes are worth testing because the new gate calls the endpoint whenever a key exists.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
import consensus_preview as cp
for sample in ([None], ['bad'], [{'date': '2025-01-01'}, {'date': '2024-01-01', 'revenueActual': 1, 'revenueEstimated': 1}]):
    cp._fmp_get_json = lambda url, sample=sample: sample
    try:
        br = cp.fetch_fmp_rev_beat_history('T', 'key', max_q=20)
        print(type(sample[0]).__name__ if sample else 'empty', br.beats, br.total)
    except Exception as exc:
        print(type(sample[0]).__name__ if sample else 'empty', 'RAISED', exc.__class__.__name__)
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 382ms:
NoneType RAISED AttributeError
str RAISED AttributeError
dict 0 1

web search: 
web search: https://site.financialmodelingprep.com/developer/docs/stable/earnings
web search: 
web search: site:site.financialmodelingprep.com/developer/docs/ "Earnings" "revenueEstimated"
codex
The malformed FMP-row probe shows a defensive gap for `[None]` or `['bad']`, but that would be an abnormal list shape and the caller still emits a skeleton row rather than crashing the run. I’m not treating that as Critical/High unless it maps to a realistic provider response shape.
exec
/bin/bash -lc "rg -n \"SLACK_WEBHOOK_STREET_ACCOUNT|FMP_API_KEY|CONSENSUS|preview\" .env.example README.md CLAUDE.md PLAN.md --glob '"'!codex_feedback/**'"'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
README.md:124:| `FMP_API_KEY` | all | FMP key (co-primary earnings merge; degrades to Finnhub-only if unset) |
CLAUDE.md:60:- **IR RSS announcement scanner (`--check-announcements`).** Only scans tickers configured in `ir_feeds.json`. Aggregator RSS (Seeking Alpha, Nasdaq, Business Wire, PR Newswire) was tested and **empirically does not carry company IR press releases** — it surfaces analyst commentary and post-release transcripts. So this is Tier 1-only and opt-in per company. When a match is found, upgrades `date_confirmed=1` and stores `announcement_url`. Regex is tight: verb + same-quarter marker, excluding "earnings preview / gears up / what to expect / transcript" noise.
CLAUDE.md:83:same-quarter phantom guard still collapses FMP's extra phantoms. `FMP_API_KEY`
CLAUDE.md:156:Strongly recommended: `FMP_API_KEY` (set 2026-06-04) — enables the FMP co-primary earnings merge (breadth + actuals timeliness). Same key as Coverage Manager's Starter plan. When unset the agent degrades to Finnhub-only (logged loudly). Wired into the daily-sync, post-earnings populate/check, and weekly-digest populate steps.

codex
I’m checking the market-data reaction-date convention too, because the preview date logic needs to match the existing post-earnings move calculation rather than inventing a separate rule.
exec
/bin/bash -lc 'rg -n "def fetch_post_earnings_move|event_hour|amc|dmh|bmo|reaction" market_data.py' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
94:def fetch_post_earnings_move(
98:    Compute the post-earnings stock reaction for a ticker.
101:        → captures the day-of reaction to a pre-market or intraday announcement.
103:        → AMC reports land after close; the reaction is the next day.
154:    if timing_l == "amc":
344:    """Classify a yfinance earnings datetime into 'bmo' / 'amc' / None
348:      < 09:30 America/New_York  -> 'bmo'  (pre-market release)
349:      >= 16:00 America/New_York -> 'amc'  (post-close release)
356:    correctly captured as 'bmo'.
369:        return "bmo"
371:        return "amc"
377:    target press-release date and infer 'bmo'/'amc' from its time-of-day.

exec
/bin/bash -lc "nl -ba market_data.py | sed -n '90,170p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
    90	                    + ("..." if len(missing) > 8 else ""))
    91	    return result
    92	
    93	
    94	def fetch_post_earnings_move(
    95	    ticker: str, earnings_date: str, timing: str | None
    96	) -> PostEarningsMove | None:
    97	    """
    98	    Compute the post-earnings stock reaction for a ticker.
    99	
   100	    BMO / DMH / unknown: move = close(earnings_date) / close(prior trading day) - 1
   101	        → captures the day-of reaction to a pre-market or intraday announcement.
   102	    AMC: move = close(next trading day) / close(earnings_date) - 1
   103	        → AMC reports land after close; the reaction is the next day.
   104	
   105	    Returns None if we don't yet have the closing prices needed (e.g. running
   106	    the check the same evening as an AMC report — next-day data doesn't exist
   107	    yet).
   108	    """
   109	    try:
   110	        ed = date.fromisoformat(earnings_date)
   111	    except ValueError:
   112	        return None
   113	
   114	    # Pull a window wide enough to cover weekends/holidays either side.
   115	    start = ed - timedelta(days=10)
   116	    end = ed + timedelta(days=10)
   117	
   118	    buf_out, buf_err = io.StringIO(), io.StringIO()
   119	    try:
   120	        with redirect_stdout(buf_out), redirect_stderr(buf_err):
   121	            hist = yf.Ticker(ticker).history(
   122	                start=start.isoformat(),
   123	                end=end.isoformat(),
   124	                auto_adjust=True,
   125	            )
   126	    except Exception as exc:
   127	        logger.debug(f"post-earnings move fetch failed for {ticker}: {exc}")
   128	        return None
   129	
   130	    if hist is None or hist.empty:
   131	        return None
   132	
   133	    closes = hist["Close"].dropna()
   134	    if closes.empty:
   135	        return None
   136	
   137	    # Normalize index to date objects for comparison.
   138	    idx_dates = [d.date() for d in closes.index.to_pydatetime()]
   139	
   140	    def _find_on_or_before(target: date) -> tuple[date, float] | None:
   141	        for d, px in sorted(zip(idx_dates, closes.values), reverse=True):
   142	            if d <= target:
   143	                return d, float(px)
   144	        return None
   145	
   146	    def _find_on_or_after(target: date) -> tuple[date, float] | None:
   147	        for d, px in zip(idx_dates, closes.values):
   148	            if d >= target:
   149	                return d, float(px)
   150	        return None
   151	
   152	    timing_l = (timing or "").lower()
   153	
   154	    if timing_l == "amc":
   155	        ref = _find_on_or_before(ed)
   156	        post = _find_on_or_after(ed + timedelta(days=1))
   157	        if not ref or not post or post[0] == ref[0]:
   158	            return None
   159	        ref_px, post_px = ref[1], post[1]
   160	        window_label = "next-day close vs earnings-day close"
   161	    else:
   162	        # BMO, DMH, or unknown timing
   163	        post = _find_on_or_after(ed)
   164	        if not post:
   165	            return None
   166	        ref = _find_on_or_before(post[0] - timedelta(days=1))
   167	        if not ref:
   168	            return None
   169	        ref_px, post_px = ref[1], post[1]
   170	        window_label = "earnings-day close vs prior close"

codex
One operational check: the new preview module is imported at CLI startup, so I verified the local import path compiles and the referenced config name exists. The only direct syntax check that failed was the write-to-`__pycache__` variant, which is expected in this read-only sandbox.
exec
/bin/bash -lc 'rg -n "from market_data|import yfinance|fetch_price|fetch_post_earnings" main.py notifications.py digest.py' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
notifications.py:25:from market_data import PostEarningsMove
digest.py:21:from market_data import fetch_ytd_performance
main.py:159:from market_data import (
main.py:160:    fetch_post_earnings_move,
main.py:361:    move = None if dry_run else fetch_post_earnings_move(ticker, earnings_date, hour)
main.py:437:    fetch_post_earnings_move returns None when the comparison close hasn't
main.py:1942:        move = fetch_post_earnings_move(ticker, record_date, hour)

exec
/bin/bash -lc "nl -ba main.py | sed -n '145,168p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/earnings_agent
 succeeded in 0ms:
   145	    ReplyContext,
   146	    ParsedAction,
   147	    ACT_LOCK,
   148	    ACT_CONFIRM_FH,
   149	    ACT_WAIT,
   150	    ACT_SNOOZE,
   151	    ACT_IGNORE,
   152	    ACT_REPORTED,
   153	    ACT_IR,
   154	    ACT_NOTE,
   155	    ACT_HELP,
   156	    ACT_STATUS,
   157	    ACT_UNKNOWN,
   158	)
   159	from market_data import (
   160	    fetch_post_earnings_move,
   161	    fetch_yfinance_earnings_date,
   162	    fetch_yfinance_hour_for_date,
   163	    fetch_yfinance_call_for_date,
   164	)
   165	from edgar_client import (
   166	    infer_cadence_signal, find_earnings_release_filing, find_results_6k, get_cik,
   167	    reset_request_stats as edgar_reset_request_stats,
   168	    get_request_stats as edgar_get_request_stats,

codex
No Critical/High issues.
tokens used
97,459
No Critical/High issues.
